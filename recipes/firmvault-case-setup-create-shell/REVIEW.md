# Review: Case Setup Create Shell

Approve only if the case shell uses the native FirmVault paths, has no raw PHI, and records only supported intake facts.

Reject if the worker invented a case path, created deprecated JSON state, wrote outside the assigned worktree, skipped the audit entry for a new shell, or added unsupported facts.
