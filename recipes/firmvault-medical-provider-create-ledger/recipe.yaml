slug: firmvault-medical-provider-create-ledger
name: FirmVault Medical Provider - Create Ledger
description: Creates or normalizes one provider ledger using intake, report, contact, or client-update evidence.
when_to_use: Use when a provider is identified and needs a native provider ledger before treatment tracking or records requests.
image: mc-recipe-agent:latest
workspace_mode: worktree
timeout_seconds: 3600
max_concurrent: 1
max_attempts: 2
secrets:
  - OPENROUTER_API_KEY
tools:
  - read_file
  - list_dir
  - grep_files
  - write_file
tags:
  - law-firm
  - firmvault
  - medical-provider
model:
  primary: openai/gpt-5.4-mini
  provider: openrouter
  params:
    temperature: 0.2
