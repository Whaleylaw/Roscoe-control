slug: firmvault-close-case-document-closure
name: FirmVault Close Case - Document Closure
description: Documents final case closure after human-confirmed final letter and archive steps.
when_to_use: Use after closing letter is sent and archive is confirmed.
image: mc-recipe-agent:latest
workspace_mode: worktree
timeout_seconds: 3600
max_concurrent: 1
max_attempts: 2
secrets:
  - OPENROUTER_API_KEY
tools:
  - read_file
  - list_dir
  - grep_files
  - write_file
tags: [law-firm, firmvault, closing]
model:
  primary: openai/gpt-5.4-mini
  provider: openrouter
  params:
    temperature: 0.2
