# FirmVault Workflow Build Roadmap

> **For agentic workers:** This document is the working roadmap for building and live-testing FirmVault workflows in Mission Control. Update the checkboxes, status table, and issue log after each workflow test. Do not choose the next workflow from memory; use this document.

**Goal:** Track the order, implementation readiness, test state, and discovered issues for FirmVault workflows as they are converted into executable Mission Control workflows.

**Architecture:** Workflows are ordered by dependency waves, not a fake strict sequence. Each wave lists workflows that become eligible at the same time, with a recommended test path through the parallel branches. FirmVault remains the case-state source of truth; Mission Control owns workflow/task/session runtime state and this roadmap.

**Tech Stack:** Mission Control workflow YAML, recipe cards, SQLite workflow/task runtime, FirmVault markdown vault, Forgejo PR review gate, local recipe runner.

---

## Current Anchor

**Active test case:** `test-ladder-007-accident-report-found`

**Current live-test state:** BI Claim Setup is complete through the no-acknowledgment follow-up branch. PIP Claim Setup is live-tested through path determination, packet preparation, human send, early acknowledgment arrival, approval processing, quality review, Forgejo PR `#31`, and is now waiting on the 30-day PIP status/exhaustion follow-up. Client Check-In Cadence is waiting on its 14-day follow-up timer. Medical Provider Status is live-tested through provider review, recipe-specific quality review, Forgejo PR `#32`, human treatment-status review, and its 30-day refresh wait. Early Lien Identification completed workflow instance `34`: it found Medicare/Medicaid payor clues but no current evidence-backed lien holder, merged PR `#33`, and left downstream lien workflows blocked. Request Medical Records and Bills completed workflow instance `35`, then was rerun against masked OCR markdown converted from the closed-file North Dixie records/bill fixture through Forgejo PRs `#42`, `#43`, and `#44`. Demand Readiness now has executable workflow YAML and is live-tested both as a standalone recipe task and as first-class workflow instance `38`: task `2204` gathered materials, passed quality review, opened Forgejo PR `#46`, merged through the webhook/reconcile path, and task `2205` completed the human confirmation node. Draft Demand workflow instance `41` completed task `2211`/PR `#52` for final-lien readiness and task `2212`/PR `#53` for attorney-review-by-merge. Send Demand workflow instance `42` completed recipient identification through PR `#55`, human-confirmed send through PR `#56`, and then exited its demand-response wait early when the Progressive offer letter arrived. Track Offers workflow instance `43` is live-tested: PR `#57` seeded the received Progressive offer-letter shadow from the Aesha Holder fixture, passive landmark resolution satisfied `initial_offer_received`, task `2215` logged the $15,000 Progressive BI offer, recipe-specific quality review opened PR `#58`, and PR `#58` merged/reconciled to complete the workflow. Offer Evaluation workflow instance `44` is live-tested: task `2216` prepared the attorney-facing offer evaluation and merged PR `#59`; human review task `2217` recorded client acceptance authority; task `2218` documented the decision and merged PR `#61`; workflow instance `44` is complete. Negotiate Claim workflow instance `45` is live-tested: task `2219` prepared the Progressive acceptance letter/handoff and merged PR `#62`; human-send task `2220` recorded a test-fixture acceptance send; task `2221` documented the send, passed quality review, opened and merged Forgejo PR `#63`, and webhook/reconcile marked the workflow complete. Settlement Processing workflow instance `46` is live-tested: task `2222` prepared the settlement statement and merged PR `#64`; task `2223` prepared the authorization-to-settle package and merged PR `#65`; human review tasks `2224` and `2225` recorded test-fixture client authorization and release execution; early `funds_received` satisfaction completed the wait node; task `2226` documented received settlement funds and merged PR `#66`; webhook/reconcile marked the workflow complete. Settlement Lien Negotiation workflow instance `47` is live-tested on the no-outstanding-lien path: task `2227` audited liens and merged PR `#67`; human review task `2228` accepted the no-lien clearance; task `2229` documented lien negotiation as not applicable and merged PR `#68`; workflow instance `47` is complete. Final Distribution workflow instance `48` is live-tested on the no-lien-cleared path: task `2230` prepared the final distribution statement and merged PR `#69`; human review tasks `2231` and `2232` recorded test-fixture issuance and receipt; task `2233` documented final distribution/trust-zero completion and merged PR `#70`; workflow instance `48` is complete. Close Case workflow instance `49` is live-tested: task `2234` verified closure readiness and merged PR `#71`; task `2235` prepared the closing-letter package and merged PR `#72`; human review tasks `2236` and `2237` recorded test-fixture letter send and archive facts; task `2238` documented final closure and merged PR `#73`; workflow instance `49` is complete. Lien Resolution workflow instance `50` is live-tested on an open Conduent / Passport Medicaid lien fixture: PR `#74` seeded the open lien, task `2239` reviewed lien inventory and merged PR `#75`, task `2240` prepared the final lien request and merged PR `#76`, human task `2241` recorded synthetic send facts, early `final_amounts_received` satisfaction completed the 30-day wait, task `2242` documented the final lien amount and fixture-state correction through PR `#77`, human task `2243` authorized payment, and task `2244` documented payment/resolution through PR `#78`; workflow instance `50` is complete. Final Distribution workflow instance `51` is live-tested on the resolved-lien branch: task `2245` prepared a lien-aware statement and merged PR `#79`, human tasks `2246` and `2247` recorded synthetic issue/receipt facts, task `2248` documented trust-zero/final distribution completion and merged PR `#80`; workflow instance `51` is complete. Next action: decide whether to rerun Close Case on the lien-aware branch or move to the next untested branch/workflow family.

**Rich closed-file fixture:** `/Users/aaronwhaley/Whaley/Aesha Holder`; manifest at `docs/superpowers/plans/2026-04-29-aesha-holder-fixture-manifest.md`. Use this fixture for the accident-report-found path and later BI, KAC/PIP, State Farm, provider, lien, demand, negotiation, and settlement workflows.

| Workflow | Mission Control definition | Status | Live test evidence | Notes |
| --- | --- | --- | --- | --- |
| Case Setup | `firmvault-case-setup` | Live Tested | Workflow instance `25`; tasks `2163`, `2164`; Forgejo PR `#9` manually merged | Created canonical case scaffold and passed human review. |
| Initial Document Collection | `firmvault-document-collection` | Live Tested | Workflow instance `26`; tasks `2165`-`2168`; Forgejo PRs `#10`-`#13` manually merged/closed as needed | Signed-document wait completed early from canonical shadows; final human review task `2168` approved. |
| Accident Report | `firmvault-accident-report` | Issue | Definition and materialization tests added; live DB definition id `6`; task `2169` passed through Forgejo PR `#14`; task `2170` resolved as evidence-backed not applicable from fixture evidence and merged through Forgejo PR `#16`; wait/analyze nodes were skipped; confirmation task `2171` was handled outside the report-found path. Separate report-found test case `test-ladder-007-accident-report-found` completed workflow instance `28`: task `2172` merged via PR `#18`, request/wait branch skipped, task `2174` merged via PR `#19`, and human confirmation task `2175` completed the workflow. | Report-found path is live-tested. Status remains `Issue` until first-class workflow-branch support replaces manual skip/reconciliation for no-report/not-applicable outcomes. |
| Medical Provider Setup | `firmvault-medical-provider-setup` | Live Tested | Workflow instance `29`; task `2176` created River City Orthopedics provider/contact ledgers, passed quality review, and merged via Forgejo PR `#20`; human confirmation task `2177` completed the workflow. | V1 deliberately separates provider setup from records/bills request workflows; no records request was sent. |
| Client Check-In Cadence | `firmvault-client-check-in-cadence` | Issue | Workflow instance `30`; task `2178` started cadence and merged via PR `#21`; task `2179` prepared a pending human check-in handoff and merged via PR `#22`; task `2180` was completed from Aaron's task comment recording a call attempt; node `wait_for_next_check_in` is waiting on its 14-day timer. | Core cadence path is live-tested through the first human contact gate. Status remains `Issue` until the human contact result is written back into FirmVault instead of living only in Mission Control comments. |
| BI Claim Setup | `firmvault-bi-claim-setup` | Live Tested | Workflow instance `31`; task `2181` identified Progressive as the supported BI carrier and merged via PR `#23`; task `2182` prepared the BI LOR human handoff and merged via PR `#24`; duplicate PR `#25` was closed; PR `#26` upgraded the generated BI LOR into a ready-to-send letter; Aaron confirmed regular-mail send on task `2183`; PR `#27` recorded the sent shadow/ledger/activity/workflow-log evidence; task `2184` completed the no-acknowledgment follow-up branch and merged via PR `#28`. | BI setup is live-tested through carrier identification, LOR prep, human send, timer wait, no-acknowledgment review, recipe-specific quality review, Forgejo PR, and workflow completion. |
| PIP Claim Setup | `firmvault-pip-claim-setup` | Issue | Workflow instance `32`; tasks `2185` and `2186` merged through PRs `#29` and `#30`; Aaron completed human-send task `2187`; passive KAC acknowledgment shadow satisfied `pip_approved`; task `2188` processed approval and merged through PR `#31`; node `wait_before_pip_status_followup` is waiting on its 30-day timer or `pip_benefits_exhausted`. | Core setup and early acknowledgment branch are live-tested. Status remains `Issue` until the PIP status/exhaustion follow-up path is tested and the Forgejo webhook/reconcile reliability issue is resolved. |
| Medical Provider Status | `firmvault-medical-provider-status` | Live Tested | Workflow instance `33`; task `2189` created River City Orthopedics `treatment.md`, activity, and workflow-log entries; recipe-specific quality review approved; PR `#32` was manually merged into `codex/test-ladder-007-accident-report-found-base`; task `2190` recorded human review that treatment is not complete; node `wait_for_treatment_status_refresh` is waiting on a 30-day timer or earlier `treatment_complete`. | Reviews provider treatment ledgers, flags stale/unknown statuses, and prepares the treatment-complete gate without requesting records/bills. |
| Early Lien Identification | `firmvault-early-lien-identification` | Live Tested | Workflow instance `34`; task `2191` found Medicare/Medicaid payor clues but no current evidence-backed lien holder, created no speculative lien ledgers, passed manual quality review, and merged through PR `#33`; human review task `2192` confirmed downstream lien workflows remain blocked until concrete lien evidence appears. | Proves lien/payor discovery can document clues without materializing final lien amount, negotiation, or payment tasks. |
| Request Medical Records and Bills | `firmvault-request-medical-records` | Live Tested | Workflow instance `35` completed tasks `2193`-`2199` through PRs `#36`, `#37`, `#38`, `#40`, and `#41`; fixture PR `#39` seeded placeholder receipt docs; PR `#42` replaced those placeholders with masked OCR markdown converted from the North Dixie records/bill PDF; task `2200` processed the converted OCR docs through PR `#43`; task `2201` updated chronology from OCR evidence through PR `#44`. | Provider-scoped records/bills and chronology path is live-tested both with placeholder arrival and realistic converted OCR fixture data. |
| Demand Readiness | `firmvault-demand-readiness` | Live Tested | Executable YAML registered as workflow definition `13`; task `2202` ran `firmvault-demand-gather-materials`, produced `demand/readiness.md`, passed quality review, opened Forgejo PR `#45`, and webhook merge reconciliation marked the task done. First-class workflow instance `38` then completed: task `2204` materialized from the generic case-workflow start path using base ref `codex/test-ladder-008-treatment-complete-base`, passed quality review, merged Forgejo PR `#46`, and advanced to human confirmation task `2205`, which completed the workflow. | Local mirror sync after merged review PRs is implemented with dirty-path protection. PR `#46` sync was intentionally skipped because the local FirmVault checkout had untracked test files on touched paths; this is correct guard behavior, but dirty checkout cleanup remains useful before later broad live testing. |
| Draft Demand | `firmvault-draft-demand` | Live Tested | Old workflow instance `39` exposed and preserved the post-merge attorney-review bug. Corrected workflow instance `41` completed task `2211` through merged PR `#52`, then task `2212` opened PR `#53` with the demand letter, damages summary, demand package, and audit entries. Aaron merged PR `#53`; reconciliation marked task `2212`, node `draft_demand_letter`, and workflow instance `41` complete. | Send Demand is the next Wave 5 workflow. |
| Send Demand | `firmvault-send-demand` | Live Tested | Workflow instance `42`; task `2213` identified Progressive as supported BI demand recipient, created `documents/generated/insurance/bi-progressive-demand-send-handoff.md`, passed quality review, and merged PR `#55`. Task `2214` correctly moved to Human Review until Aaron supplied same-task send confirmation details, then wrote `documents/sent/insurance/bi-progressive-demand-sent.md`, BI ledger updates, activity, and workflow-log evidence; Forgejo PR `#56` merged. | PR `#56` exposed a reconciler drift bug: a merged review PR did not advance when the task had returned to Human Review instead of Quality Review. Live state was repaired and the regression is covered. The workflow is now waiting on `initial_offer_received` or the 30-day timer. |
| Settlement Processing | `firmvault-settlement-processing` | Live Tested | Workflow instance `46`; task `2222` prepared the settlement statement and merged PR `#64`; task `2223` prepared authorization-to-settle and merged PR `#65`; human review tasks `2224` and `2225` recorded client authorization and release execution; early `funds_received` condition bypassed the 14-day wait; task `2226` documented received funds and merged PR `#66`. | Proves Wave 7 can use recipe nodes, human gates, wait nodes with early condition exit, quality review, Forgejo merge, and workflow completion without marking lien payment or final distribution. |
| Settlement Lien Negotiation | `firmvault-settlement-lien-negotiation` | Live Tested | Workflow instance `47`; task `2227` audited settlement liens and merged PR `#67`; human review task `2228` accepted the no-lien clearance; task `2229` documented lien negotiation as not applicable and merged PR `#68`. | Proves no-outstanding-lien bypass path before final distribution. Actual open-lien negotiation still needs a later fixture with Conduent/provider/funding lien ledgers. |
| Final Distribution | `firmvault-final-distribution` | Live Tested | No-lien workflow instance `48`: task `2230` merged PR `#69`, human tasks `2231` and `2232` completed, and task `2233` merged PR `#70`. Resolved-lien workflow instance `51`: task `2245` prepared lien-aware statement and merged PR `#79`, human tasks `2246` and `2247` completed, and task `2248` merged PR `#80`. | Proves both no-lien and paid-lien final distribution paths through recipe nodes, human gates, quality review, Forgejo merge, and workflow completion. Uses synthetic test fixture financial facts because the current case has TBD fee/cost values. |
| Close Case | `firmvault-close-case` | Live Tested | Workflow instance `49`; task `2234` verified closure readiness and merged PR `#71`; task `2235` prepared the closing-letter draft/handoff and merged PR `#72`; human send/archive tasks `2236` and `2237` completed; task `2238` documented case closure and merged PR `#73`. | Proves terminal successful-case closure path after no-lien final distribution. |
| Lien Resolution | `firmvault-lien-resolution` | Live Tested | Workflow instance `50`; fixture PR `#74` seeded an open Conduent / Passport Medicaid lien; task `2239` reviewed lien inventory and merged PR `#75`; task `2240` prepared final lien request and merged PR `#76`; human task `2241` recorded synthetic send facts; early `final_amounts_received` condition completed the 30-day wait; task `2242` documented final amount and merged PR `#77`; human task `2243` authorized payment; task `2244` documented payment/resolution and merged PR `#78`. | Proves open-lien final amount request, wait early-exit, payment authorization, payment documentation, quality review, Forgejo PR merge, and workflow completion. Test reused a prior no-lien/final-distribution case, so PR `#77` also corrected the fixture state by neutralizing the preexisting final-distribution-complete status. |

## Status Key

Use the first matching status in this order:

| Status | Meaning |
| --- | --- |
| Not Started | Workflow exists only in the catalog or original FirmVault docs. |
| Designing | Workflow steps, gates, recipes, or canonical paths are being clarified. |
| Executable YAML | Mission Control workflow YAML exists and can be registered. |
| Recipes Ready | Every recipe node has a specific recipe, `SOUL.md`, and `REVIEW.md`; no generic `firmvault-workflow-task` placeholder remains. |
| Live Tested | Workflow has run through Mission Control task lifecycle, quality review, and Forgejo PR gate where file changes exist. |
| Issue | Workflow ran but exposed a blocker or design gap that must be resolved before relying on it. |

## Dependency Waves

### Wave 0: New Case Foundation

These workflows create the case and finish minimum onboarding.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Case Setup | Live Tested | Manual new intake upload or `law_firm.new_intake_uploaded` | `case_setup_complete`, `client_info_received`, `case_setup_reviewed` | root case file, `Dashboard.md`, case `AGENTS.md`, starter ledgers, `activity/`, `workflow-log/`, `documents/` folders | Intake upload or equivalent new-case seed | Forgejo merge API hangs; manual merge path used. |
| Initial Document Collection | Live Tested | `case_setup_complete == true` | `onboarding_document_checklist_loaded`, `onboarding_documents_requested`, `onboarding_signature_packets_sent`, `contract_signed`, `medical_auth_signed`, `full_intake_complete` | `client/intake.md`, `client/contracts.md`, `client/authorizations.md`, `documents/shadows/client/*-signed.md` | Canonical case scaffold | Needed passive resolver support for `client_info_received`; fixed in Mission Control commit `6a83e35`. |

**Wave 0 completion rule:** Do not move into Wave 1 for a test case until `full_intake_complete` is satisfied and the document collection workflow is complete.

### Wave 1: File Setup Opens After `full_intake_complete`

These workflows become eligible once intake and signed authorizations are in place.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Accident Report | Issue | `full_intake_complete == true` | `accident_report_status_checked`, `accident_report_requested`, `accident_report_received`, `accident_report_obtained`, `accident_report_reviewed` | `accident/police-report.md`, `accident/accident.md`, `accident/liability.md`, `documents/shadows/accident/`, defendant/contact stubs, insurance clues | Phase 0 complete case; optional accident report shadow for passive arrival test | Report-found path with Aesha Holder fixture completed workflow instance `28`: tasks `2172`, `2174`, and `2175` done; PR `#18` and PR `#19` manually merged into `codex/test-ladder-007-accident-report-found-base` then closed in Forgejo. Current v1 still needs declarative branch/skip support so the no-report path automatically skips wait/analyze and moves to confirmation. |
| Medical Provider Setup | Live Tested | `full_intake_complete == true`; provider facts from intake/report/client | `providers_setup`, `provider_treatment_dates_recorded`, `injury_summary_recorded`, `medical_provider_setup_reviewed` | `medical-providers/<provider-slug>/`, provider contact stubs, `client/intake.md`, `activity/`, `workflow-log/` | Phase 0 complete case with provider facts | Report-found test case created `medical-providers/river-city-orthopedics/`, `contacts/river-city-orthopedics.md`, activity, and workflow log through PR `#20`. |
| Client Check-In Cadence | Issue | Case active / full intake complete | `client_check_in_active`, `client_check_in_prepared`, `client_contact_attempted`, possible `provider_referral_needed` | `client/check-ins.md`, `client/contactability.md`, `activity/`, provider notes | Phase 0 complete case | Cadence and handoff nodes live-tested through PR `#21` and PR `#22`; human contact gate task `2180` completed from Aaron's comment; workflow is waiting on the 14-day follow-up timer. Add a deterministic logger or post-human recipe so the contact result lands in FirmVault. |

**Recommended Wave 1 test path:** Build and live-test Accident Report first, then Medical Provider Setup, then Client Check-In.

### Wave 2: Accident Report Outputs Unlock Insurance

These workflows depend on accident report facts or insurance facts found during intake/report analysis.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| BI Claim Setup | Live Tested | `accident_report_obtained == true` or at-fault carrier identified elsewhere | `at_fault_insurance_identified`, `bi_lor_prepared`, `bi_lor_sent`, `bi_acknowledgment_checked`; later acknowledgment arrival can satisfy `bi_claim_acknowledged` | `insurance/bi-<carrier-slug>.md`, `contacts/<adjuster-or-carrier>.md`, `documents/generated/insurance/bi-<carrier-slug>-lor.md`, `documents/sent/insurance/bi-<carrier-slug>-lor-sent.md`, follow-up drafts under `documents/generated/insurance/` | Accident report analyzed or carrier facts manually supplied | Workflow instance `31` completed Progressive carrier identification through PR `#23`, LOR preparation through PR `#24`, ready-to-send LOR upgrade through PR `#26`, sent-mail evidence through PR `#27`, and no-acknowledgment follow-up handling through PR `#28`. Duplicate PR `#25` exposed a manual-merge/reconcile edge case. |
| PIP Claim Setup | Issue | PIP carrier identified or PIP track active | `pip_carrier_identified`, `pip_application_prepared`, `pip_lor_prepared`, `pip_application_filed`, `pip_lor_sent`, `pip_acknowledgment_checked`, optional `pip_approved`, optional `pip_benefits_exhausted` | `insurance/pip-<carrier-slug>.md`, `documents/generated/insurance/pip-<carrier-slug>-application.md`, `documents/generated/insurance/pip-<carrier-slug>-lor.md`, sent/received PIP shadows, `activity/`, `workflow-log/` | Intake/report insurance facts; Aesha Holder fixture includes KAC/Travelers and State Farm PIP/UM-UIM evidence | Workflow instance `32` completed State Farm path determination through task `2185` and PR `#29`; packet preparation completed through task `2186` and PR `#30`; human send task `2187` completed; KAC acknowledgment shadow arrived early and satisfied `pip_approved`; task `2188` normalized approval and merged through PR `#31`; workflow is waiting on 30-day status/exhaustion follow-up. |
| UM/UIM/MedPay/Workers Comp branches | Not Started | Coverage facts indicate branch applies | branch-specific coverage/claim landmarks | `insurance/um-*.md`, `insurance/uim-*.md`, `insurance/medpay-*.md`, `insurance/workers-comp-*.md` | Coverage facts from report/intake/policy docs | Treat as conditional branches, not always-on tasks. |

**Recommended Wave 2 test path:** BI Claim Setup first, then PIP Claim Setup, then conditional coverage branches only when the test case contains facts that require them.

### Wave 3: Treatment Monitoring

These workflows manage treatment and early lien discovery while the client treats.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Medical Provider Status | Live Tested | `providers_setup == true` | `provider_list_reviewed`, `provider_status_updated`, `treatment_status_reviewed`, optional `treatment_complete`, `provider_followups_flagged` | `medical-providers/<provider-slug>/provider.md`, `medical-providers/<provider-slug>/treatment.md`, `medical-providers/<provider-slug>/records-bills.md`, provider ledgers, `client/check-ins.md`, `activity/`, `workflow-log/` | Provider ledgers exist | Workflow instance `33` completed provider review task `2189` through PR `#32`; human review task `2190` concluded treatment is not complete and scheduled the 30-day refresh wait. Provider-scoped passive condition support for treatment status remains needed before downstream records/bills automation. |
| Referral to New Provider | Not Started | `provider_referral_needed == true` | `referred_provider_selected`, `referred_provider_appointment_scheduled`, `providers_setup` | `medical-providers/<provider-slug>/`, contacts, `client/check-ins.md` | Client check-in indicates referral need | Requires human review at referral-need gate. |
| Early Lien Identification | Live Tested | `providers_setup == true` or payor/lien clues found | `health_coverage_categorized`, `lien_clues_reviewed`, `liens_identified`, `lien_inventory_reviewed`; downstream `liens_opened` remains blocked until a concrete holder exists | `liens/`, provider bills/payor notes, `documents/shadows/`, `documents/received/`, `activity/`, `workflow-log/` | Provider or insurance facts exist | Workflow instance `34` found Medicare/Medicaid payor clues in the intake packet shadow but no current evidence-backed lien holder; no speculative lien file was created and downstream lien workflows remained blocked. |
| Treatment Complete Gate | Not Started | All active providers done treating | `treatment_complete` | provider ledgers and case root landmarks | Provider treatment statuses exist | This is a gate that unlocks records/bills and demand work. |

### Wave 4: Records, Bills, and Chronology

These workflows run after provider treatment completion or when records are otherwise needed.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Request Medical Records and Bills | Live Tested | provider `treatment_complete == true` or manual records request event | `medical_auth_verified`, provider request prepared/sent/follow-up/escalation landmarks | `client/authorizations.md`, `documents/shadows/client/*authorization*`, `medical-providers/<provider-slug>/requests/`, `activity/`, `workflow-log/` | Provider ledger with completed treatment and signed auth | Workflow instance `35` completed authorization verification (`2193`, PR `#36`), request preparation (`2194`, PR `#37`), human-confirmed send (`2195`, PR `#38`), and final review (`2199`). Human-send recipe needed stricter ledger-update instructions after comment-only retries. |
| Receive and Process Records/Bills | Live Tested | provider records or bills arrive | `records_and_bills_processed` | provider documents, records/bills ledger, bill totals/payors, `activity/` | Incoming provider record/bill shadows | Provider-scoped passive resolver now supports `law_firm.provider.records_or_bills_received` and `records_and_bills_received`. Fixture PR `#39` seeded received record/bill shadows; task `2197` processed receipt through PR `#40`. PR `#42` then replaced placeholders with masked OCR markdown from a scanned PDF; task `2200` processed the converted docs through PR `#43`. |
| Medical Chronology | Live Tested | records processed | `medical_chronology_updated` | provider chronology or case chronology, records shadows | Processed records | Task `2198` updated the River City Orthopedics chronology from received shadows and merged through PR `#41`, explicitly avoiding unsupported clinical detail. Task `2201` reran chronology from converted OCR medical records and merged through PR `#44`. |

**Recommended Wave 4 test path:** Use one provider test fixture first. Prove authorization verification, request preparation, human send gateway, wait release, receipt processing, and chronology update before adding multi-provider aggregation.

### Wave 5: Demand

These workflows start when treatment and records/bills work are complete enough to value the case.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Gather Demand Materials | Issue | treatment complete plus records/bills/chronology available | `all_records_received`, `all_bills_received`, `medical_chronology_updated`, `demand_materials_gathered`, `damages_calculated` | `demand/readiness.md`, provider ledgers, liens, damages summaries | Records, bills, liens, chronology ready | Executable YAML and recipe exist; task `2202` live-tested through PR `#45`. Status remains `Issue` until the workflow can be started as a first-class workflow instance instead of a manually materialized recipe task. |
| Draft Demand | Live Tested | `demand_readiness_reviewed == true` or manual start | `demand_lien_process_checked`, `demand_drafted`, `attorney_reviewed_demand` | `demand/readiness.md`, `demand/demand-letter.md`, `demand/damages-summary.md`, `demand/demand-package.md`, `liens/final-lien-readiness.md`, supporting exhibits | Demand materials ready | Corrected workflow instance `41` completed through task `2211`/PR `#52` and task `2212`/PR `#53`. The open PR itself served as the attorney approval gate; merge completed the workflow. The lien check is internal only: if a final lien process has not started, start/surface that workflow in parallel; do not include or mention lien facts in the demand. |
| Send Demand | Live Tested | `attorney_reviewed_demand == true` | `demand_recipients_identified`, `demand_sent` | `documents/sent/insurance/`, insurance claim ledgers, `activity/` | Approved demand draft | Workflow instance `42` completed recipient identification through PR `#55` and human-confirmed send evidence through PR `#56`; it now waits for response or timer follow-up. Human send gateway is required before `demand_sent` is written. |

### Wave 6: Negotiation

These workflows track offers, evaluate them, and negotiate.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Track Offers | Live Tested | `demand_sent == true` | `initial_offer_received`, `offer_documented` | `negotiation/offers.md`, insurance claim ledgers, `documents/received/insurance/`, `activity/`, `workflow-log/` | Demand sent | Workflow instance `43` waited after demand send, exited early when PR `#57` added `documents/received/insurance/progressive-offer-letter.md`, then task `2215` recorded the $15,000 Progressive BI offer and merged through PR `#58`. Passive resolver now recognizes `demand_sent` and `initial_offer_received` from canonical file placement. |
| Offer Evaluation | Live Tested | `offer_documented == true` | `offer_evaluated`, `net_to_client_prepared`, `client_advised_of_offer`, `offer_decision_documented` | `negotiation/offers.md`, `negotiation/offer-evaluation.md`, insurance ledgers, lien summaries, activity, workflow-log | Offer logged | Workflow instance `44` completed: task `2216` prepared the evaluation package and merged PR `#59`; human review task `2217` recorded client acceptance authority; task `2218` documented acceptance authority without sending acceptance or marking settlement reached, then merged PR `#61`. |
| Negotiate Claim | Live Tested | `offer_decision_documented == true` | `negotiation_response_prepared`, `negotiation_response_human_sent`, `negotiation_result_documented`, conditional `settlement_reached` after human-confirmed external acceptance | `negotiation/`, `insurance/`, `documents/generated/insurance/`, `documents/sent/insurance/`, `settlement/`, `activity/`, `workflow-log/` | Evaluated offer with client decision | Workflow instance `45` completed: task `2219` prepared `bi-progressive-acceptance-letter.md` and `bi-progressive-acceptance-handoff.md`, then PR `#62` merged; human-send task `2220` recorded a test-fixture acceptance send; task `2221` documented the acceptance send, passed quality review, opened PR `#63`, and PR `#63` merged/reconciled to complete the workflow. |

### Wave 7: Settlement and Distribution

These workflows process settlement, liens, and final distribution.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Settlement Processing | Live Tested | `settlement_reached == true` | settlement statement, authorization, client authorization, release, funds landmarks | `settlement/`, `documents/generated/settlement/`, `documents/received/settlement/`, `activity/`, `workflow-log/` | Settlement reached | Workflow instance `46` completed through PRs `#64`, `#65`, and `#66`; test-fixture human comments completed authorization/release nodes; early `funds_received` satisfied the wait before the 14-day timer. It intentionally does not pay liens or complete final distribution. |
| Settlement Lien Negotiation | Live Tested | `settlement_reached == true` and open liens/no-lien review needed | `settlement_liens_audited`, `liens_prioritized`, available funds calculated, `settlement_lien_strategy_reviewed`, `liens_negotiated` | `liens/`, `settlement/distribution.md`, `documents/generated/settlement/`, `activity/`, `workflow-log/` | Settlement and liens/no-lien state exist | Workflow instance `47` completed the no-outstanding-lien path through PRs `#67` and `#68`. Open-lien resolution now has a separate tested workflow instance `50`. |
| Lien Resolution | Live Tested | liens exist or lien track active | `liens_identified`, `liens_opened`, `final_amounts_requested`, `final_amounts_received`, `liens_negotiated`, `liens_paid` | `liens/`, `documents/generated/liens/`, `documents/sent/liens/`, `documents/received/liens/`, `settlement/distribution.md`, `activity/`, `workflow-log/` | Open lien inventory and settlement/payment context | Workflow instance `50` completed through PRs `#75`-`#78` after fixture PR `#74`. It tested final-lien request drafting, human-send gate, 30-day wait early-exit, final amount documentation, payment authorization, and payment documentation. |
| Final Distribution | Live Tested | `funds_received == true` and `liens_negotiated == true` / no liens applicable | `final_distribution_statement_prepared`, `client_distribution_issued`, `client_distributed`, `trust_account_zeroed`, `final_distribution_complete` | `settlement/distribution.md`, `settlement/settlement.md`, `documents/generated/settlement/`, `documents/sent/settlement/`, activity/workflow-log | Settlement funds and no-lien clearance or resolved-lien fixture | Workflow instance `48` completed the no-lien path through PRs `#69` and `#70`. Workflow instance `51` completed the resolved-lien path through PRs `#79` and `#80` after the append-only/reviewer-contract correction on task `2245`. |

### Wave 8: Litigation

Litigation is a separate branch, not part of the first non-litigation PI happy path.

### Phase 8: Closed

Closed workflows wind down successful, declined, dismissed, or otherwise terminal cases.

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Close Case | Live Tested | `final_distribution_complete == true` for successful settlement path | `all_obligations_verified`, `final_letter_prepared`, `final_letter_sent`, `case_archived`, `case_closed` | `closing/`, case root, `Dashboard.md`, `documents/generated/client/`, activity/workflow-log | Final distribution complete | Workflow instance `49` completed through PRs `#71`, `#72`, and `#73` on the no-lien settled test path. Declined/litigation-dismissed variants are not yet modeled. |

| Workflow | Build status | Trigger / dependency gate | Output landmarks | Canonical FirmVault paths | Test state needed | Issues |
| --- | --- | --- | --- | --- | --- | --- |
| Litigation branch | Not Started | impasse, limitations, attorney decision, or suit filed | complaint, service, discovery, deposition, mediation, trial landmarks | `litigation/` subtree | A litigation-triggered test case | Current Mission Control workflow YAML does not yet include a complete litigation workflow file. Add after non-litigation path stabilizes. |

## Recommended Build/Test Queue

- [x] Wave 0.1: Case Setup live test complete.
- [x] Wave 0.2: Initial Document Collection live test complete.
- [x] Wave 1.1: Design and implement executable Accident Report workflow.
- [x] Wave 1.2: Live-test Accident Report on `test-ladder-006-template-tool` and `test-ladder-007-accident-report-found`. Task `2169` passed; task `2170` passed through quality review and PR `#16` on the not-applicable path, then required manual branch reconciliation. Report-found workflow instance `28` completed: task `2172` merged via PR `#18`, request/wait branch reconciled, task `2174` merged via PR `#19`, and human confirmation task `2175` completed the workflow. Keep branch/skip support as an open implementation issue before relying on the no-report path broadly.
- [x] Wave 1.3: Design and implement Medical Provider Setup.
- [x] Wave 1.4: Live-test Medical Provider Setup. Workflow instance `29` completed through task `2176`, quality review, PR `#20`, and human confirmation task `2177`.
- [x] Wave 1.5: Design Client Check-In Cadence after provider setup assumptions are stable.
- [x] Wave 1.6: Complete live Client Check-In Cadence through first human/client-contact result. Task `2180` was completed from Aaron's comment, and node `wait_for_next_check_in` is waiting on its 14-day timer.
- [x] Wave 2.1: Complete live BI Claim Setup through the human-send gate. Tasks `2181` and `2182` passed quality review and Forgejo PR gate; PR `#26` upgraded the generated Progressive BI LOR into a ready-to-send letter; Aaron confirmed regular-mail send on task `2183`; PR `#27` recorded the sent evidence.
- [x] Wave 2.1a: Test BI acknowledgment handling after the five-day timer. Task `2184` found no Progressive acknowledgment in the vault, created a human-ready follow-up draft, updated the BI ledger without inventing unsupported facts, passed recipe-specific quality review, merged through PR `#28`, and completed workflow instance `31`.
- [x] Wave 2.2: Complete live PIP Claim Setup through early acknowledgment processing. Workflow instance `32` completed path determination (`2185`, PR `#29`), packet preparation (`2186`, PR `#30`), human send (`2187`), passive acknowledgment unblocking, and approval processing (`2188`, PR `#31`). It is now waiting on the 30-day PIP status/exhaustion follow-up or `pip_benefits_exhausted`.
- [x] Wave 3.1: Design and live-test provider treatment status and treatment-complete gates. Workflow instance `33` completed task `2189`, quality review, Forgejo PR `#32`, human treatment review task `2190`, and entered `wait_for_treatment_status_refresh` with a 30-day timer.
- [x] Wave 3.2: Design and live-test Early Lien Identification so lien workflows do not materialize downstream lien tasks until a concrete lien holder exists. Workflow instance `34` completed tasks `2191` and `2192`; PR `#33` merged.
- [x] Wave 4.1: Live-test provider-scoped Request Medical Records and Bills. Workflow instance `35` completed through tasks `2193`-`2199`; PRs `#36`, `#37`, `#38`, `#40`, and `#41` merged, and fixture PR `#39` seeded passive received documents.
- [x] Wave 4.2: Add provider records/bills passive document-arrival rules. Added provider-scoped passive support for `records_or_bills_received` and `records_and_bills_received`; early receipt now skips follow-up/escalation nodes through `skip_when_condition`.
- [x] Wave 4.3: Replace placeholder received records/bills with masked OCR markdown converted from the closed-file fixture. PR `#42` converted the scanned North Dixie records/bill PDF into canonical provider `records.md` and `bills.md`; task `2200` processed receipt via PR `#43`; task `2201` updated chronology from OCR records via PR `#44`.
- [x] Wave 5.1: Add executable Demand Readiness workflow YAML and `firmvault-demand-gather-materials` recipe with recipe-specific review.
- [x] Wave 5.2: Live-test gather demand materials against the OCR-backed provider fixture. Task `2202` produced `demand/readiness.md`, passed recipe-specific quality review, opened PR `#45`, and webhook reconciliation marked the task done after merge.
- [x] Wave 5.3: Add a generic case workflow-start path and UI action so `firmvault-demand-readiness` can start as a first-class workflow instance.
- [x] Wave 5.4: Add reliable local FirmVault mirror synchronization after Forgejo PR merges, then rerun Demand Readiness as a first-class workflow instance through its first recipe node and human confirmation node. Workflow instance `38` completed through task `2204`, PR `#46`, and human confirmation task `2205`; local sync correctly skipped dirty touched paths instead of overwriting untracked test files.
- [x] Wave 5.5: Normalize the test-ladder fixture's provider treatment dates before Draft Demand. Forgejo PR `#47` aligned the root/Dashboard/intake incident date with the accident report date `2019-05-04`, aligned River City Orthopedics treatment dates with OCR records/bills `2019-05-14` through `2019-07-10`, updated editable ledgers/generated shadows, and added append-only correction activity/workflow-log entries.
- [x] Wave 5.6: Complete Draft Demand live test. Corrected workflow instance `41` passed final-lien readiness through merged PR `#52`, opened attorney-review PR `#53` for task `2212`, and completed after Aaron merged PR `#53` and reconciliation marked the task/workflow done. It consumes `demand/readiness.md`, medical chronology, bills, accident/liability facts, and insurance status without drafting unsupported damages or policy facts. Lien status is checked only to ensure final-lien workflow work has started when needed; lien facts do not go in demand content.
- [x] Wave 5.7: Design and live-test Send Demand with a human send gateway. Workflow instance `42` completed recipient identification through PR `#55`. Task `2214` correctly required same-task human send confirmation, then wrote sent-demand evidence and merged PR `#56`. After repairing the PR-merge drift bug, node `send_demand_package` completed and `wait_for_response` entered a scheduled 30-day wait, still unblockable by `initial_offer_received`.
- [x] Wave 6.1: Design and live-test Track Offers. Workflow instance `43` waited after `demand_sent`, exited early when PR `#57` added the received Progressive offer-letter shadow, task `2215` logged the $15,000 Progressive BI offer, recipe-specific quality review opened PR `#58`, and PR `#58` merged/reconciled to complete the workflow.
- [x] Wave 6.2: Design and live-test Offer Evaluation from the documented Progressive initial offer. Workflow instance `44` completed through attorney evaluation PR `#59`, client decision human review task `2217`, decision-documentation task `2218`, and PR `#61`. It recorded acceptance authority only and did not send external acceptance, mark settlement reached, or start settlement processing.
- [x] Wave 6.3: Design and live-test Negotiate Claim / settlement acceptance handoff from client acceptance authority. Workflow instance `45` completed: task `2219` prepared the Progressive acceptance letter/handoff and merged PR `#62`; human-send task `2220` recorded a test-fixture acceptance send; task `2221` documented the send, passed quality review, opened PR `#63`, and PR `#63` merged/reconciled to complete the workflow.
- [x] Wave 7.1: Design and live-test Settlement Processing from `settlement_reached`. Workflow instance `46` completed: task `2222` prepared the settlement statement and merged PR `#64`; task `2223` prepared the authorization-to-settle package and merged PR `#65`; human review tasks `2224` and `2225` recorded test-fixture client authorization and release execution; early `funds_received` completed the wait node; task `2226` documented received funds and merged PR `#66`. The workflow stops before lien payment or final distribution.
- [x] Wave 7.2: Design and live-test Settlement Lien Negotiation no-outstanding-lien path. Workflow instance `47` completed: task `2227` audited settlement liens and merged PR `#67`; human strategy task `2228` accepted no-lien clearance; task `2229` documented lien negotiation as not applicable and merged PR `#68`. This clears the no-lien branch for final distribution while leaving actual open-lien negotiation for a later lien fixture.
- [x] Wave 7.3: Design and live-test Final Distribution no-lien path. Workflow instance `48` completed: task `2230` prepared a reconciled final distribution statement from same-task test figures and merged PR `#69`; human issue/receipt tasks `2231` and `2232` recorded test-fixture distribution and acceptance; task `2233` documented final distribution complete and trust balance zero, then merged PR `#70`.
- [x] Wave 7.4: Design and live-test Lien Resolution open-lien path. Fixture PR `#74` seeded an open Conduent / Passport Medicaid lien. Workflow instance `50` completed: task `2239` reviewed lien inventory and merged PR `#75`; task `2240` prepared the final lien request and merged PR `#76`; human send task `2241` recorded test-fixture send facts; early `final_amounts_received` completed the 30-day wait; task `2242` documented the final lien amount and corrected the fixture's prior no-lien distribution state through PR `#77`; human payment-review task `2243` authorized payment; task `2244` documented lien payment/resolution and merged PR `#78`.
- [x] Wave 7.5: Live-test Final Distribution against a resolved-lien fixture path. Workflow instance `51` completed: task `2245` prepared the lien-aware final distribution statement and merged PR `#79`; human issue/receipt tasks `2246` and `2247` recorded test-fixture client distribution facts; task `2248` documented lien-aware final distribution completion/trust zero and merged PR `#80`.
- [x] Phase 8.1: Design and live-test Close Case after successful no-lien final distribution. Workflow instance `49` completed: task `2234` verified closure readiness and merged PR `#71`; task `2235` prepared closing-letter draft/handoff and merged PR `#72`; human send/archive tasks `2236` and `2237` recorded test-fixture facts; task `2238` documented closed status and merged PR `#73`.

## Per-Workflow Test Checklist

Copy this checklist under the workflow section when actively working on it.

- [ ] Fixture/test case state exists in canonical FirmVault paths.
- [ ] Workflow definition is executable Mission Control YAML, not only a catalog entry.
- [ ] Every recipe node has a specific recipe, `SOUL.md`, `REVIEW.md`, and reference files.
- [ ] No normal-path step tells the agent to broadly search for canonical documents.
- [ ] Passive conditions are defined for canonical file arrival or ledger truth.
- [ ] First task materializes in the expected column.
- [ ] Recipe runner completes work in a task worktree.
- [ ] Human review gate appears where required.
- [ ] Quality review can approve/reject without looping.
- [ ] Forgejo PR opens for file-changing tasks.
- [ ] Merge/reconcile marks task done and advances workflow.
- [ ] Workflow completion or wait state is visible in Mission Control.
- [ ] Issues discovered are logged below.

## Running Issue Log

| Date | Area | Issue | Status | Resolution / next action |
| --- | --- | --- | --- | --- |
| 2026-05-02 | Final Distribution resolved-lien branch | Task `2245` initially edited existing `0115` activity/workflow-log entries from the earlier no-lien final distribution branch and the reviewer also treated preexisting downstream close-case artifacts as if they were created by the current task. | Fixed | Tightened `firmvault-final-distribution-prepare-statement` SOUL/REVIEW to require new append-only log entries and to judge downstream completion/closure artifacts only if this task added or edited them. Restored the original `0115` logs in the task worktree, added new `0145` lien-aware logs, merged PR `#79`, then completed lien-aware issue/receipt and zero-trust through PR `#80`. |
| 2026-05-02 | Lien Resolution fixture | Open-lien workflow instance `50` reused `test-ladder-007-accident-report-found` after the no-lien final distribution and close-case workflows had already marked `settlement/distribution.md` as `final_distribution_complete`. Recipe review correctly rejected task `2242` because final-lien amount documentation must not advance or retain final distribution completion. | Guarded | Treated as a fixture-state conflict, not a recipe approval problem. Same-task correction comment authorized neutralizing the stale final-distribution-complete state for the open-lien branch. Task `2242` reran and merged PR `#77`; workflow instance `50` then completed through PR `#78`. Future branch tests should use dedicated fixture base states or explicitly reset incompatible downstream landmarks before testing alternate branches. |
| 2026-05-01 | Draft Demand | Task `2210` handoff claimed `demand-letter.md`, `damages-summary.md`, and `demand-package.md` were updated, but PR `#51` only changed `demand-letter.md` plus audit/checkpoint files. Initial guard was too strict for reruns where required artifacts already exist unchanged. | Fixed | Added a deterministic `firmvault-demand-draft-letter` review-PR validator that blocks PR publication unless all three required demand artifacts exist in the worktree. Updated `SOUL.md` and `REVIEW.md` so handoffs must distinguish changed files from artifacts reviewed and left unchanged. Corrected rerun task `2212` opened PR `#53` with all three demand artifacts in the diff; merge/reconcile completed workflow instance `41`. |
| 2026-05-01 | Draft Demand | Old workflow YAML created attorney review after PR `#49` had already merged, defeating the PR approval gate. | Guarded | Corrected `firmvault-draft-demand` so the draft node's open Forgejo PR is the attorney review/merge gate and removed the separate post-merge attorney-review node for future workflow instances. Current task `2208` is stale from the old definition and should be reconciled or left as an affected test artifact. |
| 2026-05-01 | Send Demand | The Send Demand workflow reached the true external-communication gate. Task `2214` cannot mark `demand_sent` without same-task human confirmation, and the approved demand artifacts still contained `[Attorney direction needed]` for demand amount and response deadline. | Fixed | This was correct safety behavior. Aaron supplied same-task human confirmation with send date, method, recipient, demand amount, and response deadline. The recipe then wrote sent-demand evidence and opened PR `#56`. |
| 2026-05-01 | Review PR Reconciliation | PR `#56` merged in Forgejo and the PR row was marked merged, but task `2214` stayed in Human Review and workflow instance `42` did not advance because the reconciler only completed tasks currently in `quality_review`. | Fixed | Broadened the merge transition guard to allow `review` as well as `quality_review` for a merged review PR, added regression coverage, repaired live task `2214`, and advanced workflow instance `42` into `wait_for_response` with its 30-day timer scheduled. |
| 2026-05-01 | Track Offers | A new workflow instance started after `demand_sent`, but passive landmark resolution initially could not satisfy `demand_sent` or `initial_offer_received` from canonical demand/offer files. | Fixed | Added passive resolution for sent-demand evidence and offer-arrival evidence. PR `#57` seeded the received Progressive offer letter; passive resolution completed the Send Demand wait and Track Offers wait, then task `2215` logged the offer and merged through PR `#58`. |
| 2026-04-28 | Forgejo | Forgejo merge API can hang after PR approval. | Fixed | Root cause was Forgejo doing merge local-copy work under bind-mounted `/data/gitea/tmp/local-repo`, making API merges take long enough for clients to time out. Updated Forgejo `app.ini` to use container-local `/tmp/forgejo-local-repo`; throwaway PR `#35` merged through the API in 6.36s after PR `#34` proved the same path took 165.63s before the change. |
| 2026-04-28 | Passive landmarks | `client_info_received` did not re-satisfy later workflow dependencies even though case setup completed. | Fixed | Added passive resolver support in Mission Control commit `6a83e35`. |
| 2026-04-28 | Audit safety | Agents can accidentally modify existing activity/workflow log entries. | Guarded | Append-only review PR validation added; existing log edits should block PR publication. |
| 2026-04-28 | Workflow design | Many catalog workflows still use generic `firmvault-workflow-task`. | Open | Replace with workflow-specific recipes before live testing those workflows. |
| 2026-04-28 | Branching | Lien, PIP exhaustion, UM/UIM/MedPay/Workers Comp tasks can be not applicable. | Open | Ensure bypass/not-applicable marks the corresponding dependency satisfied without polluting the case. |
| 2026-04-28 | Accident Report | Request node submitted completion even though no reporting agency, report number, request method, or fee details existed. | Guarded | Tightened `firmvault-accident-report-analyze`, `REVIEW.md`, runner resume preamble, and recipe-agent tool descriptions. Recipe-specific review blocked the task; accidental PR `#15` was closed unmerged. |
| 2026-04-28 | Quality Review | Recipe-review blocked verdicts left tasks in `quality_review`, causing repeated reviewer pickup. | Fixed | Blocked recipe-review verdicts now return tasks to Human Review, and ready-task polling excludes legacy `Recipe review blocked:` rows. |
| 2026-04-28 | Accident Report | Evidence-backed no-report/not-applicable outcome should not enter the 14-day wait or analysis nodes. | Open | Task `2170` was resolved and PR `#16` merged; wait/analyze were manually skipped and task `2171` was created for Human Review. Add declarative branch/skip support before relying on this path broadly. |
| 2026-04-28 | Forgejo | Forgejo merge API returned 405 even though PR `#16` was mergeable. | Open | Merged manually into `codex/complete-workflow-v2` at `7afdf73516eb6b69ee27efcac4517a7b02e52c73`, commented on the PR, and recorded Mission Control PR state as merged. |
| 2026-04-29 | Runner worktrees | Test fixture case folders must be committed before recipe runners can mount them. | Guarded | `test-ladder-007-accident-report-found` was committed in FirmVault as `b4d9d6920`; untracked fixture folders do not appear in runner worktrees. |
| 2026-04-29 | Runner base refs | Runner prefers `forgejo/<base_ref>` when it exists, so a stale remote branch can hide local fixture commits. | Guarded | Created dedicated fixture base branch `codex/test-ladder-007-accident-report-found-base` at `b4d9d6920` for PR `#18`. Future test workflows should use a pushed fixture base branch, not a local-only commit SHA. |
| 2026-04-29 | Forgejo PR gate | PR publication fails or opens a bad diff if `workspace_source.base_ref` is a commit SHA or a divergent branch. | Guarded | Closed bad-base PR `#17`; opened corrected PR `#18` against `codex/test-ladder-007-accident-report-found-base`. Workflow/task systems should validate that PR bases are branch refs that exist both locally and in Forgejo. |
| 2026-04-29 | Forgejo PR gate | Forgejo UI merge can leave manually merged PRs visually open with zero commits. | Guarded | PR `#18` and PR `#19` were merged into `codex/test-ladder-007-accident-report-found-base` by temp worktree, reconciled in Mission Control as merged, then closed in Forgejo to keep the PR list clean. |
| 2026-04-29 | Browser/UI | Mission Control localhost tab can hang on the boot screen during authenticated task-board testing. | Open | Runtime/database path remained healthy, so task `2176` was driven by DB scripts while Forgejo was verified in browser. Investigate login/hydration issue before relying on browser-only board testing. |
| 2026-04-29 | Client Check-In | Live test reaches a true human communication gate. | Fixed | Aaron recorded "Called client and left a message" on task `2180`; the human gate was completed and the workflow advanced to `wait_for_next_check_in` with a 14-day timer. |
| 2026-04-29 | Client Check-In | Human contact results currently live only in Mission Control task comments unless a recipe changes FirmVault files. | Open | Add a deterministic logger node or post-human recipe that appends the contact attempt to `client/check-ins.md`, `activity/`, and `workflow-log/` before treating the workflow result as fully auditable in the vault. |
| 2026-04-29 | BI Claim Setup | Manual Forgejo merge followed by closing the PR before reconciliation caused a duplicate reviewer pass and duplicate PR. | Open | Task `2182` was already merged through PR `#24`, but closing it before reconcile caused duplicate PR `#25`. Reconcile manual merges before closing Forgejo PRs, or make closed/base-contains-head PRs advance as merged. |
| 2026-04-29 | BI Claim Setup | No-acknowledgment branch needed to avoid falsely marking the BI claim acknowledged when the timer expired without carrier response. | Fixed | Added `process_bi_acknowledgment` recipe node. Task `2184` produced a human follow-up draft, left unsupported claim/adjuster fields unknown, merged via PR `#28`, and completed workflow instance `31`. |
| 2026-04-29 | PIP Claim Setup | PIP packet recipe copied the root synthetic setup/opened date into date-of-loss fields instead of using the accident ledger/police-report date. | Guarded | Added recipe instruction and test coverage requiring `accident/accident.md` as the controlling date-of-loss source. Task `2186` was manually corrected before PR `#30` merged. |
| 2026-04-29 | PIP Claim Setup | Runner acknowledged a correction twice but did not edit the worktree files. | Open | Manual patch was required before quality review. Consider adding a reviewer rule that rejects "acknowledged but not edited" retries and improve runner resume instructions for correction comments. |
| 2026-04-29 | Human Review UX | Human-send tasks told the reviewer to open generated files directly, but those files live on the workflow branch/PR and may not exist in the current local checkout or file viewer. | Guarded | Updated BI and PIP human-send workflow instructions to direct the reviewer to the preceding Forgejo PR diff/files view as the authoritative review surface. Active task `2187` was updated with PR `#30`. |
| 2026-04-29 | PIP Claim Setup | Passive KAC/PIP acknowledgment arrival needed to unblock `wait_for_pip_acknowledgment` before the 10-day timer. | Fixed | Added passive `pip_approved` resolver support, seeded `documents/received/insurance/kac-acknowledgment.md`, and task `2188` processed the approval evidence through PR `#31`. |
| 2026-04-29 | Forgejo webhook/reconcile | PR `#31` merged in Forgejo but Mission Control did not receive or process the webhook automatically. | Fixed | Forgejo was blocking `http://host.docker.internal:3000/api/webhooks/forgejo` with `webhook.ALLOWED_HOST_LIST`. Added `host.docker.internal`, `localhost`, and `127.0.0.1` to Forgejo's `[webhook] ALLOWED_HOST_LIST`; throwaway PR `#35` produced successful `200` webhook deliveries. |
| 2026-04-29 | Runner base refs | Task `2189` PR branch initially compared cleanly against `forgejo/codex/test-ladder-007-accident-report-found-base` but the runner's local `codex/test-ladder-007-accident-report-found-base` ref was stale, making local diffs appear to include prior BI/PIP changes. | Guarded | Rebasing task branch `mc/task-2189` onto the current Forgejo base narrowed PR `#32` to the intended four-file treatment-status change. Add base-ref freshness validation before PR publication. |
| 2026-04-29 | Forgejo merge API | PR `#32` merge API hung again even though Forgejo reported the PR was mergeable. | Fixed | Same root cause as the broader Forgejo merge hang: slow bind-mounted local merge copy path. Fixed in Forgejo `app.ini`; future workflow PRs should use the normal Forgejo merge button/API and webhook path instead of manual temp-worktree merges. |
| 2026-04-29 | Medical Provider Status | Human review result for treatment status currently required manual DB/test-path handling to complete the review node and schedule the wait. | Open | Task `2190` recorded the test-path decision that treatment is not complete and scheduled the 30-day refresh wait. Add a normal UI/API path or deterministic post-human handler for human review gateway completion. |
| 2026-04-29 | Quality Review | Direct DB transition to `quality_review` did not wake the recipe-specific reviewer for task `2191`. | Open | Manually reviewed/published PR `#33` for the live test. Use the UI/API transition or add an explicit event/reconcile path so DB/status changes cannot strand quality-review tasks. |
| 2026-04-29 | Forgejo merge API | PR `#33` merge API timed out with HTTP status `000` while Forgejo reported the PR was mergeable. | Fixed | Reproduced with throwaway PR `#34`: a 12s client timeout killed the merge subprocess, while a 180s request succeeded after 165.63s. Moving Forgejo's local merge checkout to `/tmp/forgejo-local-repo` reduced throwaway PR `#35` API merge time to 6.36s. |
| 2026-04-29 | Runner auth | Runner daemon API calls were rejected by proxy auth before route-level runner-secret validation. | Fixed | Proxy now lets `/api/runner/*` bearer requests reach route auth. Focused proxy and runner-config tests pass. SSE still reports 401, but polling works; investigate SSE path separately. |
| 2026-04-29 | Runner resume context | Human comments were not available inside recipe task workspaces, so resumed tasks could not see owner replies. | Fixed | Runner claim payload now includes recent task comments and writes them into `.mc/task.json`; task `2195` used comment `588` as human send evidence. |
| 2026-04-29 | Request Medical Records | Send-request recipe accepted human send confirmation but initially only posted comments and left `records-bills.md` as `not_requested`. | Fixed | Tightened `firmvault-request-records-bills-send-request` SOUL/REVIEW to require provider ledger diff, send fields, activity, and workflow-log entries for human-confirmed sends. Attempt `9` updated the ledger and passed through PR `#38`. |
| 2026-04-29 | Request Medical Records | Passive provider records/bills arrival completed the wait but also materialized first follow-up. | Fixed | Added provider-scoped passive condition support and generic `skip_when_condition`; the records/bills workflow now skips follow-up/escalation nodes when `law_firm.provider.records_and_bills_received == true` is satisfied early. |
| 2026-04-29 | Fixture state | Passive resolver read local FirmVault case files, but recipe runner worktrees are created from the Forgejo base branch. | Guarded | Seeded received records/bills shadows through Forgejo fixture PR `#39` before running task `2197`. Future passive fixture arrivals should enter the same branch/PR path used by runner worktrees. |
| 2026-04-30 | Fixture quality | Placeholder records/bills shadows proved workflow mechanics but did not exercise realistic OCR content. | Fixed | Converted the scanned North Dixie records/bill fixture to masked markdown with Tesseract, merged it through PR `#42`, reran receipt processing through task `2200` / PR `#43`, and reran chronology through task `2201` / PR `#44`. |
| 2026-04-30 | Demand Readiness | Demand Readiness could not be started as a first-class generic law-firm workflow instance from the existing route/UI path. | Fixed | Manually materialized equivalent task `2202` for the gather-materials node, then added a generic case-workflow activation route/UI action. Live smoke instance `36` proved the UI/API path starts the workflow, but local mirror sync remains a separate blocker. |
| 2026-04-30 | Demand Readiness | The gather-materials recipe found inconsistent provider dates: synthetic treatment ledger dates in 2026 and OCR service dates in 2019. | Fixed | Forgejo PR `#47` normalized editable fixture dates and added append-only correction entries. Historical logs that recorded the earlier synthetic values were intentionally not edited. |
| 2026-04-30 | Quality Review | Demand Readiness review attempt `2` stopped without submitting, then attempt `3` approved and opened PR `#45`. | Guarded | No workflow was stranded, but inspect reviewer retry semantics if this repeats. |
| 2026-04-30 | Forgejo/local mirror | Forgejo PR merges update Forgejo and Mission Control task state, but the local FirmVault mirror read by passive resolvers can remain behind, especially with a dirty working tree. | Guarded | Added post-merge local mirror synchronization in review PR reconciliation. It fetches the merged base, restores only changed paths, and refuses to overwrite locally dirty touched paths. PR `#46` proved the guard by leaving the task done/workflow advanced while posting a sync-skipped comment for untracked local test files. |
| 2026-04-30 | Runner base refs | Manually started first-class case workflows defaulted to `main`, causing runner worktrees to miss the current test-ladder case branch. | Fixed | Demand Readiness task `2203` failed before model work because its workspace source used `main`. Generic case-workflow start now inherits the latest non-main base ref for the case; rerun task `2204` used `codex/test-ladder-008-treatment-complete-base` and completed. |

## Update Rules

- Update this document after every workflow design session, live test, quality review failure, PR merge, or blocker.
- Mark a workflow `Live Tested` only after it has completed the actual Mission Control lifecycle through review/quality review and Forgejo PR gate when applicable.
- Keep the next recommended workflow explicit. As of this version, create an **open-lien fixture path** from the Aesha Holder documents to test final lien requests, lien negotiation, lien payment, and lien-aware final distribution.
- Do not enable broad materialization against real cases while the current test queue is active.
