---
phase: 15-checkpoints-scheduler-v1-2
plan: 07
type: execute
wave: 4
depends_on:
  - 15-01
  - 15-02
  - 15-03
  - 15-04
  - 15-05
  - 15-06
files_modified:
  - src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts
  - src/lib/__tests__/phase-15-scheduler-integration.test.ts
  - src/lib/__tests__/phase-15-blocker-flow-integration.test.ts
autonomous: true
requirements:
  - CP-01
  - CP-02
  - CP-03
  - CP-04
  - CP-05
  - CP-06
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06
must_haves:
  truths:
    - "An end-to-end blocker → resume flow completes: blocked checkpoint → awaiting_owner → manual PUT back to assigned → reconcile tick re-emits runner_requested → claim returns resume_marker → seedMcDir appends marker to progress.md"
    - "The scheduler tick ladder fires reconcileRunnerHeartbeat every 30s and flips stuck in_progress recipe-tasks when no heartbeat in 90s"
    - "All 6 new EventType entries broadcast on the `/api/events` SSE stream and pass workspace filtering correctly (task.* carries workspace_id; recipe.* does not)"
    - "requeueStaleTasks reads runner_heartbeats.metadata_json.active_task_ids and correctly leaves alive-and-tracked tasks, flips stale-heartbeat tasks, and flips alive-but-not-tracked tasks"
    - "An atomic checkpoint POST failure (JSONL write throw) returns 500 AND leaves the DB task_checkpoints row count unchanged AND the JSONL file truncated to pre-call size"
  artifacts:
    - path: "src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts"
      provides: "End-to-end HTTP-like integration covering POST → DB+JSONL → GET → broadcast sequence including blocker transition"
    - path: "src/lib/__tests__/phase-15-scheduler-integration.test.ts"
      provides: "Scheduler + reconcile + requeueStaleTasks orchestration test across 30/90s boundaries (with vi.useFakeTimers)"
    - path: "src/lib/__tests__/phase-15-blocker-flow-integration.test.ts"
      provides: "Full blocker-resume integration — checkpoint POST → awaiting_owner → PUT assigned → claim with resume_marker → seedMcDir output"
  key_links:
    - from: "Integration tests"
      to: "All Wave 1 + Wave 2 outputs"
      via: "Import real modules (task-checkpoints, task-dispatch, runner-worktree, runner-claim); mock only network/event-bus boundaries"
      pattern: "integration"
---

<objective>
Ship three integration tests that exercise the cross-module wiring delivered in Plans 15-01 through 15-06. Unit tests within each plan cover in-module behavior; this plan proves the plans compose correctly end-to-end.

**Test suite 1 — checkpoint POST + GET roundtrip (`src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts`):**
- POST a sequence of checkpoints (completed → in_progress → blocked) for attempt=1.
- Assert DB rows AND JSONL lines land for all three.
- GET `?attempt=1` returns the full timeline ordered correctly.
- POST failure scenarios prove atomic rollback (not just unit-level behavior — the outer handler's try/catch compensates JSONL too).
- CP-05 matrix: POST at least one artifact per `kind` and verify round-trip through JSONL + GET.

**Test suite 2 — scheduler + reconcile + requeueStaleTasks (`src/lib/__tests__/phase-15-scheduler-integration.test.ts`):**
- Simulate: heartbeat row with active_task_ids:[1,2], in_progress recipe-tasks 1 and 2 (fresh updated_at), stale in_progress recipe-task 3 (updated_at=now-200s, not in active_task_ids).
- Call `requeueStaleTasks()` → task 3 flipped to `assigned`; tasks 1/2 unchanged; `task.runner_requested` broadcast for task 3 only.
- Advance fake clock 120s (no new heartbeat): call `reconcileRunnerHeartbeat()` → all stale in_progress recipe-tasks flipped to `assigned`.
- Non-recipe stale tasks follow the legacy agent-offline path (regression guard).

**Test suite 3 — blocker-resume integration (`src/lib/__tests__/phase-15-blocker-flow-integration.test.ts`):**
- Seed task.status='in_progress', recipe_slug='hello-world', worktree_path=mkdtemp path, runner_attempts=1.
- POST /api/tasks/:id/checkpoints with status='blocked', blocker_reason='need secret rotation'.
- Verify:
  - tasks.status='awaiting_owner'
  - comments has one system-authored row containing blocker_reason and attempt number
  - task_checkpoints has the new row
  - JSONL file has the new line
  - `task.checkpoint_added` broadcast payload includes blocker_reason
  - `task.status_changed` broadcast fires (awaiting_owner)
- Owner flips task back to assigned (simulate via direct DB UPDATE — this plan doesn't need to exercise PUT /api/tasks/:id, that's Phase 10 territory).
- Bump runner_attempts to 2 (simulating the claim-time increment).
- Call `resolveResumeMarker(db, taskId)` directly → returns `{ blocker_reason: 'need secret rotation', at_iso: <iso> }`.
- Simulate the dispatch payload construction via `buildDispatchPayload` OR directly inspect the claim-route response shape (if that function is exported) — asserting task.resume_marker is set.
- Call `seedMcDir(worktreePath, { task: { ..., is_resuming: true }, resume_marker })` directly.
- Assert progress.md contains the LOCKED marker line `<iso> | <<< RESUMED AFTER BLOCKER: need secret rotation >>>` appended to the preserved existing content.

Purpose: Catch wiring bugs that unit tests miss (mocked boundaries hide real composition issues). Also serves as living documentation of the intended flow for future maintainers.

Output: 3 integration test files, no production code modified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-CONTEXT.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-RESEARCH.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-01-PLAN.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-02-PLAN.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-03-PLAN.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-04-PLAN.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-05-PLAN.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-06-PLAN.md

<interfaces>
<!-- Minimal mocks — we use REAL task-checkpoints.ts, task-dispatch.ts, runner-worktree.ts, runner-claim.ts.
     Only mock @/lib/auth requireRole (to synthesize user principals) and @/lib/event-bus (to assert broadcasts).
     @/lib/db is the real getDatabase pointing at an in-memory DB via test setup.
-->

<!-- Test setup pattern (common): -->
```ts
import Database from 'better-sqlite3'
import { runMigrations } from '@/lib/migrations'
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import path from 'node:path'

let testDb: Database.Database
let tempDir: string

beforeEach(() => {
  testDb = new Database(':memory:')
  runMigrations(testDb)
  tempDir = mkdtempSync(path.join(tmpdir(), 'mc-phase15-int-'))
})
afterEach(() => {
  testDb.close()
  rmSync(tempDir, { recursive: true, force: true })
})

vi.mock('@/lib/db', () => ({
  getDatabase: () => testDb,
  db_helpers: { logActivity: vi.fn() /* ... */ },
}))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Checkpoint POST + GET integration sweep (CP-01 through CP-06 + SCHED-06)</name>
  <files>src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts</files>
  <read_first>
    - src/app/api/tasks/[id]/checkpoints/route.ts (post-Plan-15-04 + 15-05)
    - src/lib/task-checkpoints.ts (post-Plan-15-04)
    - All unit test files from Plans 15-04 + 15-05 to avoid duplicate coverage
    - .planning/phases/15-checkpoints-scheduler-v1-2/15-RESEARCH.md § "Phase Requirements → Test Map" — lists every CP-* acceptance behavior
  </read_first>
  <action>
    Create `src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts`. Use the shared setup pattern. Helpers: `seedRecipeTask(taskId, { status, runner_attempts, worktree_path })` — INSERT a task row with recipe_slug. `postCheckpoint(taskId, body)` — construct NextRequest with runner-token mock, call POST, return the response. `getCheckpoints(taskId, { attempt? })` — viewer auth, call GET.

    Test cases (minimum 8):

    1. **Full sequence: completed → in_progress → blocked for attempt=1.** POST three checkpoints in order. Assert:
       - task_checkpoints table has 3 rows with `attempt=1`, ordered by insertion.
       - JSONL file at `<worktree>/.mc/checkpoints.jsonl` has 3 lines in insertion order; parse each JSON and verify field-name symmetry with DB columns (except `artifacts` in JSONL vs `artifacts_json` in DB).
       - After the blocked checkpoint, tasks.status='awaiting_owner' AND comments table has one system row.
       - GET `/api/tasks/:id/checkpoints` returns all 3 checkpoints ordered by (attempt ASC, id ASC).
       - GET `?attempt=1` returns all 3 (all attempt=1).
       - GET `?attempt=2` returns [] (no attempt-2 rows yet).
       - `eventBus.broadcast` called at least 3 times with `task.checkpoint_added` (one per POST) plus 1 more with `task.status_changed` (blocker flip).

    2. **CP-05 artifact matrix round-trip.** POST checkpoints each carrying one of the 6 artifact kinds (file/url/diff/test_result/comment/other). For each, verify the JSONL line's `artifacts[0]` deserializes to the same shape the POST sent; verify `JSON.parse(row.artifacts_json)[0]` matches.

    3. **CP-02 atomic rollback — fs.appendFileSync throws.** Seed a working JSONL file size baseline. Use `vi.spyOn(fs, 'appendFileSync').mockImplementationOnce(() => { throw new Error('disk full') })`. POST a valid checkpoint. Assert response is 500, DB row count unchanged, JSONL file size equals the pre-call baseline (truncated back).

    4. **CP-02 atomic rollback — DB INSERT throws (simulated via trigger or spyOn).** Similar to above but force DB failure. Assert response 500, JSONL unchanged (DB rollback auto-truncates since writeCheckpoint transaction catches).

    5. **CP-01 status='blocked' without blocker_reason → 400 Zod error.** No DB writes, no JSONL writes.

    6. **CP-01 status='blocked' with whitespace-only blocker_reason → 400 Zod refine.**

    7. **Unknown kind artifact → 400 discriminator error.** No DB writes.

    8. **Viewer in different workspace → 404 on GET (masquerade as not-found).** Seed task in workspace 1; viewer from workspace 2 GETs → 404.

    9. **Sequential attempt increments: GET with attempt filter.** Seed checkpoints for attempt=1 and attempt=2. GET `?attempt=1` returns only attempt-1 rows; `?attempt=2` returns only attempt-2.

    These tests import the REAL route handler + real task-checkpoints module. Only requireRole and eventBus are mocked.
  </action>
  <verify>
    <automated>pnpm test src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - File exists with at least 9 `it(...)` cases
    - Tests import real `@/lib/task-checkpoints` (no mock on task-checkpoints module)
    - Tests use real `@/app/api/tasks/[id]/checkpoints/route` POST and GET
    - JSONL file round-trip asserted via `fs.readFileSync(jsonlPath, 'utf8').split('\n')` + JSON.parse
    - Atomic rollback cases assert (a) HTTP 500 AND (b) DB row count AND (c) JSONL size baseline — three assertions per rollback test
    - `pnpm test src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts --run` exits 0
  </acceptance_criteria>
  <done>
    CP-01..06 and SCHED-06 (task.checkpoint_added) proven at integration level. Atomic contract enforced under failure injection. Artifact discriminator matrix covered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scheduler + reconcile + requeueStaleTasks orchestration</name>
  <files>src/lib/__tests__/phase-15-scheduler-integration.test.ts</files>
  <read_first>
    - src/lib/scheduler.ts (post-Plan-15-02)
    - src/lib/task-dispatch.ts (post-Plan-15-02 — autoRouteInboxTasks, dispatchAssignedTasks, requeueStaleTasks, reconcileRunnerHeartbeat)
    - .planning/phases/15-checkpoints-scheduler-v1-2/15-RESEARCH.md Focus Area 4 (scheduler module structure) + Focus Area 9 (stale query)
  </read_first>
  <action>
    Create `src/lib/__tests__/phase-15-scheduler-integration.test.ts`. Use `vi.useFakeTimers()` and `vi.setSystemTime(base)`. Setup: in-memory DB + runMigrations + mock @/lib/event-bus.

    Test cases (minimum 6):

    1. **SCHED-01: autoRouteInboxTasks moves recipe-tagged inbox tasks without agent scoring.** Seed one recipe-tagged inbox task (no assigned_to). Seed one non-recipe inbox task (no assigned_to, requires affinity scoring). Call `autoRouteInboxTasks()`. Assert:
       - Recipe task status = 'assigned', `task.runner_requested` broadcast fires.
       - Non-recipe task follows legacy logic (runs affinity scoring — may or may not be routed depending on seeded agents; the fact that it isn't auto-routed via the fast-path is the assertion).

    2. **SCHED-02: dispatchAssignedTasks skips recipe-tagged.** Seed two assigned tasks: recipe-tagged + non-recipe. Seed an active agent. Call `dispatchAssignedTasks()`. Assert only the non-recipe task was dispatched.

    3. **SCHED-03: requeueStaleTasks combines heartbeat freshness + active_task_ids.**
       - Seed fresh heartbeat with `metadata_json:{"active_task_ids":[1,2]}`.
       - Seed in_progress recipe-tasks 1, 2, 3 with updated_at=now-1000.
       - Call `requeueStaleTasks()`.
       - Assert tasks 1 and 2 unchanged (runner knows about them), task 3 flipped to 'assigned' (runner does NOT know about it), `task.runner_requested` broadcast fired for task 3 only.

    4. **SCHED-04: reconcileRunnerHeartbeat flips stuck recipe-tasks on stale heartbeat.**
       - Seed stale heartbeat (last_heartbeat_at = now - 200).
       - Seed in_progress recipe-task with updated_at=now-100.
       - Call `reconcileRunnerHeartbeat()`.
       - Assert task flipped to 'assigned', `task.runner_requested` broadcast fired once.

    5. **SCHED-04: reconcileRunnerHeartbeat is a no-op when heartbeat is fresh.**
       - Seed fresh heartbeat + in_progress recipe-task with stale updated_at.
       - Call `reconcileRunnerHeartbeat()`.
       - Assert task UNCHANGED (the runner MAY be mid-execution, other mechanisms handle container liveness).

    6. **SCHED-04: reconcileRunnerHeartbeat does NOT flip recently-claimed tasks.**
       - Seed stale heartbeat + in_progress recipe-task with updated_at=now-5 (fresh).
       - Call `reconcileRunnerHeartbeat()`.
       - Assert task UNCHANGED (defensive: avoid flipping a just-claimed task before first heartbeat lands).

    7. **Scheduler tick dispatch ladder — reconcile_runner_heartbeat registered + triggerable.**
       - Call `initScheduler()`, then `getSchedulerStatus()`.
       - Assert the status array includes `{ id: 'reconcile_runner_heartbeat', intervalMs: 30_000 }`.
       - Stop the scheduler to prevent leaks.

    8. **Regression: legacy requeueStaleTasks for non-recipe tasks still works.**
       - Seed in_progress non-recipe task with offline agent.
       - Call `requeueStaleTasks()`.
       - Assert task flipped to 'assigned' via the legacy path (not the recipe-branch path).
  </action>
  <verify>
    <automated>pnpm test src/lib/__tests__/phase-15-scheduler-integration.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - File has at least 8 `it(...)` cases
    - Tests use `vi.useFakeTimers()` + `vi.setSystemTime(base)` to deterministically control the 90s/30s boundaries
    - Tests import real `autoRouteInboxTasks`, `dispatchAssignedTasks`, `requeueStaleTasks`, `reconcileRunnerHeartbeat` from `@/lib/task-dispatch`
    - `pnpm test src/lib/__tests__/phase-15-scheduler-integration.test.ts --run` exits 0
  </acceptance_criteria>
  <done>
    SCHED-01 through SCHED-04 proven at integration level. Legacy paths regression-tested.
  </done>
</task>

<task type="auto">
  <name>Task 3: Blocker → resume full flow integration</name>
  <files>src/lib/__tests__/phase-15-blocker-flow-integration.test.ts</files>
  <read_first>
    - src/app/api/tasks/[id]/checkpoints/route.ts (post-Plan-15-05 blocker branch)
    - src/lib/runner-claim.ts (post-Plan-15-05 resolveResumeMarker)
    - src/lib/runner-worktree.ts (post-Plan-15-03 seedMcDir resume_marker)
    - .planning/phases/15-checkpoints-scheduler-v1-2/15-CONTEXT.md § "Blocker & Resume Flow"
  </read_first>
  <action>
    Create `src/lib/__tests__/phase-15-blocker-flow-integration.test.ts`. Setup: in-memory DB + runMigrations + mkdtemp worktree + mock auth + event-bus. The test simulates the FULL blocker → resume flow without exercising HTTP for the non-checkpoint bits (use direct SQL UPDATE for the owner's awaiting_owner → assigned transition):

    Single test case (as one big `it(...)` labeled "Phase 15 end-to-end: blocker → awaiting_owner → resume → progress.md marker"):

    **Phase 1 — seed:**
    - INSERT task id=42, status='in_progress', recipe_slug='hello-world', worktree_path=tempDir, runner_attempts=1, workspace_id=1.
    - Pre-populate `<tempDir>/.mc/progress.md` with `# Progress — Task 42\n\nInitial progress.\n`.
    - Pre-populate `<tempDir>/.mc/checkpoints.jsonl` with one existing completed-checkpoint line.

    **Phase 2 — agent POSTs blocker:**
    - Construct NextRequest with runner-token auth, POST `{ step: 'waiting-for-review', summary: 'need secret rotation', status: 'blocked', blocker_reason: 'AWS_ACCESS_KEY expired — rotate in console' }`.
    - Call real POST handler.
    - Assert:
      - Response 201.
      - `SELECT status FROM tasks WHERE id=42` → 'awaiting_owner'.
      - `SELECT COUNT(*) FROM comments WHERE task_id=42 AND author='system'` → 1. Comment content includes the blocker_reason substring AND 'attempt 1'.
      - `SELECT COUNT(*) FROM task_checkpoints WHERE task_id=42` → 2 (1 pre-existing stub + 1 new blocker).
      - `fs.readFileSync(tempDir + '/.mc/checkpoints.jsonl')` has 2 lines (pre-existing + new blocker line).
      - `eventBus.broadcast` calls include `task.checkpoint_added` with blocker_reason + `task.status_changed` with previous_status='in_progress'.

    **Phase 3 — owner flips awaiting_owner → assigned:**
    - Direct SQL: `UPDATE tasks SET status='assigned', runner_attempts=2 WHERE id=42` (simulating the owner action + claim-time attempt increment).

    **Phase 4 — claim-time resume_marker resolution:**
    - Call `resolveResumeMarker(db, 42)` directly.
    - Assert returned value is `{ blocker_reason: 'AWS_ACCESS_KEY expired — rotate in console', at_iso: /^\d{4}-\d{2}-\d{2}T.*Z$/ }`.

    **Phase 5 — seedMcDir simulation (daemon would do this):**
    - Call `seedMcDir(tempDir, { task: { task_id: 42, recipe_slug: 'hello-world', attempt: 2, is_resuming: true, prior_attempts: [...] /* shape per McTaskJson */ }, resume_marker: /* the value from Phase 4 */ })`.
    - Assert `<tempDir>/.mc/progress.md` content is:
      ```
      # Progress — Task 42

      Initial progress.
      <iso> | <<< RESUMED AFTER BLOCKER: AWS_ACCESS_KEY expired — rotate in console >>>
      ```
      Use a regex for the iso to be tolerant to the exact timestamp.
    - Assert `<tempDir>/.mc/checkpoints.jsonl` is UNCHANGED from Phase 2 (seedMcDir does NOT touch checkpoints.jsonl on resume-preservation path).
    - Assert `<tempDir>/.mc/task.json` is REWRITTEN with attempt=2, is_resuming=true, prior_attempts carrying the previous attempt.
  </action>
  <verify>
    <automated>pnpm test src/lib/__tests__/phase-15-blocker-flow-integration.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - File has at least 1 large `it(...)` covering all 5 phases, or multiple smaller `it(...)` blocks (planner's choice — one per phase is acceptable)
    - Test uses real POST handler from `@/app/api/tasks/[id]/checkpoints/route`
    - Test uses real `resolveResumeMarker` from `@/lib/runner-claim`
    - Test uses real `seedMcDir` from `@/lib/runner-worktree`
    - progress.md assertion matches the exact LOCKED marker format (byte-for-byte, minus the iso timestamp which uses a regex)
    - `pnpm test src/lib/__tests__/phase-15-blocker-flow-integration.test.ts --run` exits 0
  </acceptance_criteria>
  <done>
    CP-02, CP-03, CP-04 proven end-to-end at integration level. The LOCKED progress.md marker format is proven. The resume flow from blocker → awaiting_owner → assigned → marker-enriched progress.md is tested in one cohesive test.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- All three integration tests pass: `pnpm test src/app/api/tasks/[id]/checkpoints/__tests__/integration.test.ts src/lib/__tests__/phase-15-scheduler-integration.test.ts src/lib/__tests__/phase-15-blocker-flow-integration.test.ts --run` exits 0
- `pnpm test --run` (full suite) exits 0
- `pnpm test:all` (if time permits — lint + typecheck + test + build + e2e) exits 0
- Phase 15 requirement ID coverage matrix:
  - CP-01: Plans 15-04 (POST handler) + 15-07 (integration)
  - CP-02: Plans 15-04 (atomic write) + 15-05 (blocker transaction) + 15-07 (integration)
  - CP-03: Plans 15-05 (blocker branch) + 15-07 (integration)
  - CP-04: Plans 15-03 (seedMcDir) + 15-05 (claim resume_marker + daemon) + 15-07 (integration)
  - CP-05: Plans 15-04 (Zod discriminated union) + 15-07 (integration matrix)
  - CP-06: Plans 15-04 (GET handler) + 15-07 (integration)
  - SCHED-01: Plans 15-02 (autoRoute) + 15-07 (integration)
  - SCHED-02: Plans 15-02 (dispatch skip) + 15-07 (integration)
  - SCHED-03: Plans 15-02 (requeue) + 15-06 (inventory data source) + 15-07 (integration)
  - SCHED-04: Plans 15-02 (reconcile) + 15-07 (integration)
  - SCHED-05: Plans 15-02 (3 emission points) + 15-05 (runner-exit retry emission, completes the set)
  - SCHED-06: Plans 15-01 (EventType) + 15-04 (checkpoint_added) + 15-05 (container_exited) + 15-06 (recipe.indexed/removed + container_started)
</verification>

<success_criteria>
- Three integration tests shipped, all passing
- All 12 Phase 15 requirement IDs have at least one integration-level assertion
- No production code modified in this plan
- CP-02 atomic rollback proven under failure injection
- Progress.md marker format byte-asserted
</success_criteria>

<output>
After completion, create `.planning/phases/15-checkpoints-scheduler-v1-2/15-07-SUMMARY.md` documenting:
- Integration test file locations
- Requirement ID → test location matrix
- Any seams that required adjustment during integration (e.g., if a unit-tested module needed a small refactor to compose cleanly)
- Recommendation for Phase 17 RTEST coverage: "Phase 15 integration tests cover in-process wiring; Phase 17 should extend with a full container-driven end-to-end test using the mc-hello-world-agent image."
</output>
