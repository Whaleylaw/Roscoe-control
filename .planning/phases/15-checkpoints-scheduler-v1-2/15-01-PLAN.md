---
phase: 15-checkpoints-scheduler-v1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/event-bus.ts
  - src/lib/runner-tokens.ts
  - src/lib/__tests__/event-bus.test.ts
  - src/lib/__tests__/runner-tokens-allowlist.test.ts
autonomous: true
requirements:
  - CP-01
  - SCHED-06
must_haves:
  truths:
    - "`eventBus.broadcast('task.runner_requested', ...)` compiles without a TS error"
    - "`eventBus.broadcast('task.container_started', ...)` compiles without a TS error"
    - "`eventBus.broadcast('task.container_exited', ...)` compiles without a TS error"
    - "`eventBus.broadcast('task.checkpoint_added', ...)` compiles without a TS error"
    - "`eventBus.broadcast('recipe.indexed', ...)` compiles without a TS error"
    - "`eventBus.broadcast('recipe.removed', ...)` compiles without a TS error"
    - "A bearer presented to `POST /api/tasks/:id/checkpoints` matches the RUNNER_TOKEN_ALLOWLIST pattern entry"
  artifacts:
    - path: "src/lib/event-bus.ts"
      provides: "Extended EventType union with 6 new Phase 15 event types"
      contains: "task.runner_requested"
    - path: "src/lib/runner-tokens.ts"
      provides: "RUNNER_TOKEN_ALLOWLIST extended for POST /api/tasks/:id/checkpoints"
      contains: "/api/tasks"
    - path: "src/lib/__tests__/event-bus.test.ts"
      provides: "Compile-time + runtime coverage of the 6 new event types"
    - path: "src/lib/__tests__/runner-tokens-allowlist.test.ts"
      provides: "Allowlist matcher test for the new /api/tasks/:id/checkpoints entry"
  key_links:
    - from: "src/lib/event-bus.ts"
      to: "consumers in checkpoints/route.ts, task-dispatch.ts, recipe-watcher.ts, runner-exit/route.ts, container-started/route.ts"
      via: "EventType union additions"
      pattern: "'task.runner_requested' \\| 'task.container_started' \\| 'task.container_exited' \\| 'task.checkpoint_added' \\| 'recipe.indexed' \\| 'recipe.removed'"
    - from: "src/lib/runner-tokens.ts RUNNER_TOKEN_ALLOWLIST"
      to: "src/lib/auth.ts runner-token branch (line 526)"
      via: "regex match on request path"
      pattern: "\\/api\\/tasks\\/\\(\\\\d\\+\\)\\/checkpoints"
---

<objective>
Extend two foundational modules so the rest of Phase 15 can compile and auth correctly:

1. Append six new entries to the closed `EventType` union in `src/lib/event-bus.ts` (additive-only per CONTEXT.md § Event Emission & SSE Fan-out § Compat policy). Without this, any downstream `eventBus.broadcast('task.runner_requested', ...)` call in Wave 2 fails TypeScript compilation (see 15-RESEARCH.md Pitfall 4 + Focus Area 6).

2. Extend `RUNNER_TOKEN_ALLOWLIST` in `src/lib/runner-tokens.ts` with exactly ONE new entry — `{ method: 'POST', pathPattern: /^\/api\/tasks\/(\d+)\/checkpoints\/?$/ }` — to honor the CONTEXT.md § "Checkpoint Endpoint Auth Path (added 2026-04-20 after research)" lock: POST path MUST be literal `/api/tasks/:id/checkpoints`, NOT relocated under `/api/runner/*`. This is the SOLE allowlist addition. Do NOT refactor the `/api/runner/` path-scope gate in `src/lib/auth.ts` (line 526) — the auth.ts runner-token branch currently gates on `url.pathname.startsWith('/api/runner/')` and so will NOT reach the new allowlist entry. That gate is extended in a follow-on task inside THIS plan.

Purpose: Every Wave 2 plan depends on these two additions. Splitting them out of Wave 2 keeps file ownership disjoint (event-bus.ts and runner-tokens.ts are touched here and only here in the phase).

Output: `src/lib/event-bus.ts` + `src/lib/runner-tokens.ts` + `src/lib/auth.ts` updated, plus two co-located test files covering compile-time and runtime behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-CONTEXT.md
@.planning/phases/15-checkpoints-scheduler-v1-2/15-RESEARCH.md

<interfaces>
<!-- Current event-bus.ts EventType union (lines 15-55). The six additions go at the END of the union, after `'gsd.conflict.detected'` (line 55). -->
From src/lib/event-bus.ts:
```typescript
export type EventType =
  | 'task.created'
  | 'task.updated'
  | 'task.deleted'
  | 'task.status_changed'
  // ... 40+ existing entries ending with:
  | 'gsd.conflict.detected'

export interface ServerEvent {
  type: string
  data: any
  timestamp: number
}

class ServerEventBus extends EventEmitter {
  broadcast(type: EventType, data: any): ServerEvent { /* ... */ }
}
```

<!-- Current runner-tokens.ts RUNNER_TOKEN_ALLOWLIST (lines 10-17). NEW entry is appended as the 7th tuple; all six existing tuples (`/api/runner/tasks/(\d+)/checkpoints`, `.../submit`, `.../fail`, `.../status`, `.../`, `.../comments`) are PRESERVED unchanged. -->
From src/lib/runner-tokens.ts:
```typescript
export const RUNNER_TOKEN_ALLOWLIST: ReadonlyArray<{ method: string; pathPattern: RegExp }> = [
  { method: 'POST', pathPattern: /^\/api\/runner\/tasks\/(\d+)\/checkpoints\/?$/ },
  { method: 'POST', pathPattern: /^\/api\/runner\/tasks\/(\d+)\/submit\/?$/ },
  { method: 'POST', pathPattern: /^\/api\/runner\/tasks\/(\d+)\/fail\/?$/ },
  { method: 'GET',  pathPattern: /^\/api\/runner\/tasks\/(\d+)\/status\/?$/ },
  { method: 'GET',  pathPattern: /^\/api\/runner\/tasks\/(\d+)\/?$/ },
  { method: 'GET',  pathPattern: /^\/api\/runner\/tasks\/(\d+)\/comments\/?$/ },
]
```

<!-- Current auth.ts runner-token path gate (line 526). Extend the `startsWith('/api/runner/')` gate to ALSO accept `/api/tasks/:id/checkpoints`. Narrow scope — do NOT broaden to all of `/api/tasks/`. -->
From src/lib/auth.ts (line ~526):
```typescript
if (url.pathname.startsWith('/api/runner/')) {
  const bearer = extractApiKeyFromHeaders(request.headers)
  if (bearer) {
    const match = RUNNER_TOKEN_ALLOWLIST.find(
      (rule) => rule.method === request.method && rule.pathPattern.test(url.pathname),
    )
    // ... issue runner-token principal
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EventType union with 6 Phase 15 additions</name>
  <files>src/lib/event-bus.ts, src/lib/__tests__/event-bus.test.ts</files>
  <read_first>
    - src/lib/event-bus.ts (entire file, ~80 lines — understand the closed union shape and the `broadcast(type, data)` signature)
    - .planning/phases/15-checkpoints-scheduler-v1-2/15-CONTEXT.md § "Event Emission & SSE Fan-out" (compat policy: additive-only)
    - .planning/phases/15-checkpoints-scheduler-v1-2/15-RESEARCH.md Focus Area 6 "Event bus + SSE emission" and Pitfall 4 "Missing EventType extension"
  </read_first>
  <action>
    Append the following six union members to `src/lib/event-bus.ts` at the end of the `EventType` union (immediately after `| 'gsd.conflict.detected'` at line 55):
    ```
    | 'task.runner_requested'     // SCHED-05 — MC tells runner a recipe-tagged task is ready for claim
    | 'task.container_started'    // SCHED-06 — emitted from POST /api/runner/tasks/:id/container-started
    | 'task.container_exited'     // SCHED-06 — emitted from POST /api/runner/tasks/:id/runner-exit
    | 'task.checkpoint_added'     // SCHED-06 — emitted from POST /api/tasks/:id/checkpoints
    | 'recipe.indexed'            // SCHED-06 — emitted from recipe-watcher scheduleReindex
    | 'recipe.removed'            // SCHED-06 — emitted from recipe-watcher scheduleReindex
    ```
    Preserve all existing 40+ union members verbatim. The union must remain typed (no `| string` escape hatch).

    Create `src/lib/__tests__/event-bus.test.ts`:
    - Import `eventBus` and call `.broadcast(type, data)` for each of the six new event types. Assert the returned `ServerEvent.type` matches the string literal passed. One `it(...)` per event type — six total.
    - One compile-time assertion test: declare `const t: EventType = 'task.runner_requested'` etc. (a `satisfies EventType` expression suffices). If TS compile fails, `pnpm typecheck` in CI will surface it before runtime.
    - Use `vi.fn()` to subscribe via `eventBus.on('server-event', handler)` and verify each `broadcast` fires the handler exactly once with the correct `type` and `data` payload shape: `{ type, data, timestamp: number }` per `ServerEvent` interface at line 8.
    - Tear down listeners in `afterEach` to avoid cross-test leakage.

    Do NOT modify `broadcast()` signature, `ServerEvent` interface, or any existing union member.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/event-bus.ts` contains the exact string `'task.runner_requested'` in the EventType union (grep: `grep -n "'task.runner_requested'" src/lib/event-bus.ts`)
    - `src/lib/event-bus.ts` contains all six new event type strings: `task.runner_requested`, `task.container_started`, `task.container_exited`, `task.checkpoint_added`, `recipe.indexed`, `recipe.removed`
    - `src/lib/event-bus.ts` still exports `EventType`, `ServerEvent`, and the `eventBus` singleton (no structural changes)
    - `src/lib/__tests__/event-bus.test.ts` exists with at least 6 `it(...)` blocks covering each new event type
    - `pnpm test src/lib/__tests__/event-bus.test.ts` exits 0
    - `pnpm typecheck` exits 0 (catches any typo in the union extension)
  </acceptance_criteria>
  <done>
    EventType union contains 6 new additive entries; downstream plans can call `eventBus.broadcast('task.runner_requested', ...)` without TS error; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend RUNNER_TOKEN_ALLOWLIST and auth.ts runner-token gate for POST /api/tasks/:id/checkpoints</name>
  <files>src/lib/runner-tokens.ts, src/lib/auth.ts, src/lib/__tests__/runner-tokens-allowlist.test.ts</files>
  <read_first>
    - src/lib/runner-tokens.ts (entire file, ~150 lines — understand the allowlist shape and the preamble comment that explicitly INVITES Phase 15 to extend: lines 4-8)
    - src/lib/auth.ts lines 460-575 (both runner-secret gate at 472 AND runner-token gate at 526 — the PATH-SCOPE STARTSWITH is the blocker for POST /api/tasks/:id/checkpoints)
    - .planning/phases/15-checkpoints-scheduler-v1-2/15-CONTEXT.md § "Checkpoint Endpoint Auth Path (added 2026-04-20 after research)" (LOCKED post-research decision)
    - .planning/phases/15-checkpoints-scheduler-v1-2/15-RESEARCH.md Focus Area 8 "Runner-token auth on checkpoints endpoint (CRITICAL)" — note Option A vs Option B — CONTEXT.md § LOCKED on Option-B-like approach (literal `/api/tasks/:id/checkpoints` path). Honor CONTEXT.md.
  </read_first>
  <action>
    **Part A — extend the allowlist (src/lib/runner-tokens.ts):**

    Update the `RUNNER_TOKEN_ALLOWLIST` array at lines 10-17 by appending one new entry AFTER the existing six entries:
    ```ts
    { method: 'POST', pathPattern: /^\/api\/tasks\/(\d+)\/checkpoints\/?$/ },
    ```
    Update the preamble comment at lines 4-8 from "DO NOT add entries in this phase" (which referred to Phase 11) to note that Phase 15 adds EXACTLY ONE entry for CP-01's literal roadmap path. Do NOT broaden the scope. Do NOT remove or reorder existing entries.

    **Part B — extend the auth.ts runner-token gate (src/lib/auth.ts):**

    The runner-token branch at line 526 currently gates on `url.pathname.startsWith('/api/runner/')`. That gate will reject the NEW `/api/tasks/:id/checkpoints` path before the allowlist is consulted. Extend the gate as a narrow OR condition:
    ```ts
    const isRunnerPath = url.pathname.startsWith('/api/runner/')
    const isCheckpointsTaskPath = /^\/api\/tasks\/\d+\/checkpoints\/?$/.test(url.pathname)
    if (isRunnerPath || isCheckpointsTaskPath) {
      // ... existing allowlist + verifyRunnerToken branch ...
    }
    ```
    DO NOT modify the runner-SECRET gate at line 472 — that one stays `/api/runner/`-only (runner-secret is NOT valid on /api/tasks/:id/checkpoints).

    Add an inline comment above the extended gate documenting the precedent:
    ```
    // Phase 15 (CP-01): runner-token principals MAY authenticate on
    //   POST /api/tasks/:id/checkpoints
    // in addition to the standard /api/runner/* scope. The allowlist in
    // runner-tokens.ts is the single source of truth for method+path pairs;
    // this gate is just the cheap prefix filter that decides whether to run
    // the allowlist matcher at all. DO NOT broaden — see 15-CONTEXT.md § post-research lock.
    ```

    **Part C — test (src/lib/__tests__/runner-tokens-allowlist.test.ts):**

    Create a new test file that:
    - Imports `RUNNER_TOKEN_ALLOWLIST` from `@/lib/runner-tokens`.
    - Asserts array length is 7 (was 6 before).
    - Asserts the new entry exists: `{ method: 'POST', pathPattern: /regex matching /api/tasks/(\d+)/checkpoints/ }`.
    - Positive match cases: `POST /api/tasks/1/checkpoints`, `POST /api/tasks/999/checkpoints`, `POST /api/tasks/1/checkpoints/` (trailing slash OK per `\/?$/`).
    - Negative match cases: `GET /api/tasks/1/checkpoints` (wrong method), `POST /api/tasks/1/comments` (different subpath), `POST /api/tasks/abc/checkpoints` (non-numeric id), `POST /api/tasks/1/checkpoints/extra` (trailing segment). These must NOT match the new entry.
    - Idempotence/preservation: the existing SIX runner entries still appear at their original positions 0-5.
  </action>
  <verify>
    <automated>pnpm test src/lib/__tests__/runner-tokens-allowlist.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/runner-tokens.ts` RUNNER_TOKEN_ALLOWLIST has exactly 7 entries (grep: `grep -c "pathPattern:" src/lib/runner-tokens.ts` returns at least 7 — one per entry)
    - `src/lib/runner-tokens.ts` contains the exact string `/^\\/api\\/tasks\\/(\\d+)\\/checkpoints\\/?$/` (grep: `grep -n "api/tasks" src/lib/runner-tokens.ts` returns at least 1 match)
    - `src/lib/auth.ts` contains a test like `isCheckpointsTaskPath` or a regex referencing `/^\\/api\\/tasks\\/\\d+\\/checkpoints\\/?$/` near line 526 (grep: `grep -n "/api/tasks" src/lib/auth.ts`)
    - `src/lib/auth.ts` runner-SECRET gate (line ~472) UNCHANGED — still gates on `url.pathname.startsWith('/api/runner/')` only (grep context: `grep -n -B2 -A2 "startsWith('/api/runner/')" src/lib/auth.ts` shows exactly one runner-SECRET location)
    - `src/lib/__tests__/runner-tokens-allowlist.test.ts` exists and covers both positive and negative match cases
    - `pnpm test src/lib/__tests__/runner-tokens-allowlist.test.ts --run` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    A bearer presented to `POST /api/tasks/:id/checkpoints` now passes the auth.ts path gate (Task 2B), matches a RUNNER_TOKEN_ALLOWLIST entry (Task 2A), and can reach the Wave 2 route handler. Runner-secret gate unchanged. Test file proves both positive and negative matches.
  </done>
</task>

</tasks>

<verification>
Phase-level verification for this plan:
- `pnpm typecheck` passes (catches union typos + type regressions)
- `pnpm test src/lib/__tests__/event-bus.test.ts --run` passes (6 new event types broadcast correctly)
- `pnpm test src/lib/__tests__/runner-tokens-allowlist.test.ts --run` passes (7th entry present + positive/negative match coverage)
- `grep -n "'task.runner_requested'" src/lib/event-bus.ts` returns a match
- `grep -n "/api/tasks" src/lib/runner-tokens.ts` returns a match on the new entry
- `grep -n "/api/tasks" src/lib/auth.ts` returns a match on the new gate
- No other files modified (check: `git status` lists only event-bus.ts, runner-tokens.ts, auth.ts, and the two new test files)
</verification>

<success_criteria>
- EventType union extended by exactly 6 entries — no renames, no removals
- RUNNER_TOKEN_ALLOWLIST extended by exactly 1 entry (POST /api/tasks/:id/checkpoints)
- auth.ts runner-token gate (line ~526) extended to recognize /api/tasks/:id/checkpoints for runner-token lookups
- auth.ts runner-SECRET gate (line ~472) UNTOUCHED
- Two new test files exist and pass
- `pnpm typecheck` passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-checkpoints-scheduler-v1-2/15-01-SUMMARY.md` documenting:
- The exact six union additions (verbatim string literals)
- The new allowlist entry (verbatim regex)
- The auth.ts gate extension (before/after snippet)
- Any discovered surprises (e.g., unrelated unions elsewhere in event-bus consumers, if any)
- Confirmation that Wave 2 plans 15-04/15-05/15-06 can now proceed
</output>
