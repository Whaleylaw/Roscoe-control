---
phase: 06-settings
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/project/__tests__/settings-view.test.tsx
  - src/components/project/__tests__/i18n-coverage.test.tsx
  - messages/ar.json
  - messages/de.json
  - messages/en.json
  - messages/es.json
  - messages/fr.json
  - messages/ja.json
  - messages/ko.json
  - messages/pt.json
  - messages/ru.json
  - messages/zh.json
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03

must_haves:
  truths:
    - "Test scaffold file exists at src/components/project/__tests__/settings-view.test.tsx with it.todo() stubs covering name/description/status editing (SETT-01), color/prefix/deadline/github_repo editing (SETT-02), and PATCH+Zustand-refresh save flow (SETT-03)"
    - "Test scaffolds include it.todo() stubs for each identified pitfall: ticket-prefix false-dirty after normalize+save, default-project archive 400 defensive handling, ticket_counter never in PATCH body, deadline Unix-seconds round-trip preserves date, empty-string/null coercion for description+github_repo+color+deadline"
    - "All 10 locale files contain 29 new keys under project.settings.* (sectionBasics, sectionAppearance, sectionIntegrations, readOnlyNote, nameLabel, namePlaceholder, descriptionLabel, descriptionPlaceholder, statusLabel, statusActive, statusArchived, colorLabel, colorNone, prefixLabel, prefixPlaceholder, prefixHelp, deadlineLabel, githubRepoLabel, githubRepoPlaceholder, githubRepoHelp, save, cancel, saving, unsavedChanges, errorNameRequired, errorPrefixConflict, errorPrefixInvalid, errorDefaultArchive, errorBannerHeading, errorBannerFallback) plus loadErrorHeading"
    - "Existing project.settings.placeholder stub is removed from all 10 locale files"
    - "project.settings.title value equals 'Project settings' in en.json (updated from 'Settings' to match UI-SPEC)"
    - "i18n-coverage.test.tsx extended with assertion that every locale contains all required project.settings.* keys"
    - "Full vitest suite remains green (it.todo stubs are pending, not failures)"
  artifacts:
    - path: "src/components/project/__tests__/settings-view.test.tsx"
      provides: "Unit test scaffolds for SettingsView (SETT-01, SETT-02, SETT-03)"
      contains: "it.todo"
    - path: "messages/en.json"
      provides: "English i18n keys for project.settings.* (29 new + updated title + loadErrorHeading)"
      contains: "sectionBasics"
    - path: "messages/ar.json"
      provides: "Arabic translations"
      contains: "sectionBasics"
    - path: "messages/de.json"
      provides: "German translations"
      contains: "sectionBasics"
    - path: "messages/es.json"
      provides: "Spanish translations"
      contains: "sectionBasics"
    - path: "messages/fr.json"
      provides: "French translations"
      contains: "sectionBasics"
    - path: "messages/ja.json"
      provides: "Japanese translations"
      contains: "sectionBasics"
    - path: "messages/ko.json"
      provides: "Korean translations"
      contains: "sectionBasics"
    - path: "messages/pt.json"
      provides: "Portuguese translations"
      contains: "sectionBasics"
    - path: "messages/ru.json"
      provides: "Russian translations"
      contains: "sectionBasics"
    - path: "messages/zh.json"
      provides: "Chinese translations"
      contains: "sectionBasics"
    - path: "src/components/project/__tests__/i18n-coverage.test.tsx"
      provides: "Extended coverage asserting settings keys exist in all 10 locales"
      contains: "project.settings.sectionBasics"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for the Phase 6 Settings form and atomically add the full `project.settings.*` i18n namespace across all 10 locales in a single commit. This follows the Phase 5 playbook — Wave 0 lands tests and i18n first so the Wave 1 implementation plan has a concrete test bucket to fill and so parallel plan execution in later phases cannot conflict on `messages/*.json`. No implementation code is written in this plan; settings-view.tsx remains the 16-line stub until Plan 06-01.

Purpose: Keep the test suite green, pre-wire i18n atomically, and give the Wave 1 plan a scaffold it merely fills in.
Output: One new test file, one extended test file, 10 updated locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings/06-CONTEXT.md
@.planning/phases/06-settings/06-RESEARCH.md
@.planning/phases/06-settings/06-UI-SPEC.md
@.planning/phases/06-settings/06-VALIDATION.md
@.planning/phases/05-sessions-agents/05-00-PLAN.md
@.planning/phases/05-sessions-agents/05-00-SUMMARY.md

<interfaces>
<!-- Key contracts executor must honor. Extracted from codebase. -->

From src/components/project/project-context.tsx:
```typescript
export interface ProjectWorkspaceState {
  slug: string
  view: string
  detailId: string | null
  project: Project | null
  loading: boolean
  error: string | null
}
export function useProjectWorkspace(): ProjectWorkspaceState
```

From src/store/index.ts (relevant slices):
```typescript
projects: Project[]
setProjects: (projects: Project[]) => void
fetchProjects: () => Promise<void>
currentUser: CurrentUser | null   // has role: 'admin' | 'operator' | 'viewer'
```

From src/app/api/projects/[id]/route.ts (PATCH):
  - Accepts: name, description, ticket_prefix, status, github_repo (null|string), deadline (number|null), color (null|string), github_sync_enabled, github_default_branch
  - Returns 400 "Project name cannot be empty" when name trims to empty
  - Returns 400 "Invalid ticket prefix" when normalizePrefix() yields empty string
  - Returns 409 "Ticket prefix already in use" when prefix collides
  - Returns 400 "Default project cannot be archived" when slug === 'general' and body.status === 'archived'
  - Echoes updated project row in `{ project }` response body
  - normalizePrefix: input.trim().toUpperCase().replace(/[^A-Z0-9]/g, '').slice(0, 12)

From messages/en.json (existing project.common):
  - project.common.retry = "Retry"  (reuse, do NOT add loadErrorRetry key)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings-view test scaffold with it.todo stubs</name>
  <files>src/components/project/__tests__/settings-view.test.tsx</files>
  <read_first>
    - src/components/project/__tests__/sessions-view.test.tsx (existing Phase 5 scaffold — copy imports, mock setup, describe/it.todo style)
    - src/components/project/settings-view.tsx (current 16-line stub — confirms export name SettingsView)
    - src/components/project/project-context.tsx (useProjectWorkspace contract)
    - .planning/phases/06-settings/06-UI-SPEC.md (behavior contract and dirty-state rules)
    - .planning/phases/06-settings/06-CONTEXT.md (pitfalls list in code_context block)
    - .planning/phases/06-settings/06-VALIDATION.md (Wave 0 coverage requirements)
  </read_first>
  <action>
Create `src/components/project/__tests__/settings-view.test.tsx` using vitest + @testing-library/react. Mirror the Phase 5 scaffold style from `sessions-view.test.tsx`: top-of-file `vi.mock('next-intl', ...)` stub, `vi.mock('@/components/project/project-context', ...)` stub, and a single `describe('SettingsView', () => { ... })` block containing ONLY `it.todo(...)` stubs — no implementations yet.

Include these exact it.todo stubs (one per line, verbatim text inside quotes):

Basics (SETT-01):
- `it.todo('renders name input seeded from project.name and marks it required')`
- `it.todo('renders description textarea seeded from project.description (empty when null)')`
- `it.todo('renders status select with Active and Archived options seeded from project.status')`
- `it.todo('disables the Archived option when project.slug === "general"')`
- `it.todo('SETT-01: editing name, description, or status marks the form dirty and reveals the sticky footer')`

Appearance & Tracking (SETT-02):
- `it.todo('renders 8-swatch COLOR_PALETTE row plus a None pill seeded from project.color')`
- `it.todo('clicking an unselected swatch sets color; clicking the selected swatch clears color to empty string')`
- `it.todo('renders ticket_prefix input seeded from project.ticket_prefix with monospace + uppercase styling and maxLength 12')`
- `it.todo('renders ticket_prefix helper text from i18n key project.settings.prefixHelp')`
- `it.todo('renders deadline date input seeded as YYYY-MM-DD from project.deadline Unix seconds (empty when null)')`
- `it.todo('SETT-02: editing color, prefix, deadline, or github_repo marks the form dirty')`

Integrations (SETT-02):
- `it.todo('renders github_repo input seeded from project.github_repo with placeholder "owner/repo"')`

Save flow (SETT-03):
- `it.todo('SETT-03: Save click sends PATCH /api/projects/[id] with only dirty fields, name always included')`
- `it.todo('SETT-03: serializes deadline to Math.floor(new Date(value).getTime()/1000) and null when empty')`
- `it.todo('SETT-03: serializes empty description/github_repo/color as "" (server coerces to null)')`
- `it.todo('SETT-03: on 200 response calls useMissionControl.fetchProjects and clears dirty state')`
- `it.todo('SETT-03: does NOT call router.refresh or emit any SSE event after save')`
- `it.todo('SETT-03: never includes ticket_counter in the PATCH body')`

Dirty detection and pitfalls:
- `it.todo('Pitfall — ticket_prefix dirty-check compares normalized values (uppercased, alphanumeric-only, 12-char cap) so form is not false-dirty after server echoes normalized value')`
- `it.todo('Pitfall — deadline round-trip: loading project.deadline as Unix seconds and saving back yields the same date in the user local timezone the modal uses')`
- `it.todo('Pitfall — empty string vs null: description/github_repo/color that were null on load and remain empty do NOT mark the form dirty')`
- `it.todo('Pitfall — slug==="general" status archive still shows inline status error if server returns 400 (defensive handling, Archived option is UI-disabled)')`
- `it.todo('Pitfall — form re-seeding does not clobber in-progress edits: if projects[] refreshes while form is dirty, current inputs are preserved')`

Cancel + footer:
- `it.todo('Cancel resets all fields to the last-loaded project values and hides the sticky footer')`
- `it.todo('Save button is disabled when form is pristine')`
- `it.todo('Save button is disabled when trimmed name is empty')`
- `it.todo('Save button shows Saving… text while isSaving and disables Cancel')`

Error handling:
- `it.todo('400 "Project name cannot be empty" routes to inline error under Name field with i18n key project.settings.errorNameRequired')`
- `it.todo('409 "Ticket prefix already in use" routes to inline error under Ticket prefix field with i18n key project.settings.errorPrefixConflict')`
- `it.todo('400 "Invalid ticket prefix" routes to inline error under Ticket prefix field with i18n key project.settings.errorPrefixInvalid')`
- `it.todo('400 "Default project cannot be archived" routes to inline error under Status field with i18n key project.settings.errorDefaultArchive')`
- `it.todo('Unknown error renders top-of-form banner with server error text and focuses the banner (role="alert")')`
- `it.todo('Network failure renders top-of-form banner with project.settings.errorBannerFallback copy')`

Viewer role:
- `it.todo('D-20: viewer role renders all inputs with disabled attribute, hides the sticky footer, and shows readOnlyNote at the top of the form')`

i18n wiring:
- `it.todo('all user-facing strings resolve via useTranslations("project.settings") — no hardcoded English in the JSX')`

Add a top-of-file block comment documenting the test harness setup that Wave 1 will fill in:

```
/**
 * Wave 1 (Plan 06-01) fills these stubs. Harness notes:
 *  - vi.mock('next-intl', () => ({ useTranslations: (ns: string) => (key: string) => `${ns}.${key}` }))
 *  - vi.mock('@/components/project/project-context', () => ({ useProjectWorkspace: vi.fn() }))
 *  - vi.mock('@/store', () => ({ useMissionControl: vi.fn() }))
 *  - global.fetch = vi.fn() — assert .toHaveBeenCalledWith('/api/projects/<id>', { method: 'PATCH', ... })
 *  - Seed project fixture: { id: 42, slug: 'my-app', name: 'My App', description: 'About', ticket_prefix: 'MA', status: 'active', color: '#3b82f6', deadline: 1745000000, github_repo: 'me/app' }
 */
```

Import only what the stubs reference (describe, it, expect, vi from 'vitest'). Do NOT import SettingsView yet — the stubs are `it.todo` and have no bodies, so the import would dangle. Keep the file parseable (no TypeScript errors) so `pnpm typecheck` passes.
  </action>
  <verify>
    <automated>pnpm test -- src/components/project/__tests__/settings-view.test.tsx --run 2>&1 | grep -E "todo|pass"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it.todo(" src/components/project/__tests__/settings-view.test.tsx` returns at least 32
    - `grep -q "SETT-01" src/components/project/__tests__/settings-view.test.tsx`
    - `grep -q "SETT-02" src/components/project/__tests__/settings-view.test.tsx`
    - `grep -q "SETT-03" src/components/project/__tests__/settings-view.test.tsx`
    - `grep -q "Pitfall" src/components/project/__tests__/settings-view.test.tsx`
    - `grep -q "viewer" src/components/project/__tests__/settings-view.test.tsx`
    - `grep -q "project.settings.errorBannerFallback" src/components/project/__tests__/settings-view.test.tsx`
    - `pnpm test -- src/components/project/__tests__/settings-view.test.tsx --run` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Scaffold file exists with all listed it.todo stubs; vitest reports them as pending (todo), not failing; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Atomically add project.settings.* i18n keys across all 10 locales and extend i18n-coverage test</name>
  <files>
    messages/en.json, messages/ar.json, messages/de.json, messages/es.json, messages/fr.json, messages/ja.json, messages/ko.json, messages/pt.json, messages/ru.json, messages/zh.json, src/components/project/__tests__/i18n-coverage.test.tsx
  </files>
  <read_first>
    - messages/en.json (current project.settings block at lines 2277-2280; also project.common.retry at 2282)
    - messages/ar.json (current settings stub — structure reference)
    - messages/de.json
    - messages/es.json
    - messages/fr.json
    - messages/ja.json
    - messages/ko.json
    - messages/pt.json
    - messages/ru.json
    - messages/zh.json
    - src/components/project/__tests__/i18n-coverage.test.tsx (existing coverage test — extend, don't replace)
    - .planning/phases/06-settings/06-UI-SPEC.md (Copywriting Contract + i18n Key Manifest sections contain the authoritative English copy for all 29 new keys)
    - .planning/phases/05-sessions-agents/05-00-SUMMARY.md (brand-token-untranslated rule precedent)
  </read_first>
  <action>
Replace the existing two-key `project.settings` block (currently `{"title":"Settings","placeholder":"Project settings coming soon"}`) in ALL 10 locale files with a complete, translated block containing exactly these 30 keys (29 new + updated title). Also add `project.settings.loadErrorHeading`. Do NOT add `loadErrorRetry` — reuse existing `project.common.retry`.

**Authoritative English copy** (from UI-SPEC.md Copywriting Contract):

```json
"settings": {
  "title": "Project settings",
  "sectionBasics": "Basics",
  "sectionAppearance": "Appearance & Tracking",
  "sectionIntegrations": "Integrations",
  "readOnlyNote": "You need operator permissions to edit project settings.",
  "nameLabel": "Name",
  "namePlaceholder": "My project",
  "descriptionLabel": "Description",
  "descriptionPlaceholder": "What is this project about?",
  "statusLabel": "Status",
  "statusActive": "Active",
  "statusArchived": "Archived",
  "colorLabel": "Color",
  "colorNone": "None",
  "prefixLabel": "Ticket prefix",
  "prefixPlaceholder": "PA",
  "prefixHelp": "Changing the prefix affects only new tickets. Existing tickets keep their original prefix.",
  "deadlineLabel": "Deadline",
  "githubRepoLabel": "GitHub repository",
  "githubRepoPlaceholder": "owner/repo",
  "githubRepoHelp": "Format: owner/repo. Used for linking tickets and sync.",
  "save": "Save changes",
  "cancel": "Discard changes",
  "saving": "Saving…",
  "unsavedChanges": "Unsaved changes",
  "errorNameRequired": "Project name cannot be empty",
  "errorPrefixConflict": "Ticket prefix already in use. Try another.",
  "errorPrefixInvalid": "Invalid ticket prefix — letters and numbers only, up to 12 characters.",
  "errorDefaultArchive": "The default project cannot be archived.",
  "errorBannerHeading": "Could not save changes",
  "errorBannerFallback": "Failed to update project. Try again, or check the Live Feed for details.",
  "loadErrorHeading": "Could not load project settings"
}
```

The `placeholder` key MUST be removed from every locale (settings is no longer a stub).

**Translation rules** (per D-21, D-22, and Phase 5 precedent):
- Brand tokens `PA`, `owner/repo`, hex color strings stay untranslated across all locales
- ICU placeholders `{...}` preserved verbatim (none in this manifest, but preserve ellipsis `…` — use the locale's standard; most use `…`)
- `&` in "Appearance & Tracking" translated to the natural conjunction (e.g., French "Apparence et suivi")
- `GitHub` brand name stays untranslated in every locale
- Translation quality target: idiomatic target-language phrasing, not literal — match the voice of existing Phase 5 settings-adjacent copy already present in each file

Apply the change file-by-file in one task so the 290-entry diff ships atomically. Preserve each file's existing JSON structure, indentation, and trailing newline; only mutate the `project.settings` block.

**Extend** `src/components/project/__tests__/i18n-coverage.test.tsx` by adding, at the bottom of the existing describe block (before the `it.todo` entries at the end), a new `it('all 10 locale files have every project.settings.* key required by Phase 6', () => { ... })` test that iterates `REQUIRED_SUB_KEYS`-style over this exact array:

```typescript
const SETTINGS_KEYS = [
  'title','sectionBasics','sectionAppearance','sectionIntegrations','readOnlyNote',
  'nameLabel','namePlaceholder','descriptionLabel','descriptionPlaceholder',
  'statusLabel','statusActive','statusArchived',
  'colorLabel','colorNone',
  'prefixLabel','prefixPlaceholder','prefixHelp',
  'deadlineLabel',
  'githubRepoLabel','githubRepoPlaceholder','githubRepoHelp',
  'save','cancel','saving','unsavedChanges',
  'errorNameRequired','errorPrefixConflict','errorPrefixInvalid','errorDefaultArchive',
  'errorBannerHeading','errorBannerFallback',
  'loadErrorHeading',
]
```

For each locale file, assert `data.project.settings` contains each key (`expect(data.project.settings, ...).toHaveProperty(k)`) AND that `data.project.settings.placeholder` is undefined (the stub key must be gone). Also assert `data.project.settings.title === 'Project settings'` for `en.json` specifically (the canonical source; other locales are free to translate).

Keep the existing `it.todo('all 5 stub view components use useTranslations(project)')` entries at the end unchanged.
  </action>
  <verify>
    <automated>pnpm test -- src/components/project/__tests__/i18n-coverage.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 10 locale files: `node -e "const d=require('./messages/<locale>.json'); console.log(Object.keys(d.project.settings).length)"` prints `32` (31 listed keys above + any locale additions) — at minimum ≥ 32
    - `node -e "const d=require('./messages/en.json'); console.log(d.project.settings.title)"` prints `Project settings`
    - `node -e "const d=require('./messages/en.json'); console.log('placeholder' in d.project.settings)"` prints `false` for all 10 files
    - `grep -q "sectionBasics" messages/ar.json messages/de.json messages/en.json messages/es.json messages/fr.json messages/ja.json messages/ko.json messages/pt.json messages/ru.json messages/zh.json` exits 0
    - `grep -q "errorBannerFallback" messages/ar.json messages/de.json messages/en.json messages/es.json messages/fr.json messages/ja.json messages/ko.json messages/pt.json messages/ru.json messages/zh.json` exits 0
    - `grep -q "loadErrorHeading" messages/ar.json messages/de.json messages/en.json messages/es.json messages/fr.json messages/ja.json messages/ko.json messages/pt.json messages/ru.json messages/zh.json` exits 0
    - `grep -c "placeholder.*Project settings coming soon" messages/*.json` returns 0 across all 10 files
    - `grep -q "SETTINGS_KEYS" src/components/project/__tests__/i18n-coverage.test.tsx`
    - `pnpm test -- src/components/project/__tests__/i18n-coverage.test.tsx --run` exits 0 (all assertions pass)
    - `pnpm typecheck` exits 0
    - Every locale file remains valid JSON: `node -e "JSON.parse(require('fs').readFileSync('messages/<locale>.json','utf8'))"` succeeds for each
  </acceptance_criteria>
  <done>All 10 locale files carry the full translated project.settings.* namespace atomically in one commit, placeholder stub removed, i18n-coverage test extended and green.</done>
</task>

</tasks>

<verification>
- `pnpm test` full suite green (new settings-view.test.tsx it.todo's show as pending, not failing)
- `pnpm typecheck` clean
- `pnpm lint` clean
- All 10 locale files valid JSON with identical key structure under project.settings
</verification>

<success_criteria>
- settings-view.test.tsx scaffold exists with ≥32 it.todo stubs covering SETT-01, SETT-02, SETT-03, and every pitfall from 06-CONTEXT.md
- 10 locale files atomically updated to carry all 30+ project.settings.* keys (29 new + updated title + loadErrorHeading)
- Old project.settings.placeholder stub removed from all 10 files
- i18n-coverage test extended to assert the full settings key set across locales and fails if any key is missing
- Full vitest suite green; Plan 06-01 can run against existing stubs without locale-file churn
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings/06-00-SUMMARY.md` describing:
- Test scaffold created with it.todo counts per requirement
- 290 translation entries shipped atomically across 10 locales
- Any translation judgement calls made per locale
- Pitfall stub list copied verbatim into the test file for Plan 06-01 to fill
</output>
