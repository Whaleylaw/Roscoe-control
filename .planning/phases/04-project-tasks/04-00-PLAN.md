---
phase: 04-project-tasks
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/project/__tests__/tasks-view.test.tsx
  - src/components/panels/__tests__/task-board-panel.test.tsx
  - tests/project-tasks.spec.ts
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04

must_haves:
  truths:
    - "Test scaffold file exists at src/components/project/__tests__/tasks-view.test.tsx with it.todo() stubs covering TASK-01, TASK-03, TASK-04"
    - "Test scaffold file exists at src/components/panels/__tests__/task-board-panel.test.tsx with it.todo() stubs covering all 5 TaskBoardScope prop behaviors"
    - "Playwright scaffold exists at tests/project-tasks.spec.ts with test.fixme() stubs for TASK-02 and TASK-03"
    - "Full vitest suite remains green (todos are pending, not failures)"
  artifacts:
    - path: "src/components/project/__tests__/tasks-view.test.tsx"
      provides: "Integration test stubs for TasksView wrapper + TASK-01/03/04 coverage"
      contains: "it.todo"
    - path: "src/components/panels/__tests__/task-board-panel.test.tsx"
      provides: "Unit test stubs for TaskBoardScope prop behavior"
      contains: "it.todo"
    - path: "tests/project-tasks.spec.ts"
      provides: "E2E scaffolds for TASK-02 (create-in-workspace) and TASK-03 (reassign-out-disappears)"
      contains: "test.fixme"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for Phase 4 using the `it.todo()` / `test.fixme()` pattern established in Phases 1, 2, and 3. These stubs exist before the implementation wave so the suite stays green and every implementation task in Plan 04-01 has a concrete test bucket to fill.

Purpose: Satisfy Nyquist sampling — every Plan 04-01 implementation task has an automated test target. Match the precedent of `src/components/project/__tests__/dashboard-view.test.tsx`.
Output: 3 test scaffold files (2 vitest, 1 Playwright) with stubs covering TASK-01 through TASK-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-project-tasks/04-CONTEXT.md
@.planning/phases/04-project-tasks/04-RESEARCH.md
@.planning/phases/04-project-tasks/04-VALIDATION.md

@src/components/project/__tests__/dashboard-view.test.tsx
@src/components/project/__tests__/project-context.test.tsx
@src/components/project/tasks-view.tsx
</context>

<interfaces>
<!-- Types and hooks the test files will reference. Executor should NOT explore the codebase — use these directly. -->

From src/components/project/project-context.tsx:
```typescript
export function useProjectWorkspace(): {
  slug: string
  view: string
  project: { id: number; slug: string; name: string; description?: string; status: string } | null
  loading: boolean
  error: string | null
}
```

From src/store/index.ts (existing Task shape):
```typescript
export interface Task {
  id: number
  title: string
  status: 'backlog' | 'inbox' | 'assigned' | 'awaiting_owner' | 'in_progress' | 'review' | 'quality_review' | 'done' | 'failed'
  project_id?: number
  ticket_ref?: string
  // ... other fields
}
```

New interface that Plan 04-01 will add to src/components/panels/task-board-panel.tsx (referenced in todo stub descriptions):
```typescript
export interface TaskBoardScope {
  lockedProjectId: number
  hideProjectFilter?: boolean
  hideProjectLabels?: boolean
  defaultCreateProjectId?: number
}
```

API endpoint (critical: PUT, NOT PATCH):
```
PUT /api/tasks/[id]   // body: { project_id: number, ...other task fields }
POST /api/tasks       // body: { project_id, title, status, ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TasksView integration test scaffold (TASK-01, TASK-03, TASK-04)</name>
  <files>src/components/project/__tests__/tasks-view.test.tsx</files>
  <read_first>
    - src/components/project/__tests__/dashboard-view.test.tsx
    - src/components/project/__tests__/project-context.test.tsx
    - src/components/project/tasks-view.tsx
    - .planning/phases/04-project-tasks/04-RESEARCH.md
    - .planning/phases/04-project-tasks/04-VALIDATION.md
  </read_first>
  <action>
Create NEW file `src/components/project/__tests__/tasks-view.test.tsx` containing only `it.todo()` stubs (no implementations). Follow the structure of `dashboard-view.test.tsx` exactly — lead comment block with mock setup notes, then a top-level `describe('TasksView')` with nested `describe` blocks per requirement ID.

Use `import { describe, it } from 'vitest'` at the top (no runtime mocks needed for Wave 0 — mocks will be added in Plan 04-01 when stubs are filled).

Paste this exact content:

```typescript
import { describe, it } from 'vitest'

// Mock setup (to be implemented in Plan 04-01):
// - vi.mock('@/store', ...) for useMissionControl (tasks, projects, activeProject)
// - vi.mock('@/components/project/project-context', ...) for useProjectWorkspace
// - vi.mock('next-intl', ...) for useTranslations
// - vi.mock('next/navigation', ...) for useRouter / usePathname
// - global.fetch mock for /api/tasks and /api/projects
// - Note: TaskBoardPanel is embedded directly, not mocked — this is an integration test

describe('TasksView', () => {
  describe('TASK-01: tasks filtered to current project', () => {
    it.todo('renders only tasks whose project_id matches the workspace project')
    it.todo('does not render tasks belonging to other projects')
    it.todo('renders an empty column when the workspace project has no tasks in that status')
  })

  describe('TASK-01: project filter dropdown hidden in workspace mode', () => {
    it.todo('project filter <select> is not present in the rendered DOM')
    it.todo('all 9 status columns remain visible even with filter hidden (D-10)')
  })

  describe('TASK-01: card project label hidden in workspace mode', () => {
    it.todo('card renders without the ticket_ref span inside the column view')
    it.todo('detail modal still shows ticket_ref for task identity (pitfall #4)')
  })

  describe('TASK-03: reassigns out disappears', () => {
    it.todo('task reassigned away from workspace project disappears from the board immediately via client-side filter')
    it.todo('task reassigned into the workspace project appears in the board after next fetch')
    it.todo('EditTaskModal project dropdown is present and lists other projects as reassignment targets')
  })

  describe('TASK-04: feature parity with global board', () => {
    it.todo('Projects button, Spawn form, and GNAP badge all render inside workspace (D-04 no features stripped)')
    it.todo('drag-and-drop status change still dispatches PUT /api/tasks/[id]')
    it.todo('edit and delete actions on a task card behave identically to the global board')
  })
})
```

DO NOT add `vi.mock` calls, real renders, or expectations — this wave only creates the structural scaffold. Plan 04-01 will replace each `it.todo()` with the real test body.
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && pnpm vitest run src/components/project/__tests__/tasks-view.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/project/__tests__/tasks-view.test.tsx
    - File contains `describe('TasksView')`
    - File contains `describe('TASK-01: tasks filtered to current project'`
    - File contains `describe('TASK-01: project filter dropdown hidden in workspace mode'`
    - File contains `describe('TASK-01: card project label hidden in workspace mode'`
    - File contains `describe('TASK-03: reassigns out disappears'`
    - File contains `describe('TASK-04: feature parity with global board'`
    - File contains `it.todo('task reassigned away from workspace project disappears from the board immediately via client-side filter')`
    - File contains `it.todo('detail modal still shows ticket_ref for task identity (pitfall #4)')`
    - `grep -c "it.todo" src/components/project/__tests__/tasks-view.test.tsx` returns a number >= 13
    - File does NOT contain any `vi.mock(`, `render(`, or `expect(` calls
    - `pnpm vitest run src/components/project/__tests__/tasks-view.test.tsx` exits 0 with all todos shown as pending/skipped
  </acceptance_criteria>
  <done>Test scaffold file exists with it.todo() stubs covering TASK-01 (filter, dropdown hidden, card label hidden), TASK-03 (reassign-out), and TASK-04 (feature parity). Suite passes with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskBoardPanel scope-prop unit test scaffold (TASK-01, TASK-02, TASK-03, TASK-04)</name>
  <files>src/components/panels/__tests__/task-board-panel.test.tsx</files>
  <read_first>
    - src/components/project/__tests__/dashboard-view.test.tsx
    - src/components/panels/task-board-panel.tsx
    - .planning/phases/04-project-tasks/04-RESEARCH.md
    - .planning/phases/04-project-tasks/04-VALIDATION.md
  </read_first>
  <action>
Create NEW file `src/components/panels/__tests__/task-board-panel.test.tsx` (parent directory `src/components/panels/__tests__/` must be created if it does not exist). Contents are only `it.todo()` stubs — no real mocks or renders.

Paste this exact content:

```typescript
import { describe, it } from 'vitest'

// Mock setup (to be implemented in Plan 04-01):
// - vi.mock('@/store', ...) for useMissionControl (tasks, projects, agents, activeProject)
// - vi.mock('next-intl', ...) for useTranslations('taskBoard')
// - vi.mock('next/navigation', ...) for useRouter
// - global.fetch mock for /api/tasks (GET list, POST create), /api/tasks/[id] (PUT edit — NOT PATCH, pitfall #1), /api/projects, /api/agents
// - React Testing Library render() of <TaskBoardPanel scope={...} />

describe('TaskBoardPanel', () => {
  describe('scope default (undefined) — current behavior preserved (TASK-04 regression guard)', () => {
    it.todo('renders project filter <select> dropdown when scope is undefined')
    it.todo('renders card ticket_ref when scope is undefined')
    it.todo('CreateTaskModal defaults project_id to projects[0].id when scope is undefined')
    it.todo('respects activeProject Zustand selection when scope is undefined')
  })

  describe('TASK-01: scope.lockedProjectId filters tasks', () => {
    it.todo('renders only tasks whose project_id === scope.lockedProjectId')
    it.todo('client-side filter hides tasks whose project_id changes in storeTasks (pitfall #5 — SSE reassign-out defense)')
    it.todo('activeProject changes in Zustand do not override scope.lockedProjectId')
  })

  describe('TASK-01: scope.hideProjectFilter hides the filter dropdown', () => {
    it.todo('project filter <select> is not rendered when scope.hideProjectFilter is true')
    it.todo('Projects button and other top-bar controls remain visible (D-04)')
  })

  describe('TASK-01: scope.hideProjectLabels hides the card ticket_ref — card only, not detail modal (pitfall #4)', () => {
    it.todo('column-view card does not render ticket_ref span when scope.hideProjectLabels is true')
    it.todo('detail modal header still renders ticket_ref when scope.hideProjectLabels is true')
  })

  describe('TASK-02: scope.defaultCreateProjectId pre-fills CreateTaskModal project', () => {
    it.todo('CreateTaskModal useState initializer uses defaultProjectId when provided')
    it.todo('user can still change the project dropdown away from default (D-05 editable)')
    it.todo('submit dispatches POST /api/tasks with the selected project_id')
    it.todo('defaultProjectId surviving slow projects fetch (pitfall #3) — initializer does not clobber to empty string')
  })

  describe('TASK-03: reassigns out disappears', () => {
    it.todo('EditTaskModal project <select> is visible and enabled when scope is set')
    it.todo('submitting EditTaskModal with a different project_id calls PUT /api/tasks/[id] (NOT PATCH — pitfall #1)')
    it.todo('task with project_id !== scope.lockedProjectId is immediately filtered from the board')
  })

  describe('TASK-04: feature parity when scope is set', () => {
    it.todo('Projects button, Spawn form, GNAP badge, ProjectManagerModal all render (D-04)')
    it.todo('all 9 status columns render even when empty (D-10)')
    it.todo('drag-and-drop updates task status via PUT /api/tasks/[id]')
  })
})
```
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && pnpm vitest run src/components/panels/__tests__/task-board-panel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/panels/__tests__/task-board-panel.test.tsx
    - File contains `describe('TaskBoardPanel')`
    - File contains `describe('scope default (undefined) — current behavior preserved (TASK-04 regression guard)'`
    - File contains `describe('TASK-01: scope.lockedProjectId filters tasks'`
    - File contains `describe('TASK-01: scope.hideProjectFilter hides the filter dropdown'`
    - File contains `describe('TASK-01: scope.hideProjectLabels hides the card ticket_ref — card only, not detail modal (pitfall #4)'`
    - File contains `describe('TASK-02: scope.defaultCreateProjectId pre-fills CreateTaskModal project'`
    - File contains `describe('TASK-03: reassigns out disappears'`
    - File contains `describe('TASK-04: feature parity when scope is set'`
    - File contains `it.todo('detail modal header still renders ticket_ref when scope.hideProjectLabels is true')`
    - File contains `it.todo('submitting EditTaskModal with a different project_id calls PUT /api/tasks/[id] (NOT PATCH — pitfall #1)')`
    - File contains `it.todo('client-side filter hides tasks whose project_id changes in storeTasks (pitfall #5 — SSE reassign-out defense)')`
    - `grep -c "it.todo" src/components/panels/__tests__/task-board-panel.test.tsx` returns a number >= 18
    - File does NOT contain the substring `PATCH` anywhere except inside the "NOT PATCH" notation in the pitfall #1 reference
    - `pnpm vitest run src/components/panels/__tests__/task-board-panel.test.tsx` exits 0 with all todos pending
  </acceptance_criteria>
  <done>Task-board-panel unit test scaffold exists with stubs covering default-behavior regression guard + all 4 scope prop behaviors + TASK-03 edit modal + TASK-04 parity. Suite passes with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: Create Playwright E2E scaffold for TASK-02 and TASK-03</name>
  <files>tests/project-tasks.spec.ts</files>
  <read_first>
    - tests/tasks-crud.spec.ts
    - tests/projects-crud.spec.ts
    - .planning/phases/04-project-tasks/04-RESEARCH.md
    - .planning/phases/04-project-tasks/04-VALIDATION.md
  </read_first>
  <action>
Create NEW file `tests/project-tasks.spec.ts` using Playwright's `test.fixme()` pattern (analogous to vitest `it.todo`). These tests are pending until Plan 04-01 fills them in. `test.fixme` is the correct API — it marks tests as expected-to-fail so CI stays green.

Paste this exact content:

```typescript
import { test } from '@playwright/test'

// Mock/fixture setup (to be implemented in Plan 04-01):
// - Log in as admin via existing auth fixture pattern (see tests/tasks-crud.spec.ts)
// - Seed a project via POST /api/projects or use an existing slug
// - Navigate to /project/{slug}/tasks to enter the workspace tasks view
// - Use page.getByRole / getByTestId to interact with the embedded TaskBoardPanel

test.describe('Project workspace tasks — E2E', () => {
  test.describe('TASK-02: creating a task from within the workspace pre-scopes it', () => {
    test.fixme('open CreateTaskModal inside workspace — project dropdown defaults to the current project', async () => {})
    test.fixme('submit CreateTaskModal — POST /api/tasks payload includes the current project_id', async () => {})
    test.fixme('newly created task appears in the workspace board without switching filter', async () => {})
  })

  test.describe('TASK-03: reassigning a task via edit modal', () => {
    test.fixme('open EditTaskModal — project dropdown is visible with other projects selectable', async () => {})
    test.fixme('change project in EditTaskModal and submit — PUT /api/tasks/[id] (NOT PATCH) is called with new project_id', async () => {})
    test.fixme('reassigned-out task disappears from the workspace board after next re-fetch (pitfall #5)', async () => {})
  })
})
```

Do NOT set up real fixtures, do NOT add `page.goto` calls, do NOT attempt real assertions. Plan 04-01 will replace each `test.fixme(...)` with a real body.
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && npx playwright test --list tests/project-tasks.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/project-tasks.spec.ts
    - File contains `import { test } from '@playwright/test'`
    - File contains `test.describe('Project workspace tasks — E2E'`
    - File contains `test.describe('TASK-02: creating a task from within the workspace pre-scopes it'`
    - File contains `test.describe('TASK-03: reassigning a task via edit modal'`
    - File contains `test.fixme('submit CreateTaskModal — POST /api/tasks payload includes the current project_id'`
    - File contains `test.fixme('change project in EditTaskModal and submit — PUT /api/tasks/[id] (NOT PATCH) is called with new project_id'`
    - File contains `test.fixme('reassigned-out task disappears from the workspace board after next re-fetch (pitfall #5)'`
    - `grep -c "test.fixme" tests/project-tasks.spec.ts` returns a number >= 6
    - File does NOT contain `page.goto(` or `expect(` — bodies are empty stubs
    - `npx playwright test --list tests/project-tasks.spec.ts` lists 6 tests without syntax errors
  </acceptance_criteria>
  <done>Playwright scaffold file exists at tests/project-tasks.spec.ts with test.fixme stubs for TASK-02 (create-in-workspace) and TASK-03 (reassign-via-edit-modal). Playwright lists the tests without compile errors.</done>
</task>

</tasks>

<verification>
All three scaffold files exist and their test runners parse them without error:
- `pnpm vitest run src/components/project/__tests__/tasks-view.test.tsx src/components/panels/__tests__/task-board-panel.test.tsx` → exit 0, all tests pending
- `npx playwright test --list tests/project-tasks.spec.ts` → lists 6 tests with no TypeScript errors
- `pnpm test` (full suite) → 0 failures (todos show as skipped/pending)
</verification>

<success_criteria>
1. `src/components/project/__tests__/tasks-view.test.tsx` exists with TASK-01/03/04 it.todo() stubs
2. `src/components/panels/__tests__/task-board-panel.test.tsx` exists with scope-prop it.todo() stubs covering all 4 scope fields + default-behavior regression guard
3. `tests/project-tasks.spec.ts` exists with TASK-02/03 test.fixme() stubs
4. Full vitest suite is green (no new failures)
5. Playwright can list the new spec without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-project-tasks/04-00-SUMMARY.md`.

Do NOT add `Co-Authored-By` or similar attribution trailers to any commits, per CLAUDE.md.
</output>
