---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on:
  - 01-00
files_modified:
  - src/lib/migrations.ts
  - src/lib/__tests__/project-indexes.test.ts
  - src/components/project/__tests__/i18n-coverage.test.tsx
  - messages/en.json
  - messages/ar.json
  - messages/de.json
  - messages/es.json
  - messages/fr.json
  - messages/ja.json
  - messages/ko.json
  - messages/pt.json
  - messages/ru.json
  - messages/zh.json
autonomous: true
requirements:
  - FOUN-02
  - FOUN-04

must_haves:
  truths:
    - "EXPLAIN QUERY PLAN on `SELECT status, COUNT(*) FROM tasks WHERE project_id = ? GROUP BY status` shows SEARCH USING INDEX idx_tasks_project_status, not SCAN TABLE tasks"
    - "EXPLAIN QUERY PLAN on `SELECT * FROM claude_sessions WHERE project_slug = ? AND is_active = 1` shows SEARCH USING INDEX idx_sessions_project_active, not SCAN TABLE"
    - "All 10 locale message files contain a 'project' top-level key with workspace, nav, dashboard, tasks, sessions, agents, and settings sub-keys"
  artifacts:
    - path: "src/lib/migrations.ts"
      provides: "Migration 050 with composite indexes"
      contains: "050_project_workspace_indexes"
    - path: "messages/en.json"
      provides: "English project i18n namespace"
      contains: "project"
  key_links:
    - from: "src/lib/migrations.ts"
      to: "SQLite tasks table"
      via: "CREATE INDEX idx_tasks_project_status"
      pattern: "idx_tasks_project_status"
    - from: "src/lib/migrations.ts"
      to: "SQLite claude_sessions table"
      via: "CREATE INDEX idx_sessions_project_active"
      pattern: "idx_sessions_project_active"
---

<objective>
Add database composite indexes for project-scoped queries and create the i18n namespace for all project workspace UI strings across all 10 locale files. Fill in Wave 0 test stubs for FOUN-02 and FOUN-04.

Purpose: Provide the data layer (indexes) and string layer (i18n) that all subsequent project workspace components and queries depend on.
Output: Migration 050 in migrations.ts, "project" key in all 10 locale JSON files, green tests for indexes and i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Migration system pattern from src/lib/migrations.ts -->
From src/lib/migrations.ts:
```typescript
export type Migration = {
  id: string
  up: (db: Database.Database) => void
}

// Last migration is id: '049_agent_runtime_type'
// Next must be id: '050_project_workspace_indexes'

// Migrations array is defined as:
const migrations: Migration[] = [
  { id: '001_init', up: (db) => { ... } },
  // ...
  { id: '049_agent_runtime_type', up: (db) => { ... } },
]
```

<!-- i18n pattern from messages/en.json -->
From messages/en.json:
```json
{
  "common": { "save": "Save", "cancel": "Cancel", ... },
  "taskBoard": { "title": "Task Board", ... },
  ...
}
```
Top-level keys are namespace identifiers. Components access via `useTranslations('namespace')`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add database migration 050 with project composite indexes and fill in index tests</name>
  <files>src/lib/migrations.ts, src/lib/__tests__/project-indexes.test.ts</files>
  <read_first>
    - src/lib/migrations.ts (full file -- find migration 049 to insert after it)
    - src/lib/__tests__/project-indexes.test.ts (Wave 0 test stubs to fill in)
    - src/lib/db.ts (understand getDatabase export for test usage)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: Database Migration section)
  </read_first>
  <action>
**Part A: Add migration 050**

Add a new migration entry after the existing migration `049_agent_runtime_type` in the `migrations` array in `src/lib/migrations.ts`.

The new migration object:
```typescript
{
  id: '050_project_workspace_indexes',
  up: (db) => {
    // FOUN-02, D-09: Composite index for task count grouping by project + status
    db.exec(`CREATE INDEX IF NOT EXISTS idx_tasks_project_status ON tasks(project_id, status)`)
    // FOUN-02, D-09: Composite index for active session filtering by project
    db.exec(`CREATE INDEX IF NOT EXISTS idx_sessions_project_active ON claude_sessions(project_slug, is_active)`)
  }
}
```

Insert this object as the last element in the `migrations` array, before the closing `]`. Maintain the existing code style (2-space indentation, trailing comma after previous entry).

Per D-08: Do NOT add any foreign key migration for sessions. The existing `project_slug` column and `idx_claude_sessions_project` index remain as-is.

**Part B: Fill in Wave 0 test stubs for FOUN-02 and D-10**

Open `src/lib/__tests__/project-indexes.test.ts` and replace the `it.todo(...)` stubs with real test implementations:

1. Import `getDatabase` from `@/lib/db` and the migrations array (or verify migration ID exists by importing from `@/lib/migrations`).
2. Test that migration `050_project_workspace_indexes` exists in the migrations array.
3. Use `getDatabase()` to get a DB handle, then run:
   - `PRAGMA index_info(idx_tasks_project_status)` -- expect rows returned
   - `PRAGMA index_info(idx_sessions_project_active)` -- expect rows returned
4. **D-10 EXPLAIN QUERY PLAN verification**: Run these queries and assert the output contains `USING INDEX` (not `SCAN TABLE`):
   - `EXPLAIN QUERY PLAN SELECT status, COUNT(*) FROM tasks WHERE project_id = 'test-project' GROUP BY status` -- must show `idx_tasks_project_status`
   - `EXPLAIN QUERY PLAN SELECT * FROM claude_sessions WHERE project_slug = 'test-project' AND is_active = 1` -- must show `idx_sessions_project_active`

The EXPLAIN QUERY PLAN output is an array of objects with a `detail` field. Check that `detail` contains `USING INDEX idx_tasks_project_status` and does NOT contain `SCAN TABLE tasks`.
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && pnpm typecheck 2>&1 | tail -5 && pnpm test -- --run src/lib/__tests__/project-indexes.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/migrations.ts contains the string `050_project_workspace_indexes`
    - src/lib/migrations.ts contains `idx_tasks_project_status ON tasks(project_id, status)`
    - src/lib/migrations.ts contains `idx_sessions_project_active ON claude_sessions(project_slug, is_active)`
    - Migration 050 appears after migration 049 in the array
    - `pnpm typecheck` exits 0
    - project-indexes.test.ts passes with all tests green
    - EXPLAIN QUERY PLAN tests confirm SEARCH USING INDEX, not SCAN TABLE
  </acceptance_criteria>
  <done>Migration 050 exists with both composite indexes. Index tests pass including D-10 EXPLAIN QUERY PLAN verification confirming index usage by query planner.</done>
</task>

<task type="auto">
  <name>Task 2: Add project i18n namespace to all 10 locale files and fill in i18n tests</name>
  <files>messages/en.json, messages/ar.json, messages/de.json, messages/es.json, messages/fr.json, messages/ja.json, messages/ko.json, messages/pt.json, messages/ru.json, messages/zh.json, src/components/project/__tests__/i18n-coverage.test.tsx</files>
  <read_first>
    - messages/en.json (see existing top-level key structure)
    - src/components/project/__tests__/i18n-coverage.test.tsx (Wave 0 test stubs to fill in)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: i18n Namespace Addition section)
  </read_first>
  <action>
**Part A: Add i18n namespace to all 10 locale files**

Add a `"project"` top-level key to all 10 locale message files (per D-07, FOUN-04). Use the exact same English strings as placeholders in all non-English locale files (translation happens later).

The `"project"` key to add in each file (insert as a new top-level key, maintaining alphabetical order or appending at end -- match existing file convention):

```json
"project": {
  "workspace": {
    "title": "Project Workspace",
    "notFound": "Project not found",
    "loading": "Loading project..."
  },
  "nav": {
    "dashboard": "Dashboard",
    "tasks": "Tasks",
    "sessions": "Sessions",
    "agents": "Agents",
    "settings": "Settings"
  },
  "dashboard": {
    "title": "Dashboard",
    "placeholder": "Project dashboard coming soon"
  },
  "tasks": {
    "title": "Tasks",
    "placeholder": "Project tasks coming soon"
  },
  "sessions": {
    "title": "Sessions",
    "placeholder": "Project sessions coming soon"
  },
  "agents": {
    "title": "Agents",
    "placeholder": "Project agents coming soon"
  },
  "settings": {
    "title": "Settings",
    "placeholder": "Project settings coming soon"
  }
}
```

Files to update (all 10): `messages/en.json`, `messages/ar.json`, `messages/de.json`, `messages/es.json`, `messages/fr.json`, `messages/ja.json`, `messages/ko.json`, `messages/pt.json`, `messages/ru.json`, `messages/zh.json`.

Read each file first, then add the `"project"` key. Ensure valid JSON after editing (no trailing commas, proper nesting).

**Part B: Fill in Wave 0 i18n test stubs**

Open `src/components/project/__tests__/i18n-coverage.test.tsx` and replace the `it.todo(...)` stubs with real tests:

1. **en.json has project namespace**: Read and parse `messages/en.json`, assert it has a `project` key with sub-keys `workspace`, `nav`, `dashboard`, `tasks`, `sessions`, `agents`, `settings`.
2. **All 10 locale files have project namespace**: Use `fs.readdirSync('messages/')` to find all `.json` files, parse each, assert `project` key exists.
3. The stub-view source file checks (for `useTranslations`) should remain as `it.todo(...)` -- they will be filled in by Plan 02 after the stub views are created.

Use `import fs from 'fs'` and `import path from 'path'` for file-based assertions (reading message JSON files directly in tests).
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && for f in messages/*.json; do echo "--- $f ---"; node -e "const d=require('./$f'); if(!d.project||!d.project.workspace||!d.project.nav||!d.project.dashboard) { console.error('MISSING project namespace'); process.exit(1) } else { console.log('OK') }"; done && pnpm test -- --run src/components/project/__tests__/i18n-coverage.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files in messages/ contain a top-level "project" key
    - Each "project" object contains sub-keys: "workspace", "nav", "dashboard", "tasks", "sessions", "agents", "settings"
    - All 10 JSON files parse without error
    - i18n-coverage.test.tsx passes for the namespace and locale coverage tests
  </acceptance_criteria>
  <done>All 10 locale files contain the project i18n namespace with workspace, nav, and all 5 view sub-keys. JSON is valid in every file. i18n tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes (migration type-checks)
- All 10 locale files have valid JSON with "project" namespace
- Migration 050 is properly positioned after 049
- `pnpm test -- --run src/lib/__tests__/project-indexes.test.ts` passes (including EXPLAIN QUERY PLAN checks per D-10)
- `pnpm test -- --run src/components/project/__tests__/i18n-coverage.test.tsx` passes
</verification>

<success_criteria>
1. Migration 050 exists with both composite indexes (idx_tasks_project_status, idx_sessions_project_active)
2. EXPLAIN QUERY PLAN confirms both indexes are used (SEARCH USING INDEX, not SCAN TABLE)
3. All 10 locale message files contain the "project" i18n namespace with all required sub-keys
4. TypeScript compilation succeeds
5. All unit tests for indexes and i18n pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`

Do NOT add Co-Authored-By or similar attribution trailers to any commits, per CLAUDE.md.
</output>
