---
phase: 02-navigation-workspace-shell
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/project-workspace.test.ts
  - src/lib/__tests__/project-breadcrumb.test.ts
  - src/lib/__tests__/project-tabs.test.ts
  - src/components/project/__tests__/project-context.test.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05]

must_haves:
  truths:
    - "Test files exist with it.todo() stubs covering all NAV requirements"
    - "Existing project-context tests still pass"
  artifacts:
    - path: "src/lib/__tests__/project-workspace.test.ts"
      provides: "NAV-01 test stubs"
      contains: "it.todo"
    - path: "src/lib/__tests__/project-breadcrumb.test.ts"
      provides: "NAV-02, NAV-05 test stubs"
      contains: "it.todo"
    - path: "src/lib/__tests__/project-tabs.test.ts"
      provides: "NAV-03 test stubs"
      contains: "it.todo"
    - path: "src/components/project/__tests__/project-context.test.tsx"
      provides: "NAV-04 extended test stubs"
      contains: "it.todo"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for Phase 2 navigation components.

Purpose: Establish test stubs with it.todo() so the test suite stays green before implementation. Each stub describes a behavior that Plan 01 will make pass.
Output: 4 test files with it.todo() stubs covering NAV-01 through NAV-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-workspace-shell/02-CONTEXT.md
@.planning/phases/02-navigation-workspace-shell/02-RESEARCH.md
@.planning/phases/02-navigation-workspace-shell/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffolds for workspace, breadcrumb, and tabs</name>
  <files>
    src/lib/__tests__/project-workspace.test.ts,
    src/lib/__tests__/project-breadcrumb.test.ts,
    src/lib/__tests__/project-tabs.test.ts
  </files>
  <read_first>
    src/components/project/__tests__/project-context.test.tsx,
    src/components/project/project-workspace.tsx,
    src/components/project/project-view-router.tsx,
    .planning/phases/02-navigation-workspace-shell/02-VALIDATION.md
  </read_first>
  <action>
Create 3 new test files using `it.todo()` stubs. Follow the exact same pattern as the existing `project-context.test.tsx` (vitest imports, describe blocks, vi.mock for next/navigation and next-intl).

**src/lib/__tests__/project-workspace.test.ts** (NAV-01):
```
describe('ProjectWorkspace', () => {
  it.todo('renders ProjectBreadcrumb component')
  it.todo('renders ProjectTabs component')
  it.todo('renders ProjectViewRouter component')
  it.todo('displays loading skeleton while project data is being fetched')
  it.todo('shows not-found state when project slug has no match')
})
```

**src/lib/__tests__/project-breadcrumb.test.ts** (NAV-02, NAV-05):
```
describe('ProjectBreadcrumb', () => {
  it.todo('renders Projects as first segment')
  it.todo('renders project name as second segment')
  it.todo('renders current view name as third segment when not dashboard')
  it.todo('omits third segment when view is dashboard')
  it.todo('uses > as separator between segments')
  it.todo('Projects segment navigates to project list on click')
  it.todo('project name segment navigates to project dashboard on click')
  it.todo('current view segment is not clickable')
  it.todo('uses i18n translations for all text segments')
})
```

**src/lib/__tests__/project-tabs.test.ts** (NAV-03):
```
describe('ProjectTabs', () => {
  it.todo('renders all 5 tab buttons: Dashboard, Tasks, Sessions, Agents, Settings')
  it.todo('highlights the active tab matching current view')
  it.todo('clicking a tab calls router.push with correct URL')
  it.todo('dashboard tab navigates to /project/{slug}')
  it.todo('non-dashboard tabs navigate to /project/{slug}/{view}')
  it.todo('uses i18n translations for tab labels')
})
```
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && pnpm test -- --run src/lib/__tests__/project-workspace.test.ts src/lib/__tests__/project-breadcrumb.test.ts src/lib/__tests__/project-tabs.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/__tests__/project-workspace.test.ts contains `it.todo('renders ProjectBreadcrumb component')`
    - src/lib/__tests__/project-breadcrumb.test.ts contains `it.todo('renders Projects as first segment')`
    - src/lib/__tests__/project-breadcrumb.test.ts contains `it.todo('Projects segment navigates to project list on click')`
    - src/lib/__tests__/project-tabs.test.ts contains `it.todo('renders all 5 tab buttons: Dashboard, Tasks, Sessions, Agents, Settings')`
    - All 3 test files pass with vitest (todo stubs are skipped, not failures)
  </acceptance_criteria>
  <done>3 test scaffold files exist with it.todo() stubs covering NAV-01 through NAV-05. Test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend existing project-context test with NAV-04 stubs</name>
  <files>src/components/project/__tests__/project-context.test.tsx</files>
  <read_first>
    src/components/project/__tests__/project-context.test.tsx,
    src/components/project/project-context.tsx
  </read_first>
  <action>
Add a new describe block to the existing `project-context.test.tsx` file. Do NOT modify the existing 7 tests. Append a new block after the existing describe:

```typescript
describe('ProjectWorkspaceProvider - project data fetching (NAV-04)', () => {
  it.todo('exposes fetched project object in context when slug matches store')
  it.todo('sets loading=true while project is being resolved')
  it.todo('sets error when project slug not found in store or API')
  it.todo('calls setActiveProject on Zustand store when project is loaded')
  it.todo('clears activeProject on unmount')
})
```

These stubs will be implemented alongside the context provider extension in Plan 01.
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && pnpm test -- --run src/components/project/__tests__/project-context.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/components/project/__tests__/project-context.test.tsx contains `describe('ProjectWorkspaceProvider - project data fetching (NAV-04)'`
    - src/components/project/__tests__/project-context.test.tsx contains `it.todo('exposes fetched project object in context when slug matches store')`
    - src/components/project/__tests__/project-context.test.tsx contains `it.todo('clears activeProject on unmount')`
    - All existing 7 tests still pass (the 7 from Phase 1)
    - New todo stubs are skipped, not failing
  </acceptance_criteria>
  <done>Existing project-context test file extended with 5 NAV-04 todo stubs. All 7 existing tests still pass.</done>
</task>

</tasks>

<verification>
All test files run green:
```bash
pnpm test -- --run
```
No test failures, only skipped todo stubs.
</verification>

<success_criteria>
- 4 test files contain it.todo() stubs covering NAV-01 through NAV-05
- pnpm test passes with 0 failures
- Existing 7 project-context tests unchanged and passing
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-workspace-shell/02-00-SUMMARY.md`
</output>
