---
phase: 18.1-v1-2-runtime-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runtime/agent-contract.md
autonomous: true
requirements:
  - DOC-AGT
user_setup: []

must_haves:
  truths:
    - "An agent author can read docs/runtime/agent-contract.md and build a recipe image that passes the container contract end-to-end"
    - "Doc describes the two-hop submit→review→done lifecycle explicitly — agent POST {status:'done'} flips task to 'review', Aegis runAegisReviews() flips 'review'→'done' — NEVER says 'submit flips task to done'"
    - "Doc enumerates ALL 7 entries of RUNNER_TOKEN_ALLOWLIST (not 6)"
    - "Doc emphasizes host.docker.internal (NOT localhost) in every HTTP example inside the container"
    - "Doc uses fs.appendFileSync for progress.md and checkpoints.jsonl (NEVER fs.writeFileSync) — append-only invariant"
    - "Doc warns CONTAINER-01: secrets never flow on docker argv; always --env-file"
    - "Doc enumerates all MC_* container env vars, mount layout, preamble contract, exit codes"
    - "Doc is tool-AGNOSTIC (does NOT assume Claude Code, Aegis, or any specific agent SDK)"
    - "Every claim cites a source-of-truth file + line range"
  artifacts:
    - path: "docs/runtime/agent-contract.md"
      provides: "DOC-AGT — authoritative agent-side contract for recipe image authors"
      min_lines: 280
      contains: "RUNNER_TOKEN_ALLOWLIST"
  key_links:
    - from: "docs/runtime/agent-contract.md submit section"
      to: "src/app/api/runner/tasks/[task_id]/submit/route.ts + src/lib/task-dispatch.ts (runAegisReviews)"
      via: "two-hop prose + source citations for both flipper and reviewer"
      pattern: "submit → review|status=.review.|runAegisReviews"
    - from: "docs/runtime/agent-contract.md allowlist section"
      to: "src/lib/runner-tokens.ts:18-27 (RUNNER_TOKEN_ALLOWLIST)"
      via: "7-row table with method + path + description"
      pattern: "RUNNER_TOKEN_ALLOWLIST|7 entries"
    - from: "docs/runtime/agent-contract.md env vars section"
      to: "src/lib/runner-claim.ts composeEnvMap + docker/hello-world-agent/README.md:27-40"
      via: "table of MC_* variables with source links"
      pattern: "MC_API_URL|MC_API_TOKEN|MC_TASK_ID"
    - from: "docs/runtime/agent-contract.md progress.md/checkpoints.jsonl section"
      to: "STATE.md Phase 14-07 (seedMcDir preserves on resume) + Phase 17-05 (append-only byte-assert)"
      via: "prose using 'append' language + fs.appendFileSync in every code example"
      pattern: "append|appendFileSync"
---

<objective>
Write `docs/runtime/agent-contract.md` — the authoritative operator + agent-author reference for "what a recipe image MUST do." This is the MOST load-bearing doc in the phase because it is the contract that agent-image authors implement against.

Purpose: Agent authors today must read 5-6 source files (`src/lib/runner-preamble.ts`, `src/app/api/runner/tasks/[task_id]/submit/route.ts`, `src/lib/runner-tokens.ts`, `src/lib/runner-docker.ts`, `docker/hello-world-agent/agent.mjs`) to piece together the container contract. This plan consolidates all of it into one reference doc, anchored on the canonical `docker/hello-world-agent/` example.

Output: `docs/runtime/agent-contract.md` addressing pitfalls #1 (submit→review two-hop), #2 (7-entry allowlist), #5 (host.docker.internal not localhost), #6 (secrets off argv), and #10 (append-only progress/checkpoints). The doc must be TOOL-AGNOSTIC per the user-memory file "Agent runtime is tool-agnostic" — it documents the substrate contract, NOT any specific agent SDK (no Claude Code assumptions, no Aegis assumptions).

Scope guardrail: DOCUMENTATION ONLY. No code changes anywhere.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-v1-2-runtime-documentation/18.1-RESEARCH.md

<!-- Canonical worked example — LINK TO, don't duplicate -->
@docker/hello-world-agent/README.md
@docker/hello-world-agent/agent.mjs
@docker/hello-world-agent/Dockerfile

<!-- Source-of-truth files the executor MUST cite -->
@src/lib/runner-preamble.ts
@src/app/api/runner/tasks/[task_id]/submit/route.ts
@src/app/api/runner/tasks/[task_id]/container-started/route.ts
@src/app/api/runner/tasks/[task_id]/runner-exit/route.ts
@src/app/api/tasks/[id]/checkpoints/route.ts
@src/lib/runner-tokens.ts
@src/lib/runner-docker.ts
@src/lib/runner-claim.ts
@src/lib/task-dispatch.ts

<interfaces>
<!-- RUNNER_TOKEN_ALLOWLIST — all 7 entries (src/lib/runner-tokens.ts:18-27) -->
1. POST /api/runner/tasks/:id/checkpoints  (Phase 14, runner-token)
2. POST /api/runner/tasks/:id/submit       (agent declares completion/fail, flips task to review)
3. POST /api/runner/tasks/:id/fail         (agent declares unrecoverable failure)
4. GET  /api/runner/tasks/:id/status       (agent polls own status)
5. GET  /api/runner/tasks/:id              (agent reads own task)
6. GET  /api/runner/tasks/:id/comments     (agent reads own task comments)
7. POST /api/tasks/:id/checkpoints          (Phase 15 CP-01 — literal roadmap path, runner-token ALSO reaches this)

<!-- Key invariants from STATE.md -->
Phase 17-01 RTEST-02: submit route flips task.status='in_progress' → 'review'; Aegis runAegisReviews() flips 'review' → 'done'. ALREADY_SETTLED set now includes 'review' so retries 409 cleanly.
Phase 14-05: MC_API_URL inside container = http://host.docker.internal:${PORT || 3000}; runner passes --add-host host.docker.internal:host-gateway
Phase 14-07: stageRecipe writes PREAMBLE.md AFTER deep-copy so runner owns /recipe/PREAMBLE.md; reading order PREAMBLE.md → SOUL.md → /workspace/.mc/*
Phase 14-07: seedMcDir preserves progress.md + checkpoints.jsonl on resume (is_resuming=true); ALWAYS rewrites task.json with new attempt counter + prior_attempts
Phase 14-07: CONTAINER-01 — no --env flag on docker argv carries MC_API_TOKEN value; secrets flow via --env-file
Phase 14-09: agent.mjs exit codes — 1=main() throw, 3=POST submit non-2xx, 4=POST submit fetch threw (network)
Phase 14-09: agent has NO /api/runner/checkpoint HTTP call in Phase 14 — only local file-append; Phase 15 CP-01 added the HTTP endpoint
Phase 17-05: append-only byte-assert — jsonlAfterResume.slice(0, jsonlAfterKill.length) === jsonlAfterKill
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/runtime/agent-contract.md — the canonical container contract reference</name>
  <files>docs/runtime/agent-contract.md</files>
  <action>
Create `docs/runtime/agent-contract.md` as the authoritative contract every recipe-image agent implements against.

Structure (Pattern 1 + Pattern 3 "paired prose + canonical code block"):

## Header
- H1: "Agent Contract"
- Source of truth: `docker/hello-world-agent/` (canonical worked example), `src/lib/runner-preamble.ts`, `src/lib/runner-docker.ts`, `src/app/api/runner/tasks/[task_id]/submit/route.ts`, `src/app/api/tasks/[id]/checkpoints/route.ts`, `src/lib/runner-tokens.ts`
- Who reads this: Authors of new recipe images (any agent framework — this contract is tool-agnostic)
- Prerequisites: Familiarity with Docker, HTTP, filesystem I/O. NO assumptions about which agent SDK you use.
- Map TOC: The 7-step contract, Container env vars, Mount layout, Reading order (preamble → soul → .mc), Progress + checkpoints (append-only), Checkpoint HTTP endpoint, Submit HTTP endpoint (two-hop lifecycle), Runner-token allowlist (7 entries), Runner-token lifetime, Exit codes, Resume behavior, Blocker flow, Drift-guards, Reference image, Related docs

## Section: The 7-step contract (overview)
- Prose: quote `docker/hello-world-agent/README.md:15-25` structure. Say explicitly: "This is what the reference image (`mc-hello-world-agent`) does. Your recipe image is free to do more inside these steps, but the skeleton must be preserved."
- Numbered list — each step is a hyperlink to the relevant section below:
  1. Env snapshot — log the MC_* env vars (never the token value)
  2. Read preamble + SOUL.md
  3. Append to progress.md
  4. (Optional for richness) POST checkpoint via HTTP
  5. Do the task's real work in /workspace
  6. POST submit
  7. Exit 0 on success, non-zero on failure

## Section: Container env vars
- Preamble sentence: "The runner composes these via `POST /api/runner/claim/:task_id` (claim route) and passes them into the container via `docker run --env-file` — NEVER on argv (CONTAINER-01 invariant)."
- Fenced code block quoting `docker/hello-world-agent/README.md:27-40` verbatim (the env var list).
- 4-column table "Name | Type | Source-of-truth | Notes":
  - MC_API_URL | URL | runner-claim.ts composeEnvMap | `http://host.docker.internal:<PORT>` — NOT localhost
  - MC_TASK_ID | string | runner-claim.ts | integer id, stringified
  - MC_API_TOKEN | bearer | runner-claim.ts | per-task runner-token (principal -2000); expires `runner_started_at + recipe.timeout_seconds + 60s`
  - MC_WORKSPACE | path | runner-docker.ts | `/workspace`
  - MC_RECIPE_PATH | path | runner-docker.ts | `/recipe`
  - MC_PREAMBLE_PATH | path | runner-docker.ts | `/recipe/PREAMBLE.md`
  - MC_MODEL_PRIMARY | string | runner-claim.ts | effective model id
  - MC_MODEL_PROVIDER | string | runner-claim.ts | e.g. `anthropic`
  - MC_MODEL_PARAMS_JSON | json | runner-claim.ts | serialized params
  - MC_MODEL_FALLBACK | string (optional) | runner-claim.ts | optional fallback id
- EXPLICIT PITFALL CALLOUT #5 — "⚠️ Inside the container, `$MC_API_URL` is `http://host.docker.internal:<port>`, NOT `localhost`":
  - "Containers CANNOT reach the host's `localhost`. The runner passes `--add-host host.docker.internal:host-gateway` so `host.docker.internal` resolves to the host IP. Every curl / fetch / HTTP client inside the container MUST use `$MC_API_URL` (or `host.docker.internal`). Using `http://localhost:3000` will hang or 404."
  - Cite STATE.md Phase 14-05 lock.

## Section: Mount layout
- Fenced ASCII tree quoted from `src/lib/runner-docker.ts` + `docker/hello-world-agent/README.md`:
  ```
  /workspace           ← git worktree, rw (when workspace_mode: worktree)
  /recipe/             ← recipe directory, ro
     ├── recipe.yaml
     ├── SOUL.md
     ├── PREAMBLE.md   ← authored by runner at claim time (overrides any recipe-authored file)
     └── (tools/, skills/, README.md if recipe-authored)
  /refs/<label-slug>/  ← each read_only_mount, ro
  /skills/<name>/      ← each extra_skill, ro
  ```
- Note: When `workspace_mode: readonly`, `/workspace` is also ro. When `workspace_mode: none`, `/workspace` is not mounted.

## Section: Reading order (preamble → soul → .mc)
- Prose: "The runner authors `/recipe/PREAMBLE.md` at claim time. It is the FIRST thing your agent should read. The recipe-authored `SOUL.md` sits beside it describing your agent's domain steps. After the preamble and soul, your agent reads any prior attempt context from `/workspace/.mc/progress.md` and `/workspace/.mc/checkpoints.jsonl`."
- Cite `src/lib/runner-preamble.ts` (writes the preamble) and STATE.md Phase 14-07 lock ("stageRecipe writes PREAMBLE.md AFTER deep-copy").
- Show the preamble HTTP skeleton verbatim (from `src/lib/runner-preamble.ts:69-81`):
  ```text
  POST $MC_API_URL/api/runner/checkpoint
  Authorization: Bearer $MC_API_TOKEN
  Content-Type: application/json

  { "task_id": $MC_TASK_ID, "step": "short-slug", "status": "in_progress", "summary": "what you just did" }
  ```
- Note: Phase 14 shipped file-only checkpoints; Phase 15 CP-01 added the HTTP endpoint at `POST /api/tasks/:id/checkpoints` (NOT `/api/runner/checkpoint` — the preamble URL is an alias the server also accepts for back-compat).

## Section: Progress + checkpoints (APPEND-ONLY)
- EXPLICIT PITFALL CALLOUT #10 — "⚠️ `.mc/progress.md` and `.mc/checkpoints.jsonl` are APPEND-ONLY across attempts":
  - "When your agent runs on attempt 2+, progress.md and checkpoints.jsonl still contain attempt-1's history. Your agent MUST use `fs.appendFileSync` — NEVER `fs.writeFileSync`. The runner's `seedMcDir` preserves both files on resume and rewrites only `task.json`."
  - "Phase 17-05 byte-asserts this: `jsonlAfterResume.slice(0, jsonlAfterKill.length) === jsonlAfterKill` — any write-over breaks the test."
- Show BOTH the correct and the wrong pattern:
  ```javascript
  // ✅ Correct — append keeps prior-attempt history
  fs.appendFileSync('/workspace/.mc/progress.md', `${ts} step: hello\n`)
  fs.appendFileSync('/workspace/.mc/checkpoints.jsonl', JSON.stringify({...}) + '\n')

  // ❌ Wrong — overwrites attempt-1 history on resume
  fs.writeFileSync('/workspace/.mc/progress.md', 'fresh\n')
  ```
- Cite STATE.md Phase 14-07 (seedMcDir preserves) + Phase 17-05 (byte assert).

## Section: Checkpoint HTTP endpoint (Phase 15 CP-01)
- Prose: "Phase 15 added `POST /api/tasks/:id/checkpoints` so your agent can report live status to the server. Schema: `{ step, status, summary?, artifacts?, next_step?, blocker_reason?, tokens_used?, duration_ms? }`. Status must be one of `completed | in_progress | blocked`."
- Cite `src/app/api/tasks/[id]/checkpoints/route.ts`.
- Fenced bash example:
  ```bash
  curl -X POST "$MC_API_URL/api/tasks/$MC_TASK_ID/checkpoints" \
    -H "Authorization: Bearer $MC_API_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"step":"did-a-thing","status":"in_progress","summary":"finished parsing input"}'
  ```
- Special case: `status: blocked` with `blocker_reason` transitions the task `in_progress → awaiting_owner` and gracefully stops the container (Phase 15 CP-03). Your agent SHOULD exit 0 immediately after POSTing a blocker — the runner handles the stop.

## Section: Submit HTTP endpoint (THE TWO-HOP LIFECYCLE)
- EXPLICIT PITFALL CALLOUT #1 — "⚠️ `submit` flips task to `review`, NOT `done`":
  - "When your agent POSTs `{status:'done'}` to `/api/runner/tasks/:id/submit`, the server flips `task.status` from `in_progress` to `review`, NOT to `done`. An Aegis review pass (`runAegisReviews()` in `src/lib/task-dispatch.ts`) approves the review and flips `review → done` in a second hop. The body literal `{status:'done'}` is the agent's declaration of intent; the server translates it."
  - "This is Phase 17-01 RTEST-02 LOCKED (commit `e9e5fc1`). Docs that describe submit as 'marking the task done' are wrong."
  - Cite `src/app/api/runner/tasks/[task_id]/submit/route.ts` (the flipper) AND `src/lib/task-dispatch.ts:414` (runAegisReviews, the reviewer).
- Show the canonical submit snippet verbatim (from `docker/hello-world-agent/agent.mjs:81-94`):
  ```bash
  curl -X POST "$MC_API_URL/api/runner/tasks/$MC_TASK_ID/submit" \
    -H "Authorization: Bearer $MC_API_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"status":"done"}'
  ```
- Note the two-hop transition explicitly:
  ```
  agent POST submit {status:'done'}
     → server: task.status = 'review'
     → runAegisReviews() (scheduler)
        → on approve: task.status = 'review' → 'done'
        → on reject: task.status = 'review' → 'assigned' (retry with review comments)
  ```

## Section: Runner-token allowlist (ALL 7 ENTRIES)
- EXPLICIT PITFALL CALLOUT #2 — "⚠️ The runner-token allowlist has SEVEN entries (not six)":
  - "Phase 15 CP-01 added `POST /api/tasks/:id/checkpoints` to the allowlist. The original comment in `src/lib/runner-tokens.ts` said 'DO NOT add entries'; that lock was explicitly revoked for CP-01 with a pointer to `.planning/phases/15-checkpoints-scheduler-v1-2/15-CONTEXT.md`."
- 4-column table "Method | Path | Added in | Purpose" — ALL 7 ROWS (no shortening):
  1. POST | /api/runner/tasks/:id/checkpoints | Phase 14 | Runner-scoped checkpoint write
  2. POST | /api/runner/tasks/:id/submit | Phase 14 | Agent declares completion
  3. POST | /api/runner/tasks/:id/fail | Phase 14 | Agent declares unrecoverable failure
  4. GET | /api/runner/tasks/:id/status | Phase 14 | Agent polls task status
  5. GET | /api/runner/tasks/:id | Phase 14 | Agent reads own task
  6. GET | /api/runner/tasks/:id/comments | Phase 14 | Agent reads own task comments
  7. POST | /api/tasks/:id/checkpoints | Phase 15 CP-01 | Literal roadmap path (agent-authored checkpoint with blocker support)
- Cite `src/lib/runner-tokens.ts:18-27` verbatim in a fenced ```typescript block.

## Section: Runner-token lifetime
- Prose: "The token's expiry is `runner_started_at + recipe.timeout_seconds + 60s`. If your agent exceeds the timeout, the runner SIGKILLs the container and the token is revoked."
- Cite `src/lib/runner-tokens.ts` (issueRunnerToken) and STATE.md Phase 11 runner-token-per-attempt model.

## Section: Exit codes
- 2-column table of agent-side exit codes (matches `docker/hello-world-agent/agent.mjs`):
  - 0 — Success (after successful POST submit)
  - 1 — main() threw (unhandled error)
  - 3 — POST submit returned non-2xx (allowlist reject, schema fail, etc.)
  - 4 — POST submit fetch threw (network / host.docker.internal unreachable)
- Cite STATE.md Phase 14-09 lock.
- Prose: "The runner classifies your exit via `runner-exit` (reason=`exit`, exit_code=N) for the attempt history."

## Section: Resume behavior
- Prose: "On attempts > 1, the runner sets `.mc/task.json.is_resuming = true` and `prior_attempts = [{started_at, exit_code, failure_reason}]`. Your agent SHOULD check these fields and read `.mc/progress.md` + `.mc/checkpoints.jsonl` to avoid redoing completed work."
- Cite `src/lib/runner-preamble.ts` (resume preamble variant) and STATE.md Phase 14-07 (seedMcDir).

## Section: Blocker flow
- Prose: "If your agent hits an unrecoverable external dependency (e.g. missing credential), POST a checkpoint with `status:'blocked'` and `blocker_reason:'...'`. The server transitions the task to `awaiting_owner` and posts an automatic system comment. When the human operator resolves the blocker, the task returns to `assigned` and the runner relaunches your image with `is_resuming=true` and a resume marker inline in `.mc/progress.md`."
- Cite `src/app/api/tasks/[id]/checkpoints/route.ts` (CP-03 blocker branch) + STATE.md Phase 15-05 (blocker flow).

## Section: Drift-guards for agent authors
- Quick checklist of "⚠️ do not":
  - ❌ Do not use `http://localhost` inside the container. Use `$MC_API_URL`.
  - ❌ Do not use `fs.writeFileSync` on progress.md or checkpoints.jsonl. Use append.
  - ❌ Do not call `PUT /api/tasks/:id` — that path is NOT on the runner-token allowlist. Use `POST /api/runner/tasks/:id/submit`.
  - ❌ Do not pass secrets on `docker run` argv — your container never needs to; the runner handles `--env-file`.
  - ❌ Do not assume `submit` finalizes the task. It flips to `review`. Aegis approves.
  - ❌ Do not hard-code your agent to any specific SDK (Claude Code, Aegis, etc.). The contract is tool-agnostic.

## Section: Reference image
- Prose: "The canonical worked example is `docker/hello-world-agent/`. It exercises every step of this contract without calling any external model provider. Build via `pnpm mc:build-hello-world` or `bash docker/hello-world-agent/build.sh`."
- Cross-link to `docker/hello-world-agent/README.md` (authoritative build + run reference).

## Section: Related docs
- Bullets: recipes.md (how to declare a recipe that uses your image), runner-daemon.md (what launches your image), admin-config.md (secrets store + auth tiers), task-board-surfaces.md (how progress + submit events surface in the UI), getting-started.md (end-to-end walkthrough).

## Writing rules
- TOOL-AGNOSTIC: never say "Claude Code" or assume a specific agent SDK. The contract is a substrate contract.
- Every curl / fetch example uses `$MC_API_URL` (the variable), NEVER `localhost` — enforced by the harness later.
- Append-only: every progress/checkpoints example uses `appendFileSync`.
- All 7 allowlist entries enumerated; no shortening.
- Every claim cites a source file + line range.
- Relative links (`../../` to repo root); ⚠️ emoji for callouts; conventional commits; no AI attribution.
  </action>
  <verify>
    <automated>test -f docs/runtime/agent-contract.md &amp;&amp; wc -l docs/runtime/agent-contract.md | awk '$1 &gt;= 280 { exit 0 } { exit 1 }' &amp;&amp; grep -q "RUNNER_TOKEN_ALLOWLIST" docs/runtime/agent-contract.md &amp;&amp; grep -q "submit → review\|status=.review.\|task\.status = .review." docs/runtime/agent-contract.md &amp;&amp; grep -q "host.docker.internal" docs/runtime/agent-contract.md &amp;&amp; grep -q "appendFileSync" docs/runtime/agent-contract.md &amp;&amp; grep -q "CONTAINER-01\|--env-file" docs/runtime/agent-contract.md &amp;&amp; grep -q "7 entries\|SEVEN entries\|seven entries" docs/runtime/agent-contract.md &amp;&amp; ! grep -q "submit flips.*done\b" docs/runtime/agent-contract.md &amp;&amp; ! grep -iq "Claude Code" docs/runtime/agent-contract.md</automated>
  </verify>
  <done>docs/runtime/agent-contract.md exists, is at least 280 lines, explicitly describes the submit→review two-hop lifecycle, enumerates all 7 allowlist entries, emphasizes host.docker.internal, uses appendFileSync in every progress/checkpoints code example, contains the CONTAINER-01 secrets-off-argv warning, and is tool-agnostic (no Claude Code or Aegis-SDK assumptions).</done>
</task>

</tasks>

<verification>
- File exists; byte-count sanity check passes
- All 5 targeted pitfalls (#1, #2, #5, #6, #10) have explicit ⚠️ callouts
- Plan 07 (harness) will grep-count `runner-token` allowlist entries in src/lib/runner-tokens.ts and assert the doc table has same count
- Plan 07 drift-guard will fail-fast on any occurrence of `submit → done` or `PUT /api/tasks/` in the file
</verification>

<success_criteria>
- docs/runtime/agent-contract.md created and passes verify grep assertions
- Tool-agnostic (no Claude Code / Aegis SDK assumptions)
- All references to the task lifecycle use the two-hop "review" language
- No changes outside docs/runtime/
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-v1-2-runtime-documentation/18.1-03-SUMMARY.md`.
</output>
