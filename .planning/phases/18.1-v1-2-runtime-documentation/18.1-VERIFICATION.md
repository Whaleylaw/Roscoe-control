---
phase: 18.1-v1-2-runtime-documentation
verified: 2026-04-21T18:45:00Z
status: passed
score: 7/7 must-haves verified
harness_run:
  command: node scripts/verify-runtime-docs.mjs --verbose
  result: "10/10 checks passed"
  wall_clock_ms: 190
  exit_code: 0
harness_tests:
  command: pnpm test:docs
  result: "37/37 tests passed"
  wall_clock_ms: 253
human_verification:
  - test: "Operator follows docs/runtime/getting-started.md end-to-end on a Docker-equipped workstation and reaches task.status=done"
    expected: "Task created via curl in Step 6 transitions assigned → in_progress → review → done within ~60s; Progress tab displays checkpoint timeline; .data/runner/logs/task-<id>/attempt-1/stderr.log shows the 6 agent steps + successful submit"
    why_human: "Tutorial executability is partly observational — the harness parses shell syntax but does not execute the curl/docker/runner pipeline. scripts/mc-runner-smoke.sh exists as a scripted end-to-end harness that could substitute, but requires a Docker-equipped host and a live MC server; the milestone audit scored integration 47/47 which is reasonable prior evidence that the pipeline works."
  - test: "Render docs/runtime/INDEX.md on GitHub (or a Mermaid-capable Markdown viewer) and confirm the sequence diagram draws correctly"
    expected: "Mermaid sequenceDiagram renders with Operator → MC → Scheduler → Runner → Docker → Agent → Aegis actors and the claim → submit → review → Aegis → done pipeline visible"
    why_human: "Mermaid rendering is a GitHub-side concern; the harness code-fences check confirms the fence has the 'mermaid' language hint but does not instantiate a renderer. Low risk (Mermaid syntax is small) but observational."
  - test: "Spot-check that the operator reading docs/runtime/INDEX.md → one of the six sub-docs → the cited source file can trace a factual claim back to the source line in ≤3 clicks"
    expected: "Every relative link is click-traversable; source-of-truth citations (e.g. 'src/lib/recipe-schema.ts:37-71') resolve to real lines containing the quoted content"
    why_human: "Harness checks link existence and anchor-slug match; it does not validate that the cited line-range actually contains the claim being made. Prior plan self-checks included byte-exact diff confirmations for the recipe-schema.ts quote; for other citations this is operator judgement."
---

# Phase 18.1: v1.2 Runtime Documentation Verification Report

**Phase Goal:** Ship operator-facing documentation for v1.2's recipe-based ephemeral agent runtime — recipes, runner daemon, agent contract, admin config, and task-board surfaces — so v1.2 can archive with a complete operator manual, plus a verification harness that prevents doc-drift from recurring.

**Verified:** 2026-04-21T18:45:00Z
**Status:** PASSED
**Score:** 7/7 must-haves verified
**Re-verification:** No — initial verification

## Goal Achievement

### Phase-goal narrative

**Can v1.2 be archived with a complete operator manual?** Yes. The `docs/runtime/` subdirectory now holds all six surface-scoped references plus an entry-point INDEX, totaling 1,872 lines of operator-facing documentation authored against shipped source files. All six bundles identified in 18.1-RESEARCH.md § Doc Bundle Recommendations shipped; README.md surfaces them from the repo-root Documentation table; the design-era spec carries a banner pointing at `docs/runtime/`; `docs/cli-agent-control.md` has a runtime link-back. An operator landing on the README can reach any runtime surface in ≤2 clicks.

**Does the harness prevent future drift?** Yes. `scripts/verify-runtime-docs.mjs` (908 lines, Node 22 ESM, zero new npm deps) implements all 10 cross-reference checks from 18.1-RESEARCH.md § Validation Architecture, exits non-zero on any drift, and runs in ~190ms. `scripts/__tests__/verify-runtime-docs.test.mjs` (37 vitest tests on hermetic fixture inputs) pins the harness's own parser/slugify/link/drift-guard logic against future refactors. `package.json` surfaces it as `pnpm docs:verify-runtime`. The harness's first run against the completed doc set surfaced three real drift hits (wrong test filename in two docs + legacy "submit→done" phrasing in INDEX.md) — the plan's "harness is authority" directive was followed and the drift was fixed inline before phase close. This is exactly the drift-prevention loop the phase targeted.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 6 operator-facing surface docs exist with substantive content | ✓ VERIFIED | `docs/runtime/` contains 7 files (INDEX + 6 surfaces); line counts: recipes=266, runner-daemon=184, admin-config=269, agent-contract=439, task-board-surfaces=349, getting-started=272, INDEX=93. All exceed plan `min_lines` thresholds. |
| 2 | Every doc's declared must_have artifacts + key_links exist and match source | ✓ VERIFIED | Per-plan grep checks in §Artifact Verification and §Key Link Verification tables below. Every `key_links[].pattern` grep returned ≥1 hit. |
| 3 | README.md surfaces the full runtime-doc set from the repo root | ✓ VERIFIED | README.md lines 184-190 contain all 7 `docs/runtime/*.md` paths; line 46 shows `33+ panels` (drift fix); line 227 declares `Ephemeral Agent Runtime (v1.2)` feature block. |
| 4 | Harness verifies doc/source sync and exits 0 against current tree | ✓ VERIFIED | `node scripts/verify-runtime-docs.mjs --verbose` → 10/10 checks passed, exit 0, 190ms wall-clock. |
| 5 | Harness has its own vitest unit test suite exercising parser + grep + link logic on fixtures | ✓ VERIFIED | `pnpm test:docs` → 37/37 tests pass, 253ms. Coverage includes slugify multi-dash preservation, JSONL parsing, drift-guard bash-fence scoping. |
| 6 | All 10 locked pitfalls from 18.1-RESEARCH.md are neutralized with explicit ⚠️ callouts | ✓ VERIFIED | Spot-checked: Pitfall 1 (submit→review) in agent-contract.md + getting-started.md; Pitfall 2 (7-entry allowlist) in agent-contract.md; Pitfall 3 (max_attempts filesystem-only) in recipes.md; Pitfall 4 (project_repo_map EXCLUSIVE) in admin-config.md + getting-started.md; Pitfall 5 (host.docker.internal) in agent-contract.md; Pitfall 6 (CONTAINER-01) in runner-daemon.md + admin-config.md; Pitfall 7 (MISSION_CONTROL_RECIPES_DIR) in recipes.md + admin-config.md; Pitfall 8 (secrets are ENV VAR NAMES) in recipes.md + admin-config.md; Pitfall 9 (90s stale window) in task-board-surfaces.md; Pitfall 10 (append-only) in agent-contract.md. Each located via grep with hit counts ≥1. |
| 7 | No legacy drift strings present anywhere in `docs/runtime/**.md` | ✓ VERIFIED | Harness `--check drift-guard` passes: zero hits for `indexed_error`, `submit → done`, `submit flips.*done`, `PUT /api/tasks/` (outside drift-guard callouts), or `http://localhost` inside bash fences of agent-contract.md. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docs/runtime/recipes.md` | ≥180 lines, DOC-REC operator-facing recipe authoring + indexing reference | ✓ VERIFIED | 266 lines. Contains `recipe-schema.ts`, `max_attempts`, `ENV VAR NAMES`, `error_message`, `MISSION_CONTROL_RECIPES_DIR`; no `indexed_error`. |
| `docs/runtime/runner-daemon.md` | ≥160 lines, DOC-RUN operator-facing runner daemon intro | ✓ VERIFIED | 184 lines. Links to `scripts/README.runner.md` 8 times; contains `CONTAINER-01` (2x), `--env-file` (3x), exit-code table. |
| `docs/runtime/admin-config.md` | ≥200 lines, DOC-CFG admin config reference | ✓ VERIFIED | 269 lines. Contains 8 `runtime.*` keys, `project_repo_map` + `EXCLUSIVE`, `MISSION_CONTROL_RECIPES_DIR`, `ENV VAR NAMES`, `CONTAINER-01`, `host.docker.internal`. |
| `docs/runtime/agent-contract.md` | ≥280 lines, DOC-AGT authoritative agent-side contract | ✓ VERIFIED | 439 lines. Contains `RUNNER_TOKEN_ALLOWLIST`, all 7 allowlist entries enumerated, `host.docker.internal`, `appendFileSync`, `submit → review`; zero `Claude Code` mentions (tool-agnostic per user-memory file). |
| `docs/runtime/task-board-surfaces.md` | ≥220 lines, DOC-UI six-surface reference | ✓ VERIFIED | 349 lines. All 6 surfaces documented (RecipeBadge, RunnerStatusBanner, Progress tab, RecipeCombobox, Advanced section, Recipes panel); contains `90` (stale window), `data-testid`, `RECIPE_LOCKED`/`isDispatched`; no `indexed_error`. |
| `docs/runtime/getting-started.md` | ≥200 lines, DOC-TUT end-to-end tutorial | ✓ VERIFIED | 272 lines. Contains `pnpm mc:build-hello-world`, `runtime.project_repo_map`, `node scripts/mc-runner.mjs`, `recipe_slug`, `review`; cross-links to all 5 Wave-1 reference docs. |
| `docs/runtime/INDEX.md` | ≥80 lines, entry point with Mermaid diagram + 6 sub-doc links | ✓ VERIFIED | 93 lines. Contains mermaid sequence diagram; cross-links getting-started, recipes, runner-daemon, agent-contract, admin-config, task-board-surfaces. |
| `scripts/verify-runtime-docs.mjs` | ≥250 lines, DOC-HARNESS cross-reference harness | ✓ VERIFIED | 908 lines. All 10 checks implemented (recipes, runner-env, agent-allowlist, agent-env, admin-config, ui-testids, tutorial-syntax, links, code-fences, drift-guard); runs in 190ms; exits 0. |
| `scripts/__tests__/verify-runtime-docs.test.mjs` | ≥80 lines, vitest unit tests for harness | ✓ VERIFIED | 416 lines, 37 tests, all pass in 253ms. |
| `README.md` | Documentation table + Ephemeral Agent Runtime feature block + panel-count fix | ✓ VERIFIED | 7 new runtime doc rows present; "Ephemeral Agent Runtime (v1.2)" feature block at line 227; `33+ panels` (no `32 panels`). |
| `docs/superpowers/specs/2026-04-18-recipe-agent-system-design.md` | Design-era banner added, content preserved | ✓ VERIFIED | DESIGN-ERA banner at line 7 + pointer to `docs/runtime/` at line 13; content preserved per SUMMARY self-checks. |
| `docs/cli-agent-control.md` | Runtime link-back section | ✓ VERIFIED | "See also — Runtime documentation" at line 204. |
| `package.json` | `docs:verify-runtime` + `test:docs` scripts | ✓ VERIFIED | Lines 23-24 contain both entries; JSON remains valid. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| recipes.md schema section | src/lib/recipe-schema.ts:37-71 | verbatim yaml/ts fence + source link | ✓ WIRED | Plan 01 SUMMARY reports `diff <(sed -n '37,71p' src/lib/recipe-schema.ts) <(extracted fence)` was empty (byte-exact match). |
| recipes.md max_attempts callout | src/lib/runner-claim.ts (resolveRecipeMaxAttempts) | prose + source link | ✓ WIRED | Recipes doc cites runner-claim.ts:347-381; harness `--check recipes` passes. |
| recipes.md secrets section | src/lib/recipe-schema.ts:54 (secrets array) | warning callout + annotated YAML example | ✓ WIRED | `ENV VAR NAMES` appears (grep hit); `recipe.secrets` documented as name-list with inline comment. |
| runner-daemon.md → scripts/README.runner.md | relative markdown links | ✓ WIRED | README.runner.md referenced 8 times in runner-daemon.md; each redirect block has a deep-link anchor. |
| admin-config.md project_repo_map | settings/route.ts settingDefinitions + Phase 14-08b LOCK | prose + EXCLUSIVE language + no env-var fallback | ✓ WIRED | `EXCLUSIVE` and `No env-var fallback` language present; 8 `runtime.*` keys all present in both doc and source (harness --check admin-config passes). |
| admin-config.md standalone | scripts/mc-runner.mjs:1047-1049 (getRecipesRoot) | env-var table + callout | ✓ WIRED | `MISSION_CONTROL_RECIPES_DIR` referenced; source line-range cited. |
| agent-contract.md submit section | src/app/api/runner/tasks/[task_id]/submit/route.ts + src/lib/task-dispatch.ts (runAegisReviews) | two-hop prose + cite-both strategy | ✓ WIRED | submit → review two-hop present; both flipper and reviewer cited; agent body `{"status":"done"}` literal shown but translated to `review` per Phase 17-01 RTEST-02. |
| agent-contract.md allowlist section | src/lib/runner-tokens.ts:18-27 (RUNNER_TOKEN_ALLOWLIST) | 7-row table + verbatim ts fence | ✓ WIRED | Source array has exactly 7 entries (confirmed via grep); doc enumerates all 7; harness `--check agent-allowlist` passes. |
| agent-contract.md env vars | src/lib/runner-claim.ts composeEnvMap + docker/hello-world-agent/README.md | table with source links | ✓ WIRED | All MC_* variables present; harness `--check agent-env` passes. |
| agent-contract.md append-only | Phase 14-07 seedMcDir + Phase 17-05 byte-assert | prose "append" language + appendFileSync examples | ✓ WIRED | `appendFileSync` cited; legacy `writeFileSync` shown only as ❌ wrong-pattern example. |
| task-board-surfaces.md banner section | runner-status-banner.tsx + /api/runtime/runner-status | prose + 90s stale window callout | ✓ WIRED | `90` and STALE_WINDOW_SECS referenced; ⚠️ callout present. |
| task-board-surfaces.md recipe-badge section | recipe-badge.tsx + model-tier-colors.ts | data-testid citation | ✓ WIRED | `data-testid="recipe-badge"` cited; source component confirmed to contain same testid (2 hits). |
| getting-started.md → Wave-1 reference docs | cross-link sentences | ✓ WIRED | All 5 Wave-1 docs + smoke-harness + hello-world reference linked; anchor slugs verified in SUMMARY. |
| README.md Documentation table → docs/runtime/** | 7 new table rows | ✓ WIRED | All 7 paths present at lines 184-190. |
| Design-spec banner → docs/runtime/ | ⚠️ admonition with pointer | ✓ WIRED | Banner at line 7 of design spec; pointer to `docs/runtime/` at line 13. |
| harness agent-allowlist check → runner-tokens.ts | regex count-equality | ✓ WIRED | Harness counts source array entries (7) == doc table rows (7); any divergence would fail. |
| harness runner-env check → mc-runner.mjs | env-name grep | ✓ WIRED | Doc env-var table entries all grep-confirmed in source via harness. |

### Requirements Coverage

7 phase-local pseudo-requirements defined in 18.1-RESEARCH.md § Phase Requirements (these are NOT in REQUIREMENTS.md — they are a local scope list per the research plan's explicit note). Every ID cross-referenced against the `requirements` frontmatter of all 7 PLAN.md files.

| Requirement | Description | Source Plan(s) | Status | Evidence |
|-------------|-------------|----------------|--------|----------|
| DOC-REC | Recipes — filesystem layout, recipe.yaml schema, indexer, model registry, admin resync | 18.1-01 (owner), 18.1-06 (INDEX surfaces) | ✓ SATISFIED | `docs/runtime/recipes.md` (266 lines) covers all five sub-topics; schema quoted byte-exact from recipe-schema.ts:37-71; 4 REST endpoints tabled; 3 seeded models tabled; error-row flow uses `error_message` (post-rename column name). |
| DOC-RUN | Runner daemon — CLI, env vars, boot sequence, lifecycle, logs, LaunchAgent, exit codes | 18.1-02 (owner), 18.1-06 (INDEX surfaces) | ✓ SATISFIED | `docs/runtime/runner-daemon.md` (184 lines) frames daemon + links to scripts/README.runner.md for deep content; 7-step boot sequence enumerated with source citations; env vars + exit codes tabled; CONTAINER-01 pitfall called out. |
| DOC-AGT | Agent contract — env vars, mount layout, preamble, SOUL.md, progress.md, checkpoints.jsonl, submit endpoint, exit codes | 18.1-03 (owner), 18.1-06 (INDEX surfaces) | ✓ SATISFIED | `docs/runtime/agent-contract.md` (439 lines) — most load-bearing doc in phase. All 7 RUNNER_TOKEN_ALLOWLIST entries enumerated verbatim from source; submit→review two-hop documented with both flipper + reviewer cited; append-only rule enforced in every progress/checkpoints example; CONTAINER-01 invariant documented; tool-agnostic (zero "Claude Code" hits). |
| DOC-CFG | Admin config — runtime.* settings, mount_allowlist, secrets store, gateway-optional, auth tiers | 18.1-02 (owner), 18.1-06 (INDEX surfaces) | ✓ SATISFIED | `docs/runtime/admin-config.md` (269 lines) enumerates all 8 `runtime.*` settings with per-key line-range citations; 4 pitfall callouts (#4, #6, #7, #8) rendered as ⚠️ blockquotes; auth-tier table includes negative-sentinel principal ids (-1000 runner, -2000 runner-token). |
| DOC-UI | Task-board operator surfaces — RecipeBadge, RunnerStatusBanner, Progress tab, RecipeCombobox, Advanced, Recipes panel | 18.1-04 (owner), 18.1-06 (INDEX surfaces) | ✓ SATISFIED | `docs/runtime/task-board-surfaces.md` (349 lines) covers all 6 Phase 16 UI surfaces with uniform 4-part layout; Pitfall 9 (90s stale-window) ⚠️ callout + operator resolution path; RECIPE_LOCKED gate formula + 5 isDispatched statuses enumerated; submit→review two-hop cross-linked from Progress tab. |
| DOC-TUT | Getting started with recipe agents — end-to-end walkthrough | 18.1-05 (owner), 18.1-06 (INDEX surfaces) | ✓ SATISFIED | `docs/runtime/getting-started.md` (272 lines) — 9 numbered steps from install → done-flip; three observation channels (UI/CLI/log tail); inline ⚠️ callouts for pitfall #1 (two-hop) and pitfall #4 (project_repo_map exclusive); terminal Troubleshooting table with 6 rows cross-linking to reference docs. Tutorial executability flagged in `human_verification` above. |
| DOC-HARNESS | Copy-paste verification harness — validates every documented command, env var, path, and endpoint | 18.1-07 (owner) | ✓ SATISFIED | `scripts/verify-runtime-docs.mjs` (908 lines) implements all 10 checks; 37 vitest unit tests; `pnpm docs:verify-runtime` exits 0 (10/10) in ~190ms. Harness surfaced 3 real drift hits on first run — all fixed inline per plan directive. Harness is the anti-drift mechanism the phase targeted. |

**Orphaned requirements:** None. Every pseudo-requirement from 18.1-RESEARCH.md § Phase Requirements was claimed by at least one plan. Plan 18.1-06 (INDEX + README) redundantly lists all six surface DOC-* IDs in its `requirements` frontmatter — this is intentional (the navigation layer is the entry-point surface for each) and matches the plan objective.

### Anti-Patterns Found

None. Targeted scans for drift markers all returned zero hits:

| Pattern | Severity | Location Scan | Result |
|---------|----------|---------------|--------|
| `indexed_error` (legacy column) | 🛑 Blocker | `docs/runtime/**.md` | 0 hits |
| `submit → done` / `submit to done` (legacy lifecycle) | 🛑 Blocker | `docs/runtime/**.md` | 0 hits (outside drift-guard callouts) |
| `PUT /api/tasks/` outside drift-guard callouts | 🛑 Blocker | `docs/runtime/agent-contract.md` | Present only in ❌ negative drift-guard examples; harness passes |
| `http://localhost` inside bash fences of agent-contract.md | 🛑 Blocker | bash fences only | 0 hits |
| `Claude Code` assumption in tool-agnostic contract | 🛑 Blocker | `docs/runtime/agent-contract.md` | 0 hits |
| `32 panels` (pre-Phase-16-06 drift) | ⚠️ Warning | `README.md` | 0 hits; now `33+ panels` |
| TODO/FIXME/placeholder in new runtime docs | ⚠️ Warning | `docs/runtime/**.md` | Not scanned exhaustively; no blockers surfaced in plan SUMMARYs |

### Harness Self-Check

Per plan 18.1-07's acceptance criteria, the harness was run against the completed doc set:

```
$ node scripts/verify-runtime-docs.mjs --verbose
[ok]   recipes
[ok]   runner-env
[ok]   agent-allowlist
[ok]   agent-env
[ok]   admin-config
[ok]   ui-testids
[ok]   tutorial-syntax
[ok]   links
[ok]   code-fences
[ok]   drift-guard

[verify-runtime-docs] ok — 10/10 checks passed
```

```
$ pnpm test:docs
Test Files  1 passed (1)
     Tests  37 passed (37)
  Duration  253ms
```

Wall-clock for harness: 190ms. Wall-clock for tests: 253ms. Both well within the <10s budget the research plan specified.

### Human Verification Required

Three observational items (none are blockers; all are confidence-building spot-checks):

1. **Tutorial end-to-end executability** — The shell-syntax harness check validates that commands are well-formed, but does not execute the Docker + curl + runner pipeline. A Docker-equipped operator following `docs/runtime/getting-started.md` from a fresh clone should reach `task.status=done` in roughly 10 minutes. `scripts/mc-runner-smoke.sh` is the scripted substitute and was scored 47/47 in the v1.2 milestone integration audit (strong prior evidence).

2. **Mermaid diagram rendering** — `docs/runtime/INDEX.md` contains a ```mermaid sequenceDiagram fence. GitHub renders this natively; the harness only confirms the fence carries the correct language hint. Low risk (small diagram, simple syntax) but worth a one-time render check when the doc is next viewed on GitHub.

3. **Source-of-truth citation line-range accuracy** — The harness confirms cited files exist and verifies existence of all claimed env vars / settings keys / allowlist entries. It does not spot-check that a citation like `src/lib/recipe-schema.ts:37-71` actually contains the claim being made on the line-range. Plan 01 SUMMARY reported a byte-exact diff confirmation for the recipe-schema.ts quote; other citations rely on the executing agent's discipline during authoring. A sample spot-check across 3-4 random citations would harden confidence.

### Gaps Summary

None. All 7 must-haves verified; all 7 phase-local pseudo-requirements satisfied; harness exits 0 against the completed doc set; no legacy drift strings anywhere in the runtime-docs surface. Phase 18.1 achieves its goal: v1.2 can archive with a complete operator manual, and the harness prevents future drift.

---

_Verified: 2026-04-21T18:45:00Z_
_Verifier: Claude (gsd-verifier)_
