---
phase: 18.1-v1-2-runtime-documentation
plan: 01
subsystem: documentation
tags: [recipes, runtime, docs, operator-reference, drift-guard]

# Dependency graph
requires:
  - phase: 12-recipe-system-v1-2
    provides: recipe filesystem layout, recipe.yaml Zod schema, indexer pipeline, recipe REST endpoints
  - phase: 14-runner-container-v1-2
    provides: resolveRecipeMaxAttempts claim-time resolver, filesystem-only max_attempts contract
  - phase: 18-v1-2-tech-debt-cleanup
    provides: error_message column rename (d42983a) — authoritative column name for recipe validation failures
provides:
  - docs/runtime/recipes.md — operator-facing recipe authoring + indexing reference (DOC-REC)
  - Pitfall #3 drift-guard (max_attempts is filesystem-only)
  - Pitfall #7 drift-guard (MISSION_CONTROL_RECIPES_DIR for standalone)
  - Pitfall #8 drift-guard (recipe.secrets is ENV VAR NAMES)
  - Recipe REST endpoint table (list/detail/search/resync) with auth tiers + source citations
affects:
  - 18.1-02 (runner-daemon.md + admin-config.md) — cross-links to MISSION_CONTROL_RECIPES_DIR
  - 18.1-03 (agent-contract.md) — cross-links to /recipe/PREAMBLE.md runner-authored warning
  - 18.1-04 (task-board-surfaces.md) — cross-links to Recipes panel Resync button
  - 18.1-06 (INDEX.md) — first sub-doc linked from runtime documentation index
  - 18.1-07 (verify-runtime-docs.mjs) — every source-of-truth citation in this doc will be validated by the harness

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pattern 1 (surface-scoped reference doc with top-of-file Map header)"
    - "Pattern 2 (4-column Field | Type | Default | Source table with relative-path source links)"
    - "Pattern 3 (paired prose + canonical code block — Zod schema quoted verbatim, 35 lines byte-matched against src/lib/recipe-schema.ts:37-71)"

key-files:
  created:
    - docs/runtime/recipes.md
  modified: []

key-decisions:
  - "Zod schema (recipe-schema.ts:37-71) quoted verbatim inside ```typescript fence — diff against source confirms byte-exact match. Paraphrase would invite drift."
  - "Drift-guard callout for max_attempts cites BOTH recipe-schema.ts:14-18 (contract declaration) AND runner-claim.ts:347-381 (resolveRecipeMaxAttempts implementation) — operators need both the rule and the code that enforces it."
  - "error_message drift-guard written WITHOUT the literal legacy column name as a string — the plan's verify block asserts `! grep -q 'indexed_error'` so the warning had to reference the rename via commit hash d42983a instead of quoting the stale name."
  - "Secrets pitfall illustrated with a fabricated ANTHROPIC_API_KEY/OPENAI_API_KEY pair in a YAML fence (canonical hello-world/recipe.yaml omits secrets entirely because it makes no model calls — quoting it alone would not teach operators what the list looks like populated)."
  - "Model registry table uses 200,000 context_window verbatim from model-registry.ts:47,55,61 — all three seeded models share it in v1.2, documented as a table column rather than inline prose."
  - "POST /api/recipes intentionally omitted from the operator-facing endpoint table (it exists in the code but the blessed authoring path is disk + git commit); cited as a footnote so operators know it exists without being steered toward it."

patterns-established:
  - "Pattern: Source-of-truth citations as relative markdown links of the form [path:LINE](../../path#LLINE) — docs/runtime/*.md resolves back to repo root via ../.. prefix, verifiable by a future link-checker harness"
  - "Pattern: Drift-guards live in blockquote callouts with ⚠️ prefix; cross-reference BOTH the shipped behavior and the historical drift (phase + commit hash) so readers know when the narrative changed"
  - "Pattern: Cross-links to sibling docs that don't exist yet are written as plain markdown links with a '(to be written in Plan 18.1-XX)' parenthetical — the verify harness in Plan 18.1-07 will later promote these from best-effort to hard assertions"

requirements-completed:
  - DOC-REC

# Metrics
duration: 3min
completed: 2026-04-21
---

# Phase 18.1 Plan 01: Recipes Operator Reference Summary

**Operator-facing reference doc for the Mission Control recipe surface — recipe.yaml Zod schema quoted verbatim, indexer pipeline (boot scan + chokidar + admin resync) documented, 4 REST endpoints tabled with auth tiers, and three drift-pitfall callouts (max_attempts filesystem-only, recipe.secrets = ENV VAR NAMES, MISSION_CONTROL_RECIPES_DIR for standalone) that future plans and the verify harness can grep for.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-21T14:59:45Z
- **Completed:** 2026-04-21T15:02:33Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Authored `docs/runtime/recipes.md` (266 lines) — first concrete doc of the new `docs/runtime/` subdirectory tree
- Quoted `src/lib/recipe-schema.ts:37-71` byte-exact (diff confirmed empty against source) so the schema section can never silently drift
- Tabled all 14 schema fields with type, default, and per-field source-of-truth links
- Documented the 3-path indexing pipeline (boot scan + chokidar watcher + POST /api/recipes/resync) and the error-row flow (`error_message` column, NOT the pre-rename legacy name)
- Tabled all 4 recipe REST endpoints (list, detail, search, resync) with method + path + auth tier + source file link
- Tabled the 3 seeded models from `src/lib/model-registry.ts` with full-id warning (Phase 14-10 lock)
- Shipped all three required drift-guard callouts (Pitfalls #3, #7, #8 from 18.1-RESEARCH.md §Pitfall Catalogue)
- Plan's full verify block (`wc -l ≥ 180` + 5 presence greps + 1 absence grep on `indexed_error`) passes

## Task Commits

Each task was committed atomically:

1. **Task 1: Author docs/runtime/recipes.md — schema + filesystem layout + indexer + pitfall guards** — `5aa5b78` (docs)

_Single-task plan; no additional commits. Plan metadata commit will be added at execution close._

## Files Created/Modified

- `docs/runtime/recipes.md` — **created** (266 lines). Operator-facing reference for the recipe system covering: filesystem layout, recipe.yaml Zod schema (verbatim quote + 14-field table), drift-pitfall callouts (max_attempts filesystem-only + recipe.secrets is ENV VAR NAMES), SOUL.md convention, canonical recipe.yaml example, indexing pipeline (boot scan + chokidar + admin resync + error-row flow), REST endpoint table (4 routes), model registry (3 seeded models), and common errors. Every factual claim carries a relative-path markdown link of the form `[path:LINE](../../path#LLINE)` that resolves back to the authoritative source file.

## Decisions Made

- **Zod schema quoted verbatim, not paraphrased.** The 35-line block at `docs/runtime/recipes.md` lines 55-89 is a byte-exact copy of `src/lib/recipe-schema.ts:37-71`. A `diff` run during execution confirmed zero deviation. Pattern 3 from 18.1-RESEARCH.md (paired prose + canonical code block) explicitly required this.
- **error_message drift-guard avoided quoting the legacy string.** The plan's verify block asserts `! grep -q "indexed_error"` (the legacy name must NOT appear anywhere in the file), so the drift-guard callout references the rename via Phase 18 Plan 04 commit hash `d42983a` rather than naming the old column. An earlier draft failed the absence check and was rewritten in-task.
- **Secrets example uses fabricated env var names, not the canonical hello-world recipe.** `recipes/hello-world/recipe.yaml` omits `secrets` entirely (it makes no model calls). A populated example with `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` was fabricated inside the pitfall callout so operators can see the list-of-names pattern populated, with an inline comment enforcing the "names not values" rule.
- **Cross-links to sibling docs that don't exist yet were written as plain markdown links with "(to be written in Plan 18.1-XX)" parentheticals.** Plan 18.1-07's verify-runtime-docs.mjs harness will later promote these from best-effort to hard assertions once all sibling docs ship.

## Deviations from Plan

None - plan executed exactly as written.

The single intra-task edit (rewording the `indexed_error` drift-guard callout to avoid the literal string) was a compliance fix for the plan's own verify block — the grep-absence assertion caught my first draft before commit, and the rewrite restored passing verification. This is plan-compliance, not a deviation.

## Issues Encountered

- **First draft of the error_message drift-guard quoted the legacy column name and failed the plan's `! grep -q "indexed_error"` verify assertion.** Rewrote the callout to reference the rename via commit hash `d42983a` and phase number, without spelling the legacy string. Verify block then passed cleanly. Fix required no file changes outside `docs/runtime/recipes.md`.

## User Setup Required

None — no external service configuration required. Documentation-only plan.

## Next Phase Readiness

**Plan 18.1-02 (runner-daemon.md + admin-config.md) is unblocked.**

- This doc contains two forward-links that Plan 18.1-02 is responsible for making good:
  1. The Pitfall #7 standalone-mode note cross-links to `admin-config.md` for the full standalone checklist.
  2. The secrets pitfall references runner-side resolution in `runner-daemon.md`.
- This doc contains one forward-link that Plan 18.1-03 is responsible for making good:
  1. The SOUL.md section cross-links to `agent-contract.md#preamble` for the runner-authored `/recipe/PREAMBLE.md` contract.
- This doc contains one forward-link that Plan 18.1-04 is responsible for making good:
  1. The indexing pipeline section cross-links to `task-board-surfaces.md` for the Recipes panel's Resync button.
- Plan 18.1-07 (verify-runtime-docs.mjs harness) is the ultimate backstop — it will grep-verify every source-of-truth citation in this file against the referenced source lines.

**No blockers.** All source-of-truth files cited in this doc exist and were read during authoring.

## Self-Check: PASSED

Verified on 2026-04-21T15:02:33Z:

- **File exists:** `docs/runtime/recipes.md` — FOUND (266 lines, exceeds plan's `min_lines: 180` threshold)
- **Commit exists:** `5aa5b78` — FOUND in `git log --oneline` (`docs(18.1-01): add docs/runtime/recipes.md operator reference`)
- **Plan verify block passes:** full chain `test -f ... && wc -l ... ≥ 180 && grep -q "recipe-schema.ts" && grep -q "max_attempts" && grep -q "ENV VAR NAMES" && grep -q "error_message" && grep -q "MISSION_CONTROL_RECIPES_DIR" && ! grep -q "indexed_error"` evaluates to success (ALL VERIFY CHECKS PASSED).
- **Schema verbatim:** `diff <(sed -n '37,71p' src/lib/recipe-schema.ts) <(extracted typescript fence)` — empty output (byte-exact match).
- **Scope discipline:** `git status --short` confirms only `docs/runtime/recipes.md` was added; no src/, schema/, migration/, or test changes in this commit. Pre-existing modifications to `.planning/STATE.md`, `scripts/mc-runner.mjs`, and `scripts/e2e-openclaw/start-e2e-server.mjs` visible in working tree are unrelated to this plan and remain unstaged.

---
*Phase: 18.1-v1-2-runtime-documentation*
*Completed: 2026-04-21*
