---
phase: 19-project-scoped-queue-plan-activation
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - openapi.json
  - scripts/mc-cli.cjs
  - scripts/mc-mcp-server.cjs
autonomous: true
requirements:
  - QUEUE-01
  - QUEUE-02
  - COMPAT-01

must_haves:
  truths:
    - "`openapi.json` documents the new `project_id`, `gsd_plan_id`, and `wave` query params on GET /api/tasks/queue."
    - "`openapi.json` documents the `queue_activation` response object on POST /api/gsd/plans/{plan_id}/transition with the full shape from Plan 19-02."
    - "`pnpm mc tasks queue` accepts `--project <id>`, `--plan <id>`, and `--wave <n>` flags, all optional; omitting them preserves v1.2 workspace-level behavior."
    - "The MCP tool `mc_poll_task_queue` accepts `project_id`, `gsd_plan_id`, AND `wave` input properties."
    - "Legacy MCP callers that omit the new `wave` param continue to function identically."
  artifacts:
    - path: "openapi.json"
      provides: "OpenAPI 3 spec entries for queue scoping params and transition queue_activation response"
      contains: "queue_activation"
    - path: "scripts/mc-cli.cjs"
      provides: "CLI `tasks queue --project --plan --wave` flag support"
      contains: "wave"
    - path: "scripts/mc-mcp-server.cjs"
      provides: "MCP `mc_poll_task_queue` tool schema with wave input"
      contains: "wave"
  key_links:
    - from: "scripts/mc-cli.cjs tasks queue handler"
      to: "GET /api/tasks/queue (query string builder)"
      via: "URLSearchParams composition with --project, --plan, --wave"
      pattern: "wave=.*encodeURIComponent|qs\\s*\\+=.*wave"
    - from: "scripts/mc-mcp-server.cjs mc_poll_task_queue tool"
      to: "GET /api/tasks/queue (fetch handler)"
      via: "inputSchema properties + handler body"
      pattern: "wave.*description|qs\\s*\\+=.*wave"
    - from: "openapi.json POST /api/gsd/plans/{plan_id}/transition response"
      to: "queue_activation schema"
      via: "inline schema under responses.200.content.application/json.schema.properties.queue_activation"
      pattern: "queue_activation.*properties.*activated"
---

<objective>
Propagate the Phase 19 queue-scoping + plan-activation contract to the three external surfaces that agents use to drive the system without raw REST: `openapi.json` (spec), `scripts/mc-cli.cjs` (CLI), and `scripts/mc-mcp-server.cjs` (MCP tools). Closes SC #5 of Phase 19 ("`openapi.json`, `scripts/mc-cli.cjs`, and `scripts/mc-mcp-server.cjs` reflect the new scoping params so CLI and MCP callers can drive the scoped queue without raw REST").

Purpose: Without this plan, the queue primitives land but agents can't discover or drive them, and the runtime drift harness (`verify-runtime-docs.mjs`) will flag the spec as stale.

Output: Updated openapi.json, CLI, and MCP tool schemas — all documenting `project_id`, `gsd_plan_id`, AND `wave` on queue polling, plus the `queue_activation` response shape on plan transition. Backward-compatible (all new params optional).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-project-scoped-queue-plan-activation/19-CONTEXT.md
@.planning/phases/19-project-scoped-queue-plan-activation/19-01-PLAN.md
@.planning/phases/19-project-scoped-queue-plan-activation/19-02-PLAN.md
@openapi.json
@scripts/mc-cli.cjs
@scripts/mc-mcp-server.cjs

<prior_plan_outputs>
**Plan 19-01 (parallel wave 1) finalizes the queue endpoint contract:**
- Query params: `agent`, `max_capacity`, `project_id`, `gsd_plan_id`, `wave` (all scoping params optional positive integers).
- 400 error: `Invalid <param>. Expected positive integer.` for each of the three scoping params.
- 400 error: cross-filter project/plan mismatch when both `project_id` and `gsd_plan_id` are present and the plan belongs to a different project.
- Response shape unchanged from v1.2 — the existing Task schema is still returned.

**Plan 19-02 (parallel wave 1) finalizes the transition endpoint contract:**
- On `to_status: in_progress` success: response body includes `queue_activation: { activated, already_active, skipped_by_state, reassigned, by_status: {inbox, assigned}, task_ids }`.
- On any other `to_status`: response body includes `queue_activation: null`.
- New event: `gsd.plan.tasks_activated` with the same `queue_activation` payload (for OpenAPI this is metadata, not a response — skip unless there's a pattern for documenting events in this spec).
</prior_plan_outputs>

<interfaces>
From scripts/mc-cli.cjs (already has project_id and gsd_plan_id support, MISSING wave):
```javascript
// Current flags in the `tasks queue` handler:
if (flags['max-capacity']) qs += `&max_capacity=${encodeURIComponent(String(flags['max-capacity']))}`
if (flags.project)         qs += `&project_id=${encodeURIComponent(String(flags.project))}`
if (flags['plan-id'])      qs += `&gsd_plan_id=${encodeURIComponent(String(flags['plan-id']))}`
// ADD:
// if (flags.wave)          qs += `&wave=${encodeURIComponent(String(flags.wave))}`
```

From scripts/mc-mcp-server.cjs (mc_poll_task_queue tool — already has project_id and gsd_plan_id, MISSING wave):
```javascript
inputSchema: {
  type: 'object',
  properties: {
    agent: { type: 'string', description: 'Agent name to poll for' },
    max_capacity: { type: 'number', description: 'Max tasks to return (default 1)' },
    project_id: { type: ['string', 'number'], description: 'Optional project scope for queue polling' },
    gsd_plan_id: { type: ['string', 'number'], description: 'Optional plan scope for queue polling' },
    // ADD: wave: { type: ['string', 'number'], description: 'Optional plan-wave scope for queue polling' },
  },
  required: ['agent'],
}
```

From openapi.json (relevant paths):
- Line ~8469: `/api/tasks/queue` GET — currently documents `agent`, `max_capacity`, `x-agent-name`. MISSING `project_id`, `gsd_plan_id`, `wave`.
- Line ~6698: `/api/gsd/plans/{plan_id}/transition` POST — MISSING `queue_activation` object in the 200 response schema.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wave flag to CLI + wave input property to MCP tool</name>
  <files>scripts/mc-cli.cjs, scripts/mc-mcp-server.cjs</files>
  <action>
**scripts/mc-cli.cjs** — locate the `tasks queue` handler (around line 670-677 where `flags.project` and `flags['plan-id']` are composed into the query string). Add one more conditional:
```javascript
if (flags.wave) qs += `&wave=${encodeURIComponent(String(flags.wave))}`
```
Add the `--wave <n>` flag to the help/usage block if usage text enumerates per-command flags (locate the `mc tasks queue --agent Aegis --max-capacity 2` example around line 91 and add an updated example showing `--project`, `--plan`, `--wave` together).

**scripts/mc-mcp-server.cjs** — locate the `mc_poll_task_queue` tool (around line 371). In `inputSchema.properties`, add:
```javascript
wave: { type: ['string', 'number'], description: 'Optional plan-wave scope for queue polling (matches gsd_plans.wave on the linked plan)' },
```
In the handler body, locate the existing `if (project_id …)` / `if (gsd_plan_id …)` blocks (around line 386-391) and add:
```javascript
if (wave !== undefined && wave !== null && String(wave).trim() !== '') {
  qs += `&wave=${encodeURIComponent(String(wave))}`;
}
```
Destructure `wave` in the handler signature alongside `project_id` and `gsd_plan_id`.

Do NOT add slug-resolving logic for `--project` or `--plan` — CONTEXT.md explicitly defers this. Raw IDs only.

Do NOT touch the other CLI commands or MCP tools — only `tasks queue` / `mc_poll_task_queue`.
  </action>
  <verify>
    <automated>node -e "const cli=require('./scripts/mc-cli.cjs'); console.log('cli loads');" &amp;&amp; node -e "const m=require('./scripts/mc-mcp-server.cjs'); console.log('mcp loads');"</automated>
  </verify>
  <done>
Both script files parse as valid CommonJS modules. `grep wave scripts/mc-cli.cjs` finds the new flag composition. `grep wave scripts/mc-mcp-server.cjs` finds the new property definition and handler branch. CLI help text (if per-command examples exist) shows the new flag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update openapi.json for queue scoping params and transition queue_activation response</name>
  <files>openapi.json</files>
  <action>
**Path `/api/tasks/queue` GET** — around line 8469 of `openapi.json`. In the `parameters` array (currently containing `agent`, `max_capacity`, `x-agent-name`), add three new entries:

```json
{
  "name": "project_id",
  "in": "query",
  "required": false,
  "schema": { "type": "integer", "minimum": 1 },
  "description": "Optional project scope — narrow queue polling to tasks linked to this project."
},
{
  "name": "gsd_plan_id",
  "in": "query",
  "required": false,
  "schema": { "type": "integer", "minimum": 1 },
  "description": "Optional plan scope — narrow queue polling to tasks linked to this plan. If combined with project_id, the plan's project_id must match or the request returns 400."
},
{
  "name": "wave",
  "in": "query",
  "required": false,
  "schema": { "type": "integer", "minimum": 1 },
  "description": "Optional plan-wave scope — narrow queue polling to tasks whose linked plan has gsd_plans.wave = this value."
}
```

Add a 400 response entry to the `responses` block (next to the existing 200) documenting:
```json
"400": {
  "description": "Invalid query params — invalid scope integer, or gsd_plan_id points at a plan whose project does not match the project_id filter.",
  "content": { "application/json": { "schema": { "type": "object", "properties": { "error": { "type": "string" } } } } }
}
```

**Path `/api/gsd/plans/{plan_id}/transition` POST** — around line 6698 of `openapi.json`. Locate the 200 response schema. In its `properties` block, add a `queue_activation` entry:

```json
"queue_activation": {
  "description": "Side effect of a to_status='in_progress' transition — summarizes which linked tasks were moved into the queue. NULL for any other to_status.",
  "oneOf": [
    {
      "type": "object",
      "required": ["activated", "already_active", "skipped_by_state", "reassigned", "by_status", "task_ids"],
      "properties": {
        "activated": { "type": "integer", "minimum": 0, "description": "Count of tasks newly moved from backlog/todo into inbox or assigned." },
        "already_active": { "type": "integer", "minimum": 0, "description": "Count of linked tasks that were already in inbox or assigned (no UPDATE fired)." },
        "skipped_by_state": { "type": "integer", "minimum": 0, "description": "Count of linked tasks in out-of-set states (review, in_progress, awaiting_owner, failed, etc.) — not activated." },
        "reassigned": { "type": "integer", "minimum": 0, "description": "Count of tasks whose dead assignee was cleared and the task was re-routed to inbox." },
        "by_status": {
          "type": "object",
          "required": ["inbox", "assigned"],
          "properties": {
            "inbox": { "type": "integer", "minimum": 0 },
            "assigned": { "type": "integer", "minimum": 0 }
          }
        },
        "task_ids": { "type": "array", "items": { "type": "integer" }, "description": "IDs of the newly-activated tasks. Length equals `activated`." }
      }
    },
    { "type": "null" }
  ]
}
```

Preserve JSON formatting conventions of the surrounding file (indentation, property order). Do NOT reformat the rest of the file. Validate the file is still valid JSON after editing — `node -e "JSON.parse(require('fs').readFileSync('openapi.json','utf8'))"`.

Do NOT document the `gsd.plan.tasks_activated` event in openapi.json unless the file already has an `x-events` or equivalent extension — OpenAPI 3.0 has no event channel concept. If the file DOES document events elsewhere, match that pattern; otherwise skip and note in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('openapi.json','utf8')); console.log('openapi valid JSON')" &amp;&amp; grep -q queue_activation openapi.json &amp;&amp; grep -q '"name": "wave"' openapi.json</automated>
  </verify>
  <done>
`openapi.json` parses as valid JSON. The `/api/tasks/queue` GET block documents `project_id`, `gsd_plan_id`, `wave` parameters plus a 400 response. The `/api/gsd/plans/{plan_id}/transition` POST 200 response schema includes a `queue_activation` property with the full six-field shape and `oneOf: [object, null]` — nullable handling for non-in_progress transitions.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` exits 0 (script files are CJS but the workspace typecheck must still pass).
- `node -e "JSON.parse(require('fs').readFileSync('openapi.json','utf8'))"` exits 0.
- `node scripts/mc-cli.cjs tasks queue --help` (or equivalent, if the CLI exposes per-command help) renders without throwing; the top-level help block shows `--wave` where applicable.
- `node scripts/mc-mcp-server.cjs` loads without throwing (`node -e "require('./scripts/mc-mcp-server.cjs')"`).
- Manual sanity: `pnpm mc tasks queue --agent test --project 1 --plan 1 --wave 1` composes a request with `project_id=1&gsd_plan_id=1&wave=1` in the query string (can verify by running against a local dev server or using `--dry-run` if CLI supports it; else confirm the string is built correctly by reading the handler output path).
- If `scripts/verify-runtime-docs.mjs` exists (it does per earlier phases), `node scripts/verify-runtime-docs.mjs` continues to exit 0.
</verification>

<success_criteria>
- OpenAPI spec documents the three new queue-scoping params and the queue_activation response block.
- CLI accepts `--wave <n>` on `tasks queue` and composes it into the REST call.
- MCP tool `mc_poll_task_queue` accepts `wave` in its inputSchema and forwards it on the underlying fetch.
- All three surfaces remain backward compatible — omitting the new params produces pre-v1.3 behavior.
- openapi.json is still valid JSON; no CLI/MCP script regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/19-project-scoped-queue-plan-activation/19-03-SUMMARY.md` — MUST document: whether events were added to openapi.json (per the extension-scheme check in Task 2), and any help-text changes to the CLI usage block.
</output>
