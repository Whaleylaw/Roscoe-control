---
phase: 14-runner-container-v1-2
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/api/runner/heartbeat/__tests__/route.test.ts
  - src/app/api/runner/ready-tasks/__tests__/route.test.ts
  - src/app/api/runner/claim/[task_id]/__tests__/route.test.ts
  - src/app/api/runner/pending-containers/__tests__/route.test.ts
  - src/app/api/runner/terminal-tasks/__tests__/route.test.ts
  - src/app/api/runner/tasks/[task_id]/runner-exit/__tests__/route.test.ts
  - src/lib/__tests__/runner-preamble.test.ts
  - src/lib/__tests__/runner-worktree-seed.test.ts
  - src/lib/__tests__/runner-docker-args.test.ts
  - src/lib/__tests__/runner-env-file.test.ts
  - src/lib/__tests__/runner-recipe-stage.test.ts
autonomous: true
requirements: [RUNNER-01, RUNNER-02, RUNNER-03, RUNNER-04, RUNNER-05, RUNNER-06, RUNNER-07, RUNNER-08, RUNNER-09, RUNNER-10, RUNNER-11, RUNNER-12, RUNNER-13, RUNNER-14, CONTAINER-01, CONTAINER-02, CONTAINER-03, WORK-01, WORK-02, WORK-04, WORK-05, WORK-06, WORK-07, MODEL-04]
locked_decisions:
  - "Wave 0 scaffolds are test-first shells: `it.todo` stubs naming each success-criterion, NOT real implementations. Wave 1/2 tasks MUST replace each .todo with a real test body."
  - "Route tests live in `__tests__/route.test.ts` next to the route they cover (follows the existing pattern in src/app/api/**/__tests__/)."
  - "Lib-level tests that are process-internal (preamble text, docker-arg composition, worktree seeding file layout) live in src/lib/__tests__/ — pure unit tests, no network or child_process."
must_haves:
  truths:
    - "Every Phase 14 route has a route.test.ts with it.todo stubs for each requirement"
    - "Every Phase 14 runner-internal pure-logic module has a __tests__/ suite with it.todo stubs"
    - "Running `pnpm test` reports N new `.todo` entries matching the requirement ID count — not ‘no tests found’"
  artifacts:
    - path: "src/app/api/runner/heartbeat/__tests__/route.test.ts"
      provides: "Test scaffold for POST /api/runner/heartbeat (Plan 14-04)"
      contains: "it.todo"
    - path: "src/app/api/runner/ready-tasks/__tests__/route.test.ts"
      provides: "Test scaffold for GET /api/runner/ready-tasks (Plan 14-04)"
      contains: "it.todo"
    - path: "src/app/api/runner/claim/[task_id]/__tests__/route.test.ts"
      provides: "Test scaffold for POST /api/runner/claim/:task_id (Plan 14-05)"
      contains: "it.todo"
    - path: "src/app/api/runner/pending-containers/__tests__/route.test.ts"
      provides: "Test scaffold for GET /api/runner/pending-containers (Plan 14-04)"
      contains: "it.todo"
    - path: "src/app/api/runner/terminal-tasks/__tests__/route.test.ts"
      provides: "Test scaffold for GET /api/runner/terminal-tasks (Plan 14-04)"
      contains: "it.todo"
    - path: "src/app/api/runner/tasks/[task_id]/runner-exit/__tests__/route.test.ts"
      provides: "Test scaffold for POST /api/runner/tasks/:task_id/runner-exit (Plan 14-06)"
      contains: "it.todo"
    - path: "src/lib/__tests__/runner-preamble.test.ts"
      provides: "Scaffold for preamble generator (first-attempt + resume variants)"
      contains: "it.todo"
    - path: "src/lib/__tests__/runner-worktree-seed.test.ts"
      provides: "Scaffold for .mc/ seeding"
      contains: "it.todo"
    - path: "src/lib/__tests__/runner-docker-args.test.ts"
      provides: "Scaffold for docker run argv composition"
      contains: "it.todo"
    - path: "src/lib/__tests__/runner-env-file.test.ts"
      provides: "Scaffold for env-file generation (no secrets on argv)"
      contains: "it.todo"
    - path: "src/lib/__tests__/runner-recipe-stage.test.ts"
      provides: "Scaffold for recipe-stage copy + PREAMBLE.md write"
      contains: "it.todo"
  key_links:
    - from: "it.todo stubs"
      to: "real test bodies in Wave 1/2"
      via: "replace-in-place"
      pattern: "it\\.todo\\("
---

<objective>
Create empty test scaffolds ahead of implementation so Wave 1/2 plans can replace `it.todo` stubs with real assertions in-place — a standard v1.2 Wave 0 pattern (Phase 11-00 / 12-00 / 13-00 precedent).

Purpose: Pre-committing the test shape locks the requirement→test mapping early, makes Wave 1/2 plans smaller (they only add code + test bodies, never test infrastructure), and ensures nothing in the Phase-14 requirement set lacks a test home.
Output: 11 new test files each containing import boilerplate, a top-level `describe(...)`, and one `it.todo(...)` per success criterion, sourced directly from the Research doc's Test Map table.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-runner-container-v1-2/14-CONTEXT.md
@.planning/phases/14-runner-container-v1-2/14-RESEARCH.md

<!-- Phase 11-00 / 12-00 / 13-00 Wave 0 scaffolds are the precedent -->
@src/app/api/tasks/[id]/__tests__/route.test.ts

<interfaces>
Scaffold template (per test file):

```typescript
import { describe, it } from 'vitest'

describe('POST /api/runner/heartbeat', () => {
  it.todo('RUNNER-05: accepts runner_id + ts, upserts runner_heartbeats row with last_heartbeat_at')
  it.todo('RUNNER-05: rejects requests missing Authorization: Bearer header with 401')
  it.todo('RUNNER-05: rejects bearer that is not the runner-secret with 401')
  it.todo('RUNNER-05: mutationLimiter rate-limits repeated heartbeats from the same IP')
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 route-test scaffolds under src/app/api/runner/**/__tests__/</name>
  <files>src/app/api/runner/heartbeat/__tests__/route.test.ts, src/app/api/runner/ready-tasks/__tests__/route.test.ts, src/app/api/runner/claim/[task_id]/__tests__/route.test.ts, src/app/api/runner/pending-containers/__tests__/route.test.ts, src/app/api/runner/terminal-tasks/__tests__/route.test.ts, src/app/api/runner/tasks/[task_id]/runner-exit/__tests__/route.test.ts</files>
  <action>
Create the six files listed in `files_modified`. Each file contains:

- ESM import of `describe, it` from `vitest`
- A single top-level `describe(<route-path>, () => { ... })`
- One `it.todo(...)` stub per row of the Research Doc's Test Map table for that route + requirement

Minimum stub counts:

**heartbeat/__tests__/route.test.ts** (4 stubs): RUNNER-05 happy path, missing bearer 401, wrong bearer 401, rate-limit on repeat

**ready-tasks/__tests__/route.test.ts** (4 stubs): RUNNER-04 returns assigned+recipe_slug+container_id IS NULL, excludes tasks already claimed, includes task model_override where present, rejects non-runner-secret bearer

**claim/[task_id]/__tests__/route.test.ts** (10 stubs): RUNNER-06 atomic claim happy path + 409 on double-claim + 409 on wrong status, RUNNER-07 mount allowlist escape rejection + skill allowlist rejection, RUNNER-08 global cap 409 + per-recipe cap 409, MODEL-04 env.MC_MODEL_PRIMARY resolves to task.model_override when set else recipe.model.primary, token issued with expiry = runner_started_at + timeout_seconds + 60, dispatch payload includes recipe body + task.is_resuming + task.prior_attempts

**pending-containers/__tests__/route.test.ts** (3 stubs): RUNNER-13 returns tasks with container_id NOT NULL and status in {assigned, in_progress}, excludes terminal tasks, rejects non-runner-secret bearer

**terminal-tasks/__tests__/route.test.ts** (3 stubs): WORK-07 returns tasks terminal since ?since=<iso8601>, omits non-terminal, rejects non-runner-secret bearer

**tasks/[task_id]/runner-exit/__tests__/route.test.ts** (6 stubs): RUNNER-11 increments runner_attempts is a noop (claim owns increment) + inserts task_runner_attempts row + status → assigned when attempts < cap, status → failed with runner_last_failure_reason when attempts ≥ cap, WORK-06 cap resolution task.runner_max_attempts ?? recipe.max_attempts ?? 3, clears container_id to allow reclaim, rejects non-runner-secret bearer

Each `it.todo` string MUST start with the requirement ID (e.g. "RUNNER-06:") so `pnpm test --reporter=verbose` prints them in order.

Do NOT add any implementation code; the route files themselves do not yet exist. Test files compile as pure Vitest scaffolds.
  </action>
  <verify>
    <automated>pnpm test src/app/api/runner -- --run --reporter=verbose 2>&1 | grep -cE "\.todo|skipped" && pnpm typecheck</automated>
  </verify>
  <done>All 6 files exist, each contains at least the minimum stub count, total ≥ 30 it.todo entries, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 lib-test scaffolds under src/lib/__tests__/</name>
  <files>src/lib/__tests__/runner-preamble.test.ts, src/lib/__tests__/runner-worktree-seed.test.ts, src/lib/__tests__/runner-docker-args.test.ts, src/lib/__tests__/runner-env-file.test.ts, src/lib/__tests__/runner-recipe-stage.test.ts</files>
  <action>
Create the five files listed. Stub counts:

**runner-preamble.test.ts** (6 stubs): WORK-05 first-attempt preamble contains "/workspace/.mc/progress.md" and "append a line", WORK-04 resume preamble contains "read .mc/task.json" and "git status" and "git log --oneline", both variants include MC_PREAMBLE_PATH forward-reference to POST /api/runner/checkpoint, first-attempt is 30-50 lines, resume is ≈45 lines, snapshot stable between runs

**runner-worktree-seed.test.ts** (7 stubs): WORK-01 seeds .mc/task.json + progress.md + checkpoints.jsonl + .gitignore on first attempt, WORK-02 task.json contains task_id+recipe_slug+attempt+is_resuming+prior_attempts, .gitignore contents equals "*\\n", resume attempt preserves existing progress.md and checkpoints.jsonl, resume attempt rewrites task.json with updated attempt number and new prior_attempts, mkdir is idempotent, file permissions are 0600 for task.json (contains token-free data but defensive)

**runner-docker-args.test.ts** (8 stubs): RUNNER-10 produces argv starting with ['run','--rm','-d'], CONTAINER-02 mount flags map worktree→/workspace:rw + recipe-stage→/recipe:ro + each read_only_mount→/refs/<label>/:ro + each extra_skill→/skills/<basename>:ro, labels mc.task_id + mc.recipe_slug + mc.attempt + mc.runner_id + mc.runner_started_at present, --name equals mc-task-<id>-a<attempt>, --add-host host.docker.internal:host-gateway present, --memory uses resolved limit (task.resource_override not implemented → recipe.memory_limit ?? runner-default 2g), --cpus same precedence with recipe.cpu_limit ?? 1.0, no --env flag contains MC_API_TOKEN value (CONTAINER-01 secrets-via-env-file)

**runner-env-file.test.ts** (5 stubs): CONTAINER-01 env-file contents include MC_API_URL + MC_TASK_ID + MC_API_TOKEN + MC_WORKSPACE + MC_RECIPE_PATH + MC_PREAMBLE_PATH + MC_MODEL_PRIMARY + MC_MODEL_PROVIDER + MC_MODEL_PARAMS_JSON, env-file permissions are 0600, env-file contains recipe-declared secrets from .data/runner/secrets/<NAME>, generated file path is deterministic per (task_id, attempt), env-file cleanup removes the file after container exit

**runner-recipe-stage.test.ts** (4 stubs): recipe-stage dir is under .data/runner/recipe-stage/task-<id>/, copy is deep (tools/ + skills/ + README.md + SOUL.md copied), PREAMBLE.md written AFTER copy so it doesn't get overwritten, recipe-stage dir path is OUTSIDE MISSION_CONTROL_RECIPES_DIR to avoid chokidar re-index (reference Pitfall 10)

All stubs prefix the message with the requirement ID when applicable.
  </action>
  <verify>
    <automated>pnpm test src/lib/__tests__/runner-preamble.test.ts src/lib/__tests__/runner-worktree-seed.test.ts src/lib/__tests__/runner-docker-args.test.ts src/lib/__tests__/runner-env-file.test.ts src/lib/__tests__/runner-recipe-stage.test.ts -- --run 2>&1 | grep -cE "\.todo|skipped"</automated>
  </verify>
  <done>All 5 files exist; total it.todo count across these 5 files ≥ 30; typecheck clean.</done>
</task>

</tasks>

<verification>
- `pnpm test src/app/api/runner src/lib/__tests__/runner-preamble.test.ts src/lib/__tests__/runner-worktree-seed.test.ts src/lib/__tests__/runner-docker-args.test.ts src/lib/__tests__/runner-env-file.test.ts src/lib/__tests__/runner-recipe-stage.test.ts -- --run` exits 0 (todos skip cleanly)
- `pnpm typecheck` exits 0
- Total new `.todo` count across all 11 files ≥ 60 (sum of 30 + 30 minimum stubs above)
</verification>

<success_criteria>
Wave 1 and Wave 2 executors arrive to 11 test files with the requirement→test mapping already written. Their job becomes "replace each it.todo with a real it() body + the matching production code" — no test-file creation needed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-runner-container-v1-2/14-03-SUMMARY.md` describing:
- 11 test files created
- Stub count per file (verified via grep)
- Requirement IDs covered vs. any that still need a home
- Which Wave 1/2 plan is expected to replace each group of stubs
</output>
