---
phase: 14-runner-container-v1-2
plan: 04
type: execute
wave: 1
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - src/app/api/runner/heartbeat/route.ts
  - src/app/api/runner/ready-tasks/route.ts
  - src/app/api/runner/pending-containers/route.ts
  - src/app/api/runner/terminal-tasks/route.ts
  - src/app/api/runner/heartbeat/__tests__/route.test.ts
  - src/app/api/runner/ready-tasks/__tests__/route.test.ts
  - src/app/api/runner/pending-containers/__tests__/route.test.ts
  - src/app/api/runner/terminal-tasks/__tests__/route.test.ts
autonomous: true
requirements: [RUNNER-04, RUNNER-05, RUNNER-13, WORK-07]
locked_decisions:
  - "All four endpoints are runner-secret-authenticated (id=-1000, operator role). NOT runner-token paths — these are daemon-facing."
  - "heartbeat endpoint uses mutationLimiter for rate-limit defense but MUST tolerate the 10s interval (300/min ceiling is fine; default limiter is typically 60/min — document if adjustment needed)."
  - "ready-tasks / pending-containers / terminal-tasks are GET endpoints, read-only, NO rate limiter (same precedent as other read endpoints)."
must_haves:
  truths:
    - "Runner POSTs heartbeat with runner_id → runner_heartbeats row is upserted atomically"
    - "Runner GETs /api/runner/ready-tasks → receives only tasks with status='assigned', recipe_slug IS NOT NULL, container_id IS NULL"
    - "Runner GETs /api/runner/pending-containers → receives tasks with container_id IS NOT NULL and status in ('assigned','in_progress')"
    - "Runner GETs /api/runner/terminal-tasks?since=<iso8601> → receives tasks transitioned to terminal status since that timestamp"
    - "Any request without valid runner-secret bearer is rejected 401/403"
  artifacts:
    - path: "src/app/api/runner/heartbeat/route.ts"
      provides: "POST handler persisting heartbeat"
      exports: ["POST"]
    - path: "src/app/api/runner/ready-tasks/route.ts"
      provides: "GET handler returning claimable tasks"
      exports: ["GET"]
    - path: "src/app/api/runner/pending-containers/route.ts"
      provides: "GET handler for reconciliation"
      exports: ["GET"]
    - path: "src/app/api/runner/terminal-tasks/route.ts"
      provides: "GET handler for GC driver"
      exports: ["GET"]
  key_links:
    - from: "requireRole(request,'operator') + user.id === -1000 guard"
      to: "runner-secret auth branch in src/lib/auth.ts:463"
      via: "bearer token resolution"
      pattern: "user\\.id === -1000"
    - from: "runner_heartbeats upsert"
      to: "migration 060 (Plan 14-01)"
      via: "INSERT ... ON CONFLICT(runner_id) DO UPDATE"
      pattern: "ON CONFLICT\\(runner_id\\)"
---

<objective>
Ship the four runner-secret-authenticated read + heartbeat endpoints the daemon needs: (1) POST /api/runner/heartbeat for 10s heartbeat persistence, (2) GET /api/runner/ready-tasks for 15s poll fallback, (3) GET /api/runner/pending-containers for post-crash reconciliation, (4) GET /api/runner/terminal-tasks for GC-tick cleanup discovery.

Purpose: These are the "small" runner endpoints — read-mostly, simple queries, no atomic claim semantics. Shipping all four in one plan keeps the heavier claim + runner-exit plans (14-05, 14-06) narrowly scoped to their complex concerns. Each endpoint covers one of Research's endpoint sketches and each has its test scaffold already in place from Plan 14-03.
Output: Four route files + four filled-in test files (replacing it.todo stubs with real bodies).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-runner-container-v1-2/14-CONTEXT.md
@.planning/phases/14-runner-container-v1-2/14-RESEARCH.md
@.planning/phases/14-runner-container-v1-2/14-01-SUMMARY.md
@.planning/phases/14-runner-container-v1-2/14-02-SUMMARY.md
@.planning/phases/14-runner-container-v1-2/14-03-SUMMARY.md

<!-- Auth precedent — runner-secret branch sets user.id = -1000 -->
@src/lib/auth.ts
<!-- Rate-limit precedent -->
@src/lib/rate-limit.ts
<!-- Heartbeat endpoint pattern (agents) — shape only, different semantics -->
@src/app/api/agents/[id]/heartbeat/route.ts

<interfaces>
Runner-secret auth guard (copy-paste for each of the 4 routes):

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { requireRole } from '@/lib/auth'
import { getDatabase } from '@/lib/db'

export async function POST(request: NextRequest) {
  const auth = requireRole(request, 'operator')
  if ('error' in auth) return NextResponse.json({ error: auth.error }, { status: auth.status })
  if (auth.user.id !== -1000) {
    return NextResponse.json({ error: 'runner-secret principal required' }, { status: 403 })
  }
  // ...
}
```

mutationLimiter invocation (heartbeat only):

```typescript
import { mutationLimiter } from '@/lib/rate-limit'
const limited = mutationLimiter(request)
if (limited) return limited
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /api/runner/heartbeat + its tests</name>
  <files>src/app/api/runner/heartbeat/route.ts, src/app/api/runner/heartbeat/__tests__/route.test.ts</files>
  <action>
**Route**:
- POST handler
- Auth: `requireRole('operator')` + `user.id === -1000` guard
- Rate limit: `mutationLimiter(request)` (defensive; if interval is too short under 10s heartbeat, raise a comment noting that a runner-specific limiter might be warranted in Phase 16 — do NOT implement here)
- Body validation (Zod inline or manual): `{ runner_id: string (1..64 chars), ts: number (unix ms), metadata?: Record<string, unknown> }`
- DB operation: UPSERT via `INSERT INTO runner_heartbeats (runner_id, last_heartbeat_at, registered_at, metadata_json) VALUES (?, ?, ?, ?) ON CONFLICT(runner_id) DO UPDATE SET last_heartbeat_at = excluded.last_heartbeat_at, metadata_json = excluded.metadata_json`
- `last_heartbeat_at` = unixepoch seconds (convert `ts` ms→s via Math.floor / 1000). `registered_at` = same on first insert, preserved via `ON CONFLICT DO UPDATE` NOT touching registered_at.
- Response: 204 No Content on success; 400 on invalid body; 401/403 via auth guard.

**Tests** (replace every it.todo with a real it body):
1. `RUNNER-05: accepts runner_id + ts, upserts runner_heartbeats row with last_heartbeat_at`
   - Seed settings/runner-secret, POST with valid body, expect 204, query runner_heartbeats → row present with matching last_heartbeat_at
2. `RUNNER-05: rejects requests missing Authorization: Bearer header with 401`
3. `RUNNER-05: rejects bearer that is not the runner-secret with 401 or 403` (session-cookie user hits 403 at the id-guard)
4. `RUNNER-05: mutationLimiter rate-limits repeated heartbeats from the same IP` — skip if the limiter is IP-keyed and can't be exercised from within jsdom, assert via the presence of the `mutationLimiter(request)` call instead (grep-ish assertion on the module).

Use the NextRequest helper pattern from other route tests in the repo. runner-secret can be obtained via `getRunnerSecret()` in setup.
  </action>
  <verify>
    <automated>pnpm test src/app/api/runner/heartbeat/__tests__/route.test.ts -- --run</automated>
  </verify>
  <done>Route returns 204 on valid heartbeat; runner_heartbeats row is upserted; 4 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/runner/ready-tasks + its tests</name>
  <files>src/app/api/runner/ready-tasks/route.ts, src/app/api/runner/ready-tasks/__tests__/route.test.ts</files>
  <action>
**Route**:
- GET handler, auth guard identical to Task 1
- Query: `SELECT id, recipe_slug, model_override, workspace_source, read_only_mounts, extra_skills, runner_max_attempts, runner_attempts FROM tasks WHERE status = 'assigned' AND recipe_slug IS NOT NULL AND container_id IS NULL ORDER BY id ASC LIMIT 50`
- Parse JSON columns (workspace_source / read_only_mounts / extra_skills) in the response shape — mirror existing mapTaskRow from src/app/api/tasks/route.ts for the narrow subset.
- Response: `{ tasks: Array<{id, recipe_slug, ...}> }` with 200 OK.

**Tests** (replace it.todo):
1. `RUNNER-04: returns assigned+recipe_slug+container_id IS NULL` — seed three tasks: assigned+recipe+no-container (returned), assigned+no-recipe (excluded), in_progress+recipe (excluded)
2. `RUNNER-04: excludes tasks already claimed` — seed task with container_id set → not in response
3. `RUNNER-04: includes task model_override where present` — task with model_override = 'haiku-4-5' → round-trips in response
4. `RUNNER-04: rejects non-runner-secret bearer` — session cookie user → 403
  </action>
  <verify>
    <automated>pnpm test src/app/api/runner/ready-tasks/__tests__/route.test.ts -- --run</automated>
  </verify>
  <done>All 4 tests pass; query returns only claimable tasks.</done>
</task>

<task type="auto">
  <name>Task 3: GET /api/runner/pending-containers and /api/runner/terminal-tasks + their tests</name>
  <files>src/app/api/runner/pending-containers/route.ts, src/app/api/runner/pending-containers/__tests__/route.test.ts, src/app/api/runner/terminal-tasks/route.ts, src/app/api/runner/terminal-tasks/__tests__/route.test.ts</files>
  <action>
**pending-containers/route.ts**:
- GET handler, auth guard identical
- Query: `SELECT id, recipe_slug, container_id, status, runner_started_at, runner_attempts FROM tasks WHERE container_id IS NOT NULL AND status IN ('assigned', 'in_progress') ORDER BY id ASC`
- Response: `{ tasks: [...] }`

pending-containers tests (3):
1. `RUNNER-13: returns tasks with container_id NOT NULL and status in (assigned, in_progress)` — seed 3 tasks mixing status
2. `RUNNER-13: excludes terminal tasks (done/failed/cancelled)` with container_id set → excluded
3. `RUNNER-13: rejects non-runner-secret bearer` → 403

**terminal-tasks/route.ts**:
- GET handler, auth guard identical
- Query param: `?since=<iso8601>` (required — 400 if missing or unparseable)
- Query: `SELECT id, status, updated_at FROM tasks WHERE status IN ('done', 'failed', 'cancelled') AND updated_at >= ? ORDER BY updated_at ASC LIMIT 200`
- Response: `{ tasks: Array<{ task_id: number, status: string, terminal_at: number }> }` — note: `task_id` alias for MC consistency with runner expectations in CONTEXT.md ("{task_id, status, terminal_at}")

terminal-tasks tests (3):
1. `WORK-07: returns tasks terminal since ?since=<iso8601>` — seed 4 tasks (2 terminal after cutoff, 1 terminal before, 1 non-terminal) → only 2 returned
2. `WORK-07: omits non-terminal tasks even if updated recently` — task with status='assigned' and updated_at > since → excluded
3. `WORK-07: rejects non-runner-secret bearer` → 403
4. `WORK-07: 400 when ?since= missing or not ISO 8601` — defensive guard

All four routes mirror the heartbeat auth pattern. Use the shared in-memory DB helper to keep tests fast.
  </action>
  <verify>
    <automated>pnpm test src/app/api/runner/pending-containers src/app/api/runner/terminal-tasks -- --run</automated>
  </verify>
  <done>Both routes respond correctly; 7 tests pass (3 + 4).</done>
</task>

</tasks>

<verification>
- `pnpm test src/app/api/runner/heartbeat src/app/api/runner/ready-tasks src/app/api/runner/pending-containers src/app/api/runner/terminal-tasks -- --run` → all suites pass (≥ 14 tests)
- `pnpm typecheck` clean
- `pnpm lint` clean
- Manual curl smoke (post-merge, optional): `curl -H "Authorization: Bearer <runner-secret>" http://localhost:3000/api/runner/ready-tasks` returns 200 JSON
</verification>

<success_criteria>
Four endpoints are live and daemon-ready. Plan 14-08 (runner daemon) can call all four without further server work.
</success_criteria>

<output>
After completion, create `.planning/phases/14-runner-container-v1-2/14-04-SUMMARY.md` describing:
- 4 routes + exact response shapes (so Plan 14-08 executor knows what to parse)
- Test count per route
- Any runtime gotchas encountered (e.g., limiter conflict with 10s heartbeat rhythm)
</output>
