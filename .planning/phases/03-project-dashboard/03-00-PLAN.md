---
phase: 03-project-dashboard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/project/__tests__/dashboard-view.test.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Test file exists with it.todo() stubs for all 7 DASH requirements"
    - "Test suite runs green (all stubs are pending, none fail)"
  artifacts:
    - path: "src/components/project/__tests__/dashboard-view.test.tsx"
      provides: "Test scaffolds for dashboard requirements DASH-01 through DASH-07"
      contains: "it.todo"
  key_links: []
---

<objective>
Create wave-0 test scaffolds for the project dashboard, following the it.todo() pattern established in Phases 1 and 2.

Purpose: Ensure the test file exists before implementation begins, so executors in later plans can progressively fill in tests.
Output: Test file with it.todo() stubs covering DASH-01 through DASH-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-dashboard/03-CONTEXT.md
@.planning/phases/03-project-dashboard/03-RESEARCH.md

@src/components/project/__tests__/project-context.test.tsx
@src/components/project/dashboard-view.tsx
@src/store/index.ts
</context>

<interfaces>
<!-- Key types the test file will reference -->

From src/store/index.ts:
```typescript
export interface Task {
  id: number
  title: string
  status: 'backlog' | 'inbox' | 'assigned' | 'awaiting_owner' | 'in_progress' | 'review' | 'quality_review' | 'done' | 'failed'
  project_id?: number
  // ... other fields
}

export interface Activity {
  id: number
  type: string
  entity_type: string
  entity_id: number
  actor: string
  description: string
  created_at: number
  entity?: { type: string; id?: number; title?: string; status?: string }
}

export interface Project {
  id: number
  name: string
  slug: string
  description?: string
  status: string
  // ... other fields
}
```

From src/components/project/project-context.tsx:
```typescript
export interface ProjectWorkspaceState {
  slug: string
  view: string
  project: Project | null
  loading: boolean
  error: string | null
}
export function useProjectWorkspace(): ProjectWorkspaceState
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard test scaffolds with it.todo() stubs</name>
  <files>src/components/project/__tests__/dashboard-view.test.tsx</files>
  <read_first>
    src/components/project/__tests__/project-context.test.tsx
    src/components/project/dashboard-view.tsx
    src/store/index.ts
  </read_first>
  <action>
Create `src/components/project/__tests__/dashboard-view.test.tsx` with it.todo() stubs covering all DASH requirements. Follow the pattern from Phase 1/2 where wave-0 uses it.todo() so the suite stays green.

Structure the file with:

1. A `describe('DashboardView')` block containing:
   - Mock setup comments for `useMissionControl`, `useProjectWorkspace`, `useTranslations`, and `fetch`
   - `describe('DASH-01: status overview cards')` with:
     - `it.todo('renders active task count card')`
     - `it.todo('renders blocked task count card')`
     - `it.todo('renders completed task count card')`
     - `it.todo('groups task statuses correctly: active = inbox+assigned+awaiting_owner+in_progress+review+quality_review')`
   - `describe('DASH-02: progress indicator')` with:
     - `it.todo('shows progress bar with correct width percentage')`
     - `it.todo('shows completion text like 6/8 tasks')`
   - `describe('DASH-03: project brief')` with:
     - `it.todo('renders project description as markdown')`
     - `it.todo('shows empty state when no description exists')`
   - `describe('DASH-04: activity feed')` with:
     - `it.todo('displays recent activity entries')`
     - `it.todo('shows relative timestamps')`
     - `it.todo('shows empty state when no activities')`
   - `describe('DASH-05: blocked tasks attention')` with:
     - `it.todo('blocked card has distinct warning styling when count > 0')`
     - `it.todo('blocked card has normal styling when count is 0')`
   - `describe('DASH-06: health indicator')` with:
     - `it.todo('shows On Track when 0 blocked tasks')`
     - `it.todo('shows At Risk when blocked < 25%')`
     - `it.todo('shows Off Track when blocked >= 25%')`
   - `describe('DASH-07: real-time updates')` with:
     - `it.todo('updates counts when store tasks change')`

Add necessary imports at the top: `import { describe, it } from 'vitest'`

Do NOT add any mock implementations yet -- just the structure with it.todo() stubs.
  </action>
  <verify>
    <automated>cd /Users/aaronwhaley/mission-control && pnpm vitest run src/components/project/__tests__/dashboard-view.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/project/__tests__/dashboard-view.test.tsx
    - Contains "DASH-01" in a describe block
    - Contains "DASH-02" in a describe block
    - Contains "DASH-03" in a describe block
    - Contains "DASH-04" in a describe block
    - Contains "DASH-05" in a describe block
    - Contains "DASH-06" in a describe block
    - Contains "DASH-07" in a describe block
    - Contains at least 15 it.todo() calls
    - Test suite runs without failures (all todos are pending)
  </acceptance_criteria>
  <done>Test file exists with it.todo() stubs for all 7 DASH requirements. Suite runs green with all tests pending.</done>
</task>

</tasks>

<verification>
pnpm vitest run src/components/project/__tests__/dashboard-view.test.tsx
All tests show as "todo" (pending), none fail.
</verification>

<success_criteria>
- Test scaffold file committed
- All DASH-01 through DASH-07 have at least one it.todo() stub
- Suite runs green
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-dashboard/03-00-SUMMARY.md`
</output>
