---
phase: 11-runtime-foundation-v1-2
verified: 2026-04-18T22:30:00Z
status: passed
score: 6/6 must-haves verified
must_haves:
  truths:
    - "Fresh DB and existing production DB both migrate cleanly to include recipes, task_runner_tokens, task_checkpoints tables plus 12 additive tasks columns"
    - "Developer can import getModel() from src/lib/model-registry.ts and look up Opus 4.7 / Sonnet 4.6 / Haiku 4.5; unknown identifiers return null"
    - "Creating a task with model_override set to an unknown model is rejected with a clear validation error referencing the registry"
    - "A request presenting .data/runner.secret authenticates as the runner principal and only resolves on /api/runner/* routes; other paths reject"
    - "A request presenting a valid per-task, per-attempt bearer token authenticates as runner-token; cross-task access blocked via requireRunnerToken (403); tokens SHA-256 hashed at rest with expiry runner_started_at + recipe.timeout_seconds + 60s"
    - "When a task reaches a terminal status the associated runner token row is marked revoked_at and subsequent presentations are rejected"
  artifacts:
    - path: "src/lib/model-registry.ts"
      provides: "Code-seeded immutable model registry with getModel/isKnownModel/MODEL_IDS exports"
    - path: "src/lib/runner-secret.ts"
      provides: "Auto-generation + read of .data/runner.secret with 0600 perms and >=32 byte entropy"
    - path: "src/lib/runner-tokens.ts"
      provides: "Token mint/verify/revoke/hash plus RAUTH-06 allowlist export"
    - path: "src/lib/auth.ts"
      provides: "User type widened with runner_token_task_id; runner-secret + runner-token branches in getUserFromRequest; requireRunnerToken wrapper distinguishes 401 vs 403"
    - path: "src/lib/migrations.ts"
      provides: "Migrations 054-057 add recipes, task_runner_tokens, task_checkpoints tables and 12 additive tasks columns"
    - path: "src/lib/validation.ts"
      provides: "createTaskSchema gains model_override field validated against isKnownModel registry check"
    - path: "src/app/api/tasks/[id]/route.ts"
      provides: "PATCH handler atomically calls revokeTokensForTask inside same db.transaction as terminal-status UPDATE"
    - path: "src/lib/db.ts"
      provides: "Boot wiring calls ensureRunnerSecret() peer to ensureAutoGeneratedCredentials()"
  key_links:
    - from: "src/lib/validation.ts"
      to: "src/lib/model-registry.ts"
      via: "imports isKnownModel + MODEL_IDS for createTaskSchema.model_override refinement"
    - from: "src/lib/auth.ts"
      to: "src/lib/runner-secret.ts"
      via: "imports getRunnerSecret and constant-time compares against bearer in /api/runner/* branch"
    - from: "src/lib/auth.ts"
      to: "src/lib/runner-tokens.ts"
      via: "imports verifyRunnerToken + RUNNER_TOKEN_ALLOWLIST + VerifiedRunnerToken; runner-token branch in getUserFromRequest and requireRunnerToken wrapper"
    - from: "src/app/api/tasks/[id]/route.ts"
      to: "src/lib/runner-tokens.ts"
      via: "imports revokeTokensForTask, calls inside db.transaction wrapping the terminal-status UPDATE"
    - from: "src/lib/db.ts"
      to: "src/lib/runner-secret.ts"
      via: "imports + invokes ensureRunnerSecret() during getDatabase() bootstrap"
---

# Phase 11: Runtime Foundation (v1.2) Verification Report

**Phase Goal:** The database schema, model registry, and auth principals that every later v1.2 phase depends on are in place — no runtime code yet, just the substrate
**Verified:** 2026-04-18T22:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Fresh + existing DBs migrate cleanly to add recipes, task_runner_tokens, task_checkpoints + 12 additive tasks columns | ✓ VERIFIED | Migrations 054-057 exist in `src/lib/migrations.ts` (L1559-1660). `migrations-v12-runtime.test.ts` (7 tests passing) proves: fresh-DB creation, FK CASCADE, partial index `WHERE revoked_at IS NULL`, all 12 additive nullable columns, NULL pre-existing rows except `runner_attempts=0`, idempotency, and forward-compat (drop tables + clear migration rows + re-run = clean rebuild) |
| 2 | `getModel()` returns typed metadata for Opus 4.7 / Sonnet 4.6 / Haiku 4.5; unknown returns null | ✓ VERIFIED | `src/lib/model-registry.ts` exports `MODELS`, `MODEL_IDS`, `getModel`, `isKnownModel`. All three identifiers seeded with exact-match strings (`claude-opus-4-7`, `claude-sonnet-4-6`, `claude-haiku-4-5-20251001`) and the locked metadata shape `{provider, context_window, output_tokens_max, supports_tools, supports_thinking}`. `getModel` returns `?? null` (no throw, no undefined). 14 tests passing. |
| 3 | Task creation with unknown `model_override` is rejected with a clear error referencing the registry | ✓ VERIFIED | `src/lib/validation.ts:54-62` adds `.refine(isKnownModel, { error: (issue) => "model_override '...' is not in the model registry. Known models: claude-opus-4-7, claude-sonnet-4-6, claude-haiku-4-5-20251001" })`. Both `createTaskSchema` and `updateTaskSchema` (inherited via `.partial()`) gated. 9 tests passing including substring assertions on "model registry" and known-id enumeration. |
| 4 | `.data/runner.secret` authenticates as `runner` principal only on `/api/runner/*`, other paths reject | ✓ VERIFIED | `src/lib/auth.ts:471-515` runner branch is gated `if (url.pathname.startsWith('/api/runner/'))`; `safeCompare` against bearer; on hit returns User `{id:-1000, username:'runner', role:'operator'}`. `auth-runner-principal.test.ts` (11 tests passing) covers: hit on `/api/runner/tasks/1/checkpoints`, X-API-Key header, reject on `/api/tasks`, `/api/users`, wrong secret, no auth, missing file. Audit log emits `{event_type:'runner_auth', detail: ...}` with no secret leak. |
| 5 | Runner-token authenticates per-task, cross-task blocked, SHA-256 hashed, expiry = started + timeout + 60 | ✓ VERIFIED | `src/lib/runner-tokens.ts` mints base64url(32 random bytes), persists `sha256(token)` hex via `INSERT INTO task_runner_tokens`, `expiresAt = runnerStartedAtUnix + timeoutSeconds + 60`. `verifyRunnerToken` enforces `revoked_at IS NULL` AND `expires_at > now`. `auth.ts:526-572` runner-token branch returns principal only on allowlist + path-id match; cross-task case falls through. **`requireRunnerToken(request, taskId)` wrapper (auth.ts:776-837) returns explicit `{status:403}` on cross-task** vs `{status:401}` on missing/invalid/expired/revoked/non-allowlisted. 29 token tests + 26 auth-runner-token tests passing — all 6 RAUTH-06 endpoints + 401-vs-403 discrimination explicitly asserted. |
| 6 | Terminal task status flips `revoked_at` and subsequent token presentations are rejected | ✓ VERIFIED | `src/app/api/tasks/[id]/route.ts:13` imports `revokeTokensForTask`; `:21` defines `TERMINAL_TASK_STATUSES = new Set(['done','failed','cancelled'])`; `:319-328` wraps the `UPDATE tasks` and `revokeTokensForTask(db, taskId)` inside a single `db.transaction(() => { ... })()` guarded by `isTerminalTransition`. `runner-token-revocation.test.ts` (7 tests passing) covers: terminal transition revokes both tokens, non-terminal does NOT revoke, cross-task tokens unaffected, atomicity (rollback rolls both halves back), terminal→terminal no double-revoke. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/model-registry.ts` | Code-seeded registry with `Model`, `getModel`, `isKnownModel`, `MODEL_IDS`, `MODELS` exports + 3 seeded Claude models | ✓ VERIFIED | All exports present (L20-93). Metadata shape exactly matches lock: `{provider, context_window, output_tokens_max, supports_tools, supports_thinking}`. `as const satisfies` keeps literal-key narrowing. No pricing / display_name / doc_url leaked. |
| `src/lib/runner-secret.ts` | Auto-gen + read helpers, 0600 perms, >=32 byte entropy, no secret in logs | ✓ VERIFIED | `randomBytes(32).toString('base64url')`, `rmSync` then `writeFileSync(..., {mode: 0o600})` to guarantee fresh-create perms (L100-106). `getRunnerSecret` validates decoded length >= 32 bytes. Log line `'Auto-generated runner secret (persisted to .data/runner.secret — mode 0600)'` does not include the value. Test asserts secret never appears in any logger.info call. |
| `src/lib/runner-tokens.ts` | mint / verify / revoke / hash + RAUTH-06 allowlist, no JWT, no parseable prefix, no background sweeper | ✓ VERIFIED | Exactly the 5 named exports + `VerifiedRunnerToken` type + `RUNNER_TOKEN_ALLOWLIST`. SHA-256 hex via `createHash('sha256').update(rawToken).digest('hex')`. Strict `<=` expiry rejection (token cannot be used AT expiry instant). No JWT, no prefix, no rotation helper. |
| `src/lib/auth.ts` | Runner-secret branch (Plan 02), runner-token branch (Plan 04), `requireRunnerToken` wrapper, User widened | ✓ VERIFIED | Imports lines 7-8. `User.runner_token_task_id?: number \| null` (L74-75). Both branches placed inside `if (url.pathname.startsWith('/api/runner/'))` gates. `requireRunnerToken` (L776-837) mirrors `requireRole`'s discriminated return shape. Cross-task case explicitly returns `{status:403}` (L811-813). |
| `src/lib/migrations.ts` | 4 new sequential migrations adding 3 tables + 12 additive tasks columns + indexes, no DROP, no DESTRUCTIVE, idempotent | ✓ VERIFIED | IDs 054-057 (the planned 036-039 were taken by earlier work; correct sequential append). Plan-spec drift (036→054) is benign — IDs are opaque tokens; only sequence + uniqueness matter. `hasTaskCol` idiom used per migration. Partial index `WHERE revoked_at IS NULL` present (L1610). FK CASCADE on both child tables (L1604, L1630). |
| `src/lib/validation.ts` | model_override field validated against isKnownModel | ✓ VERIFIED | Import L4. Field L54-62. Note: spec said `(val) => ({message: ...})` but Zod v4 removed that signature; executor switched to `{error: (issue) => ...}` form preserving all message requirements (mentions "model registry", echoes input, lists known IDs). Documented as auto-fix in 11-01-SUMMARY.md. |
| `src/app/api/tasks/[id]/route.ts` | PATCH handler atomically revokes runner tokens on terminal transition inside same transaction as status UPDATE | ✓ VERIFIED | Import L13. Const L21. Guard `isTerminalTransition` checks both new status terminal AND current not-already-terminal (L319-321). `db.transaction(() => { stmt.run(...); revokeTokensForTask(db, taskId); })()` (L323-328) — both ops in same atomic block; crash between them rolls BOTH back. |
| `src/lib/db.ts` | ensureRunnerSecret() invoked during boot peer to ensureAutoGeneratedCredentials() | ✓ VERIFIED | L10 imports `ensureRunnerSecret`; L25-28 in `getDatabase()` calls `ensureAutoGeneratedCredentials()` then `ensureRunnerSecret()`. Bootstrap chain matches the AUTH_SECRET / API_KEY UX pattern. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `src/lib/validation.ts` | `src/lib/model-registry.ts` | `import { isKnownModel, MODEL_IDS } from './model-registry'` + `.refine(isKnownModel, ...)` | ✓ WIRED | Verified at validation.ts:4 + L58 |
| `src/lib/auth.ts` | `src/lib/runner-secret.ts` | `import { getRunnerSecret }` + constant-time compare in /api/runner/* branch | ✓ WIRED | Verified at auth.ts:7 + L474-475 |
| `src/lib/auth.ts` | `src/lib/runner-tokens.ts` | imports verifyRunnerToken + RUNNER_TOKEN_ALLOWLIST + VerifiedRunnerToken; used in both branch and wrapper | ✓ WIRED | Verified at auth.ts:8, L530, L540, L786, L801 |
| `src/lib/auth.ts` | `src/lib/security-events.ts` | `logSecurityEvent({event_type:'runner_auth', ...})` without secret in detail | ✓ WIRED | Verified at auth.ts:477-486; tests assert detail does not contain secret value |
| `src/app/api/tasks/[id]/route.ts` | `src/lib/runner-tokens.ts` | `import { revokeTokensForTask }` + invocation inside same db.transaction as UPDATE | ✓ WIRED | Verified at route.ts:13 + L326 inside transaction L323-328 |
| `src/lib/db.ts` | `src/lib/runner-secret.ts` | `import { ensureRunnerSecret }` + `ensureRunnerSecret()` called in `getDatabase()` boot path | ✓ WIRED | Verified at db.ts:10 + L28 |
| `src/lib/runner-secret.ts` | `config.dataDir` | `path.join(config.dataDir, 'runner.secret')` for runtime path resolution | ✓ WIRED | Verified at runner-secret.ts:22-24 |
| `src/lib/runner-tokens.ts` | `task_runner_tokens` table | prepared INSERT/SELECT/UPDATE statements against task_id/attempt/token_hash/expires_at/revoked_at | ✓ WIRED | Verified at runner-tokens.ts:39-42, 73-78, 96-100 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| TCTX-07 | 11-03 | Task record tracks runner execution state via container_id, runner_started_at, runner_exit_code, worktree_path, runner_attempts, runner_max_attempts, runner_last_failure_reason | ✓ SATISFIED | Migration 057 adds all 7 columns; test asserts presence + nullability + `runner_attempts=0` default |
| RAUTH-01 | 11-02 | New `runner` principal in src/lib/auth.ts, authenticated by `.data/runner.secret`, scoped strictly to /api/runner/* | ✓ SATISFIED | auth.ts:471-515 path-prefix-gated branch returns runner User; tests prove rejection on /api/tasks, /api/users |
| RAUTH-02 | 11-04 | New `runner-token` principal; per-task, per-attempt; SHA-256 hashed; expires runner_started_at + recipe.timeout_seconds + 60s | ✓ SATISFIED | runner-tokens.ts: `expiresAt = runnerStartedAtUnix + timeoutSeconds + 60`; SHA-256 hex via createHash; auth.ts:526-572 returns runner-token User |
| RAUTH-03 | 11-04 | runner-token verifies path :id matches token's task_id; cross-task blocked | ✓ SATISFIED | requireRunnerToken (auth.ts:811-813) returns explicit `{status:403}` on `verified.task_id !== taskId`. Two cross-task 403 tests assert this distinction (NOT 401) |
| RAUTH-04 | 11-04 | Route handlers opt into runner-token auth explicitly (no rank-ordered escalation) | ✓ SATISFIED | requireRunnerToken does NOT fall back to session/api-key. getUserFromRequest cross-task case returns null explicitly. Wrapper documented as the single entry point |
| RAUTH-05 | 11-04 | When a task reaches terminal status, runner token revoked (revoked_at set) and unusable | ✓ SATISFIED | revokeTokensForTask called atomically inside the same db.transaction as the terminal-status UPDATE in tasks/[id]/route.ts. revocation test asserts subsequent verifyRunnerToken returns null |
| RAUTH-06 | 11-04 | runner-token call can hit only narrow set: checkpoints, submit, fail, scoped status, read own task, read own task comments | ✓ SATISFIED | RUNNER_TOKEN_ALLOWLIST has exactly 6 entries with locked regex patterns matching the spec verbatim. Tests assert non-allowlisted /claim, /ready-tasks, and DELETE method are rejected |
| MODEL-01 | 11-01 | New src/lib/model-registry.ts exports typed map seeded with Opus 4.7, Sonnet 4.6, Haiku 4.5 | ✓ SATISFIED | All three identifiers seeded with locked metadata shape; type narrowing via `as const satisfies Record<string, Model>`; `@ts-expect-error` test asserts compile-time literal-union guard |
| MODEL-03 | 11-01 | Task creation rejects model_override values not in registry with clear error | ✓ SATISFIED | createTaskSchema.model_override `.refine(isKnownModel, ...)` produces the exact error format documented in 11-01-SUMMARY.md, including "model registry" substring + known-id enumeration |

**Coverage:** 9/9 requirement IDs satisfied. No orphaned requirements (REQUIREMENTS.md L327-367 confirms only these 9 IDs map to Phase 11).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | No TODOs, FIXMEs, stubs, empty handlers, or placeholder returns in any of the 8 created/modified files | ℹ️ Info | Phase shipped clean substrate. Only stylistic flourishes (e.g., comment "where `:id` is the task_id placeholder") match the regex; not actual placeholders. |

### Test Suite Status

| Suite | Tests | Status |
|-------|-------|--------|
| `model-registry.test.ts` | 14 | ✓ All passing |
| `validation-model-override.test.ts` | 9 | ✓ All passing |
| `runner-secret.test.ts` | 11 | ✓ All passing |
| `auth-runner-principal.test.ts` | 11 | ✓ All passing |
| `migrations-v12-runtime.test.ts` | 7 | ✓ All passing |
| `runner-tokens.test.ts` | 29 | ✓ All passing |
| `auth-runner-token-principal.test.ts` | 26 | ✓ All passing |
| `runner-token-revocation.test.ts` | 7 | ✓ All passing |
| `auth.test.ts` (regression) | 13 | ✓ All passing — no regressions |
| **Total** | **127** | **✓ 127/127 passing** |

`pnpm typecheck` clean. Pre-existing typecheck error noted in `deferred-items.md` (Phase 10 origin, validation.ts:58 Zod v4 closure form) is documented and out-of-scope.

### Human Verification Required

No human verification required. All 6 success criteria are observable programmatically and asserted by the 127-test substrate suite. Phase 11 ships no UI surfaces, no real-time behavior, no external service integration — only DB schema + library modules + auth wiring. Future phases (14/15/16) that consume this substrate will need the typical UI / real-time / E2E human checks.

### Gaps Summary

None. The phase delivered exactly the substrate the goal called for:
- Schema additions are additive and idempotent (proven by the forward-compat test that drops + re-runs)
- Model registry is code-seeded, immutable, and properly typed with literal-union narrowing
- Both new auth principals are strictly path-scoped, with the `requireRunnerToken` wrapper concentrating the 401-vs-403 decision in one place per the locked spec
- Atomic revocation lives inside the same db.transaction as the terminal-status UPDATE, satisfying the "no sweeper / no lazy" lock from CONTEXT.md
- Boot wiring (db.ts) ensures `.data/runner.secret` exists before any /api/runner/* request can land

Two minor plan-spec drifts encountered during execution were correctly handled:
1. Migration IDs shifted from planned 036-039 → actual 054-057 because earlier phases consumed those IDs; sequential append preserved.
2. Zod refinement signature shifted from planned `(val) => ({message})` → actual `{error: (issue) => ...}` because Zod v4 removed the function-form signature; error message contract preserved.

Both drifts are documented in their respective SUMMARY files and have no observable impact on the goal.

---

*Verified: 2026-04-18T22:30:00Z*
*Verifier: Claude (gsd-verifier)*
