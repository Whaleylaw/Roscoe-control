---
phase: 08-projects-entry-point
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modals/project-manager-modal.tsx
  - src/components/modals/__tests__/project-manager-modal.test.tsx
  - messages/ar.json
  - messages/de.json
  - messages/en.json
  - messages/es.json
  - messages/fr.json
  - messages/ja.json
  - messages/ko.json
  - messages/pt.json
  - messages/ru.json
  - messages/zh.json
autonomous: true
gap_closure: true
requirements: [NAV-01]

must_haves:
  truths:
    - "The create-project form in ProjectManagerModal lets the user set github_repo, deadline, and color at creation time (not only after)"
    - "When github_repo is non-empty, a 'Enable GitHub sync + initialize labels' checkbox appears (default checked); when empty, the checkbox is hidden"
    - "On create, if github_repo is provided AND the sync checkbox is checked, after POST /api/projects succeeds the UI calls POST /api/github?action=init-labels and then PATCH /api/projects/{id} with {github_sync_enabled: 1, github_labels_initialized: 1}"
    - "init-labels failure does not block project creation — the project is still created and an inline warning is shown"
    - "github_repo format 'owner/repo' is validated client-side before POST is sent (non-empty input must match /^[^/]+\\/[^/]+$/)"
  artifacts:
    - path: "src/components/modals/project-manager-modal.tsx"
      provides: "Upgraded create form with github_repo, deadline, color, and github_sync_enabled fields plus init-labels chain on submit"
      contains: "github_repo"
    - path: "src/components/modals/__tests__/project-manager-modal.test.tsx"
      provides: "New test suite covering new fields, validation, init-labels chain, graceful failure, and POST payload shape"
      contains: "init-labels"
    - path: "messages/en.json"
      provides: "i18n keys for new create-form field labels and the enable-sync checkbox"
      contains: '"create"'
  key_links:
    - from: "project-manager-modal create form"
      to: "POST /api/projects"
      via: "fetch('/api/projects', { method: 'POST', body: JSON.stringify({ name, ticket_prefix, description, github_repo, deadline, color }) })"
      pattern: "github_repo.*deadline.*color|color.*deadline.*github_repo"
    - from: "project-manager-modal post-create flow"
      to: "POST /api/github?action=init-labels"
      via: "fetch('/api/github', { method: 'POST', body: JSON.stringify({ action: 'init-labels', repo }) }) followed by PATCH /api/projects/{id}"
      pattern: "action.*init-labels|init-labels"
---

<objective>
Close UAT Gap 2 (medium severity): upgrade the create-project form in ProjectManagerModal so users can specify github_repo, deadline, color, and opt into GitHub sync + label initialization at creation time — eliminating the current 3-step dance (create → edit inline for repo → GitHub Sync panel for init-labels + enable sync).

Purpose: The backend POST /api/projects already accepts github_repo, deadline, and color. GitHub integration is first-class in Mission Control (github-sync-engine.ts, github-sync-panel.tsx, init-labels endpoint at /api/github?action=init-labels). Forcing users through a multi-step flow to wire GitHub to a new project contradicts the single-source-of-truth promise behind reusing ProjectManagerModal from the Projects panel (Phase 8 D-12).

Output: An expanded create form with four new optional fields; a post-create chain that fires init-labels + PATCH when the user opts in; graceful handling of init-labels failure; atomic i18n across 10 locales; a new test file establishing `src/components/modals/__tests__/` as the canonical test directory for modals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-projects-entry-point/08-CONTEXT.md
@.planning/phases/08-projects-entry-point/08-HUMAN-UAT.md
@src/components/modals/project-manager-modal.tsx
@src/app/api/projects/route.ts
@src/app/api/github/route.ts
@src/lib/github-sync-engine.ts
@messages/en.json

<interfaces>
<!-- Current create form state (line 52 of project-manager-modal.tsx): -->
```tsx
const [form, setForm] = useState({ name: '', ticket_prefix: '', description: '' })
```

<!-- Current createProject POST body (lines 95-103) — ONLY sends name/ticket_prefix/description: -->
```tsx
body: JSON.stringify({
  name: form.name,
  ticket_prefix: form.ticket_prefix,
  description: form.description
})
```

<!-- Backend POST /api/projects ALREADY accepts these fields (src/app/api/projects/route.ts lines 96-98): -->
```ts
const githubRepo = typeof body?.github_repo === 'string' ? body.github_repo.trim() || null : null
const deadline = typeof body?.deadline === 'number' ? body.deadline : null
const color = typeof body?.color === 'string' ? body.color.trim() || null : null
```

<!-- Backend returns project object on 201 (line 128): -->
```ts
return NextResponse.json({ project }, { status: 201 })
// project has: id, workspace_id, name, slug, description, ticket_prefix, ticket_counter, status,
//              github_repo, deadline, color, github_sync_enabled, github_labels_initialized, github_default_branch, ...
```

<!-- init-labels endpoint (src/app/api/github/route.ts lines 427-447): -->
```ts
// POST /api/github with body { action: 'init-labels', repo: 'owner/repo' }
// Response: { ok: true, repo } on success
// Side effect: UPDATE projects SET github_labels_initialized = 1 WHERE github_repo = ?
//   (NOTE: backend already sets github_labels_initialized=1 — the PATCH below only needs to set github_sync_enabled)
```

<!-- COLOR_PALETTE already exported at module scope (lines 30-39) — reuse verbatim for the create form color picker (same UX as inline edit at lines 384-396): -->
```ts
const COLOR_PALETTE = ['#3b82f6', '#10b981', '#f59e0b', '#ef4444', '#8b5cf6', '#ec4899', '#06b6d4', '#f97316']
```

<!-- Existing projects i18n namespace (messages/en.json lines 2201-2216) — insertion point for new "create" sub-namespace. Match Phase 8 precedent (atomic Node script). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add projects.create.* i18n keys atomically across 10 locales + upgrade create form state and fields</name>
  <files>
    src/components/modals/project-manager-modal.tsx
    messages/ar.json
    messages/de.json
    messages/en.json
    messages/es.json
    messages/fr.json
    messages/ja.json
    messages/ko.json
    messages/pt.json
    messages/ru.json
    messages/zh.json
  </files>

  <read_first>
    - src/components/modals/project-manager-modal.tsx (ENTIRE file — 435 lines; this is the file being modified)
    - messages/en.json (lines 2201-2216 — existing projects namespace; find insertion point right before "empty")
    - messages/ar.json, messages/de.json, messages/es.json, messages/fr.json, messages/ja.json, messages/ko.json, messages/pt.json, messages/ru.json, messages/zh.json (find projects block in each)
    - .planning/phases/08-projects-entry-point/08-01-SUMMARY.md (atomic 10-locale i18n Node-script pattern — follow verbatim)
    - src/app/api/projects/route.ts (lines 90-105 — confirm backend accepts github_repo/deadline/color on POST)
  </read_first>

  <behavior>
    - After this task: the modal still creates a project when only name+ticket_prefix+description are provided (regression-safe).
    - After this task: users can type "owner/repo" in github_repo, pick a date for deadline, pick a color swatch, and toggle a github_sync_enabled checkbox that only appears when github_repo is non-empty.
    - NOTE: the init-labels chain is wired in Task 2. This task adds fields + payload; Task 2 adds the chain and its tests.
  </behavior>

  <action>
    STEP 1 — Add projects.create.* keys atomically across all 10 locales.

    Use a one-shot Node script (Phase 8 precedent). The new sub-namespace goes INSIDE the existing "projects" object, positioned between "title" and "empty" (or between "header" and "empty" if 08-04 lands first — accept either position, the script should detect and insert before "empty"). Keys to add:

    ```json
    "create": {
      "githubRepoLabel": "GitHub repo",
      "githubRepoPlaceholder": "owner/repo",
      "githubRepoHelp": "Optional. Link a GitHub repository to this project.",
      "githubRepoInvalid": "Use owner/repo format",
      "deadlineLabel": "Deadline",
      "colorLabel": "Color",
      "enableSyncLabel": "Enable GitHub sync + initialize labels",
      "enableSyncHelp": "Creates Mission Control labels on the repo and turns on two-way sync",
      "initLabelsFailedWarning": "Project created, but GitHub label setup failed. You can retry from the GitHub Sync panel."
    }
    ```

    Translations per locale (use these EXACT values):

    - ar: githubRepoLabel: "مستودع GitHub", githubRepoPlaceholder: "owner/repo", githubRepoHelp: "اختياري. اربط مستودع GitHub بهذا المشروع.", githubRepoInvalid: "استخدم تنسيق owner/repo", deadlineLabel: "الموعد النهائي", colorLabel: "اللون", enableSyncLabel: "تمكين مزامنة GitHub + تهيئة العلامات", enableSyncHelp: "ينشئ علامات Mission Control على المستودع ويُفعّل المزامنة الثنائية", initLabelsFailedWarning: "تم إنشاء المشروع، لكن فشل إعداد علامات GitHub. يمكنك المحاولة مجددًا من لوحة مزامنة GitHub."
    - de: "GitHub-Repo", "owner/repo", "Optional. Verknüpfen Sie ein GitHub-Repository mit diesem Projekt.", "Format: owner/repo", "Frist", "Farbe", "GitHub-Sync aktivieren und Labels initialisieren", "Erstellt Mission-Control-Labels im Repo und aktiviert Zwei-Wege-Sync", "Projekt erstellt, aber GitHub-Label-Setup fehlgeschlagen. Sie können es im GitHub-Sync-Panel erneut versuchen."
    - en: (values above)
    - es: "Repositorio GitHub", "owner/repo", "Opcional. Vincula un repositorio GitHub a este proyecto.", "Usa el formato owner/repo", "Fecha límite", "Color", "Activar sincronización GitHub + inicializar etiquetas", "Crea etiquetas Mission Control en el repo y activa la sincronización bidireccional", "Proyecto creado, pero la configuración de etiquetas GitHub falló. Puedes reintentar desde el panel de GitHub Sync."
    - fr: "Dépôt GitHub", "owner/repo", "Optionnel. Liez un dépôt GitHub à ce projet.", "Utilisez le format owner/repo", "Échéance", "Couleur", "Activer la synchronisation GitHub + initialiser les libellés", "Crée les libellés Mission Control sur le dépôt et active la synchronisation bidirectionnelle", "Projet créé, mais l'installation des libellés GitHub a échoué. Vous pouvez réessayer depuis le panneau GitHub Sync."
    - ja: "GitHub リポジトリ", "owner/repo", "任意。GitHub リポジトリをこのプロジェクトにリンクします。", "owner/repo 形式で入力してください", "期限", "色", "GitHub 同期を有効にしてラベルを初期化", "リポジトリに Mission Control ラベルを作成し、双方向同期を有効にします", "プロジェクトは作成されましたが、GitHub ラベルの設定に失敗しました。GitHub Sync パネルから再試行できます。"
    - ko: "GitHub 저장소", "owner/repo", "선택 사항입니다. GitHub 저장소를 이 프로젝트에 연결합니다.", "owner/repo 형식을 사용하세요", "마감일", "색상", "GitHub 동기화 활성화 및 레이블 초기화", "저장소에 Mission Control 레이블을 생성하고 양방향 동기화를 활성화합니다", "프로젝트는 생성되었지만 GitHub 레이블 설정에 실패했습니다. GitHub Sync 패널에서 다시 시도할 수 있습니다."
    - pt: "Repositório GitHub", "owner/repo", "Opcional. Vincule um repositório GitHub a este projeto.", "Use o formato owner/repo", "Prazo", "Cor", "Ativar sincronização GitHub + inicializar etiquetas", "Cria etiquetas do Mission Control no repositório e ativa a sincronização bidirecional", "Projeto criado, mas a configuração de etiquetas do GitHub falhou. Você pode tentar novamente no painel GitHub Sync."
    - ru: "Репозиторий GitHub", "owner/repo", "Необязательно. Свяжите репозиторий GitHub с этим проектом.", "Используйте формат owner/repo", "Крайний срок", "Цвет", "Включить синхронизацию GitHub и инициализировать метки", "Создаёт метки Mission Control в репозитории и включает двустороннюю синхронизацию", "Проект создан, но настройка меток GitHub не удалась. Повторите попытку из панели GitHub Sync."
    - zh: "GitHub 仓库", "owner/repo", "可选。将 GitHub 仓库关联到此项目。", "使用 owner/repo 格式", "截止日期", "颜色", "启用 GitHub 同步并初始化标签", "在仓库上创建 Mission Control 标签并启用双向同步", "项目已创建，但 GitHub 标签设置失败。您可以在 GitHub Sync 面板中重试。"

    Script pattern (one-shot via `node -e`):
    ```js
    const fs = require('fs');
    const data = { /* per-locale object with the 9 keys above */ };
    for (const [l, keys] of Object.entries(data)) {
      const path = `messages/${l}.json`;
      const obj = JSON.parse(fs.readFileSync(path, 'utf8'));
      const p = obj.projects;
      const { title, header, empty, picker, row, ...rest } = p;
      const reordered = { title, ...(header ? { header } : {}), create: keys, empty, picker, row, ...rest };
      obj.projects = reordered;
      fs.writeFileSync(path, JSON.stringify(obj, null, 2) + '\n');
    }
    ```

    STEP 2 — Expand create-form state in src/components/modals/project-manager-modal.tsx.

    Replace line 52 with an expanded state shape:

    ```tsx
    const [form, setForm] = useState({
      name: '',
      ticket_prefix: '',
      description: '',
      github_repo: '',
      deadline: '', // yyyy-mm-dd from <input type="date">
      color: '',
      github_sync_enabled: true, // default-checked; only matters when github_repo is non-empty
    })
    const [githubRepoError, setGithubRepoError] = useState<string | null>(null)
    const [initLabelsWarning, setInitLabelsWarning] = useState<string | null>(null)
    ```

    Update the existing setForm reset at line 106 (inside createProject) to reset all new fields:
    ```tsx
    setForm({ name: '', ticket_prefix: '', description: '', github_repo: '', deadline: '', color: '', github_sync_enabled: true })
    setGithubRepoError(null)
    ```

    STEP 3 — Render new form fields inside the existing `<form onSubmit={createProject}>` block (lines 227-255).

    Add these fields AFTER the existing textarea (line 248-254) and BEFORE the closing `</form>` tag. Exact JSX:

    ```tsx
    <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
      <div>
        <label className="block text-xs text-muted-foreground mb-1">{t('create.githubRepoLabel')}</label>
        <input
          type="text"
          value={form.github_repo}
          onChange={(e) => {
            const v = e.target.value
            setForm((prev) => ({ ...prev, github_repo: v }))
            if (v && !/^[^/]+\/[^/]+$/.test(v.trim())) {
              setGithubRepoError(t('create.githubRepoInvalid'))
            } else {
              setGithubRepoError(null)
            }
          }}
          placeholder={t('create.githubRepoPlaceholder')}
          className="w-full bg-surface-1 text-foreground border border-border rounded-md px-3 py-2 text-sm"
        />
        <p className="text-[11px] text-muted-foreground/70 mt-1">{t('create.githubRepoHelp')}</p>
        {githubRepoError && <p className="text-[11px] text-red-400 mt-1">{githubRepoError}</p>}
      </div>
      <div>
        <label className="block text-xs text-muted-foreground mb-1">{t('create.deadlineLabel')}</label>
        <input
          type="date"
          value={form.deadline}
          onChange={(e) => setForm((prev) => ({ ...prev, deadline: e.target.value }))}
          className="w-full bg-surface-1 text-foreground border border-border rounded-md px-3 py-2 text-sm"
        />
      </div>
    </div>
    <div>
      <label className="block text-xs text-muted-foreground mb-1">{t('create.colorLabel')}</label>
      <div className="flex gap-1.5 items-center flex-wrap">
        {COLOR_PALETTE.map(c => (
          <button
            key={c}
            type="button"
            onClick={() => setForm((prev) => ({ ...prev, color: prev.color === c ? '' : c }))}
            className={`w-6 h-6 rounded-full border-2 transition-smooth ${form.color === c ? 'border-foreground scale-110' : 'border-transparent hover:border-border'}`}
            style={{ backgroundColor: c }}
            aria-label={c}
          />
        ))}
      </div>
    </div>
    {form.github_repo.trim() && !githubRepoError && (
      <div className="flex items-start gap-2">
        <input
          id="mc-create-sync"
          type="checkbox"
          checked={form.github_sync_enabled}
          onChange={(e) => setForm((prev) => ({ ...prev, github_sync_enabled: e.target.checked }))}
          className="mt-0.5"
        />
        <div className="text-xs">
          <label htmlFor="mc-create-sync" className="text-foreground cursor-pointer">{t('create.enableSyncLabel')}</label>
          <p className="text-muted-foreground/70">{t('create.enableSyncHelp')}</p>
        </div>
      </div>
    )}
    {initLabelsWarning && (
      <div className="text-xs text-amber-400 bg-amber-500/10 border border-amber-500/20 rounded p-2">
        {initLabelsWarning}
      </div>
    )}
    ```

    Add `useTranslations` import at top of file (currently not imported — the modal hardcodes English strings like "Project Management"). Add:
    ```tsx
    import { useTranslations } from 'next-intl'
    ```
    and inside the component, right after `const { ... } = useState(...)` block:
    ```tsx
    const t = useTranslations('projects')
    ```
    Leave all EXISTING hardcoded English strings (lines 221, 233, 241, 245, 258, 281, etc.) UNCHANGED — this task scope is the create form only; translating the full modal is out of scope (belongs to a future i18n hardening phase). Only the NEW strings introduced here use t('create.*').

    STEP 4 — Update the POST /api/projects body in createProject (lines 95-103).

    Replace the body with:
    ```tsx
    body: JSON.stringify({
      name: form.name,
      ticket_prefix: form.ticket_prefix,
      description: form.description,
      github_repo: form.github_repo.trim() || undefined,
      deadline: form.deadline ? Math.floor(new Date(form.deadline).getTime() / 1000) : undefined,
      color: form.color || undefined,
    })
    ```

    Also add a pre-submit guard: if `form.github_repo.trim()` is non-empty and fails the regex `/^[^/]+\/[^/]+$/`, do NOT POST — set githubRepoError and return early. Add at the top of createProject right after the existing `if (!form.name.trim()) return`:

    ```tsx
    const repo = form.github_repo.trim()
    if (repo && !/^[^/]+\/[^/]+$/.test(repo)) {
      setGithubRepoError(t('create.githubRepoInvalid'))
      return
    }
    ```

    STEP 5 — Save the created project id + repo + sync-enabled intent to module-level refs or local const for Task 2 to consume. Task 2 will add the init-labels chain. For now, keep createProject returning normally on 201.

    Actually: keep this task's scope to fields + payload + validation. Task 2 adds the post-create chain and tests for the full modal behavior.
  </action>

  <verify>
    <automated>pnpm typecheck &amp;&amp; grep -l '"create"' messages/*.json | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep -l '"create"' messages/*.json | wc -l` outputs `10`
    - `grep -c '"githubRepoLabel"' messages/en.json` outputs `1`
    - `grep -c '"initLabelsFailedWarning"' messages/en.json` outputs `1`
    - `grep -n "github_repo:" src/components/modals/project-manager-modal.tsx` shows the field in the POST body (inside createProject)
    - `grep -n "deadline: form.deadline" src/components/modals/project-manager-modal.tsx` finds the deadline unix-seconds conversion
    - `grep -n "COLOR_PALETTE" src/components/modals/project-manager-modal.tsx` shows the palette used in BOTH the create form AND the inline edit form (two matches after this task)
    - `grep -n "useTranslations" src/components/modals/project-manager-modal.tsx` finds the import
    - `grep -n "t('create.githubRepoLabel')" src/components/modals/project-manager-modal.tsx` finds exactly one match
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>

  <done>
    Create form in ProjectManagerModal collects github_repo/deadline/color/github_sync_enabled. github_repo format is validated. POST body includes all new fields. i18n keys exist in all 10 locales.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire init-labels + PATCH chain on create + graceful failure + new unit test suite</name>
  <files>
    src/components/modals/project-manager-modal.tsx
    src/components/modals/__tests__/project-manager-modal.test.tsx
  </files>

  <read_first>
    - src/components/modals/project-manager-modal.tsx (AFTER Task 1 changes — read the updated createProject function)
    - src/app/api/github/route.ts (lines 427-447 — init-labels handler; note it already sets github_labels_initialized=1 on the backend, so the client PATCH only needs to set github_sync_enabled=1)
    - src/app/api/projects/[id]/route.ts (if exists — to confirm PATCH accepts github_sync_enabled)
    - src/components/panels/__tests__/projects-panel.test.tsx (reference pattern for vi.mock of next-intl, useMissionControl, and component mocks — mirror this structure)
    - src/components/panels/__tests__/create-task-modal-open-workspace.test.tsx (modal test pattern from Phase 8 plan 08-02)
  </read_first>

  <behavior>
    - Test 1: "renders new fields" — mount the modal; assert inputs for github_repo, deadline, and color-swatch buttons are present.
    - Test 2: "sync checkbox hidden when github_repo is empty" — mount modal; assert no checkbox with label /enableSyncLabel/i is rendered. Type "owner/repo" into github_repo input; assert the checkbox now renders and is checked by default.
    - Test 3: "sync checkbox hidden when github_repo is invalid" — type "bad-format" (no slash); assert error message renders AND sync checkbox is NOT rendered.
    - Test 4: "invalid github_repo blocks submit" — type name + "invalid-repo", click submit; assert no fetch('/api/projects') call occurs AND error message is shown.
    - Test 5: "POST payload includes all new fields" — fill name, ticket_prefix, github_repo ("octocat/hello"), deadline ("2026-12-31"), color (pick first palette entry); stub fetch to return 201 {project: {id: 42, github_repo: 'octocat/hello', ...}}; click submit; assert the POST body (JSON-parsed) contains name, ticket_prefix, description, github_repo: 'octocat/hello', deadline: (unix seconds for 2026-12-31), color: '#3b82f6'.
    - Test 6: "init-labels + PATCH chain fires when sync enabled and repo valid" — same as Test 5, with sync checkbox left checked; stub fetch to return 201 for /api/projects, 200 {ok: true} for /api/github (init-labels), and 200 for /api/projects/42 (PATCH). Assert fetch was called 3 times in order: POST /api/projects, POST /api/github with action=init-labels and repo='octocat/hello', PATCH /api/projects/42 with github_sync_enabled: 1.
    - Test 7: "sync checkbox unchecked skips init-labels chain" — same as Test 6 but user unchecks the sync checkbox before submit. Assert only ONE fetch call fires: POST /api/projects. No /api/github call. No PATCH call.
    - Test 8: "init-labels failure does not block creation — shows inline warning" — stub fetch: POST /api/projects returns 201, POST /api/github returns 500 {error: 'GITHUB_TOKEN not configured'}. Assert the project was still created (the form reset and load() was called), the PATCH is NOT attempted, and the warning text /initLabelsFailedWarning/i is visible.
    - Test 9: "no github_repo -> no chain" — fill only name + ticket_prefix (github_repo empty). Assert only POST /api/projects is called.
  </behavior>

  <action>
    STEP 1 — Extend the createProject function in project-manager-modal.tsx to add the post-create chain.

    After the line `if (!response.ok) throw new Error(data.error || 'Failed to create project')` (currently line 105), insert the chain. The current structure sets form + loads + calls onChanged; the chain must sit between `if (!response.ok)` and `setForm(...)` so failures surface as warnings before the form resets.

    New structure (replacing lines 91-112):

    ```tsx
    const createProject = async (e: React.FormEvent) => {
      e.preventDefault()
      if (!form.name.trim()) return

      const repo = form.github_repo.trim()
      if (repo && !/^[^/]+\/[^/]+$/.test(repo)) {
        setGithubRepoError(t('create.githubRepoInvalid'))
        return
      }

      setInitLabelsWarning(null)

      try {
        const response = await fetch('/api/projects', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            name: form.name,
            ticket_prefix: form.ticket_prefix,
            description: form.description,
            github_repo: repo || undefined,
            deadline: form.deadline ? Math.floor(new Date(form.deadline).getTime() / 1000) : undefined,
            color: form.color || undefined,
          })
        })
        const data = await response.json()
        if (!response.ok) throw new Error(data.error || 'Failed to create project')

        const createdProject = data.project as { id: number } | undefined

        // Post-create chain: init labels + enable sync if user opted in
        if (repo && form.github_sync_enabled && createdProject?.id) {
          try {
            const initRes = await fetch('/api/github', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ action: 'init-labels', repo })
            })
            if (!initRes.ok) {
              const initData = await initRes.json().catch(() => ({}))
              throw new Error(initData.error || 'Failed to initialize labels')
            }
            // Init-labels succeeded (backend already set github_labels_initialized=1); enable sync client-side.
            await fetch(`/api/projects/${createdProject.id}`, {
              method: 'PATCH',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ github_sync_enabled: 1 })
            })
          } catch {
            setInitLabelsWarning(t('create.initLabelsFailedWarning'))
          }
        }

        setForm({ name: '', ticket_prefix: '', description: '', github_repo: '', deadline: '', color: '', github_sync_enabled: true })
        setGithubRepoError(null)
        await load()
        await onChanged?.()
      } catch (err) {
        setError(err instanceof Error ? err.message : 'Failed to create project')
      }
    }
    ```

    Notes on the chain:
    - init-labels failure is caught locally and shown as a warning; the project is still considered created and the modal continues its normal post-create flow (setForm reset, load(), onChanged()).
    - PATCH failure is currently silent (await without error handling). This is intentional for v1 — if init-labels succeeded, the repo has the labels; the user can manually toggle sync in the inline edit UI. Revisit if feedback surfaces.
    - The empty form reset includes github_sync_enabled: true (default-checked for the next creation).

    STEP 2 — Create src/components/modals/__tests__/project-manager-modal.test.tsx.

    This directory does not exist yet — mkdir + create. Follow the vi.mock pattern from projects-panel.test.tsx (next-intl returns keys verbatim; useMissionControl isn't used by this modal so skip that mock). The modal uses useFocusTrap and fetch — mock useFocusTrap to a passthrough ref, and stub global.fetch per-test.

    File skeleton:

    ```tsx
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
    import { render, screen, fireEvent, cleanup, waitFor } from '@testing-library/react'

    vi.mock('next-intl', () => ({
      useTranslations: () => (k: string) => k,
    }))

    vi.mock('@/lib/use-focus-trap', () => ({
      useFocusTrap: () => ({ current: null }),
    }))

    import { ProjectManagerModal } from '../project-manager-modal'

    function mockFetchSequence(responses: Array<{ ok: boolean; status?: number; json: any }>) {
      const calls: Array<{ url: string; init?: RequestInit }> = []
      const fetchMock = vi.fn(async (url: string, init?: RequestInit) => {
        calls.push({ url, init })
        const res = responses.shift()
        if (!res) throw new Error(`Unexpected fetch: ${url}`)
        return {
          ok: res.ok,
          status: res.status ?? (res.ok ? 200 : 500),
          json: async () => res.json,
        } as Response
      })
      ;(global as any).fetch = fetchMock
      return { fetchMock, calls }
    }

    // Default responses for the initial load() GET calls (projects + agents)
    const initialLoad = [
      { ok: true, json: { projects: [] } },
      { ok: true, json: { agents: [] } },
    ]

    beforeEach(() => {
      vi.clearAllMocks()
    })

    afterEach(() => {
      cleanup()
      ;(global as any).fetch = undefined
    })

    describe('ProjectManagerModal — create form upgrades', () => {
      it('renders new github_repo, deadline, and color-palette fields', async () => {
        mockFetchSequence([...initialLoad])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())

        expect(screen.getByPlaceholderText('owner/repo')).toBeTruthy()
        // deadline: type=date has no placeholder by default; query by label text
        expect(screen.getByText(/create\.deadlineLabel/i)).toBeTruthy()
        // Color palette: eight buttons with aria-label = hex value
        expect(screen.getByRole('button', { name: '#3b82f6' })).toBeTruthy()
      })

      it('sync checkbox is hidden when github_repo is empty', async () => {
        mockFetchSequence([...initialLoad])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        expect(screen.queryByLabelText(/create\.enableSyncLabel/i)).toBeNull()
      })

      it('sync checkbox appears + is checked when valid github_repo is typed', async () => {
        mockFetchSequence([...initialLoad])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('owner/repo'), { target: { value: 'octocat/hello' } })
        const cb = screen.getByLabelText(/create\.enableSyncLabel/i) as HTMLInputElement
        expect(cb.checked).toBe(true)
      })

      it('shows invalid-format error and hides sync checkbox for malformed repo input', async () => {
        mockFetchSequence([...initialLoad])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('owner/repo'), { target: { value: 'bad-format' } })
        expect(screen.getByText(/create\.githubRepoInvalid/i)).toBeTruthy()
        expect(screen.queryByLabelText(/create\.enableSyncLabel/i)).toBeNull()
      })

      it('invalid github_repo blocks POST /api/projects', async () => {
        const { calls } = mockFetchSequence([...initialLoad])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('Project name'), { target: { value: 'X' } })
        fireEvent.change(screen.getByPlaceholderText('owner/repo'), { target: { value: 'nope' } })
        fireEvent.click(screen.getByRole('button', { name: /Add Project/i }))
        // Only the two initial GETs should have fired
        expect(calls.filter(c => c.init?.method === 'POST')).toHaveLength(0)
      })

      it('POST payload includes github_repo, deadline (unix seconds), and color when provided', async () => {
        const { calls } = mockFetchSequence([
          ...initialLoad,
          { ok: true, status: 201, json: { project: { id: 42, github_repo: 'octocat/hello' } } },
          { ok: true, json: { ok: true } }, // init-labels
          { ok: true, json: {} },            // PATCH
          { ok: true, json: { projects: [] } }, // reload
          { ok: true, json: { agents: [] } },
        ])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('Project name'), { target: { value: 'Hello' } })
        fireEvent.change(screen.getByPlaceholderText('Ticket prefix (e.g. PA)'), { target: { value: 'HEL' } })
        fireEvent.change(screen.getByPlaceholderText('owner/repo'), { target: { value: 'octocat/hello' } })
        // pick color
        fireEvent.click(screen.getByRole('button', { name: '#3b82f6' }))
        // pick deadline
        const deadlineInput = screen.getByDisplayValue('') // the date input; or use container.querySelector('input[type=date]')
        // Find more robustly via the label wrapper:
        const dateInput = document.querySelector('input[type="date"]') as HTMLInputElement
        fireEvent.change(dateInput, { target: { value: '2026-12-31' } })

        fireEvent.click(screen.getByRole('button', { name: /Add Project/i }))
        await waitFor(() => {
          const post = calls.find(c => c.url === '/api/projects' && c.init?.method === 'POST')
          expect(post).toBeTruthy()
          const body = JSON.parse(post!.init!.body as string)
          expect(body.name).toBe('Hello')
          expect(body.ticket_prefix).toBe('HEL')
          expect(body.github_repo).toBe('octocat/hello')
          expect(body.color).toBe('#3b82f6')
          // Unix seconds for 2026-12-31 UTC: 1798761600 (approximately; assert it's a number within a sane range)
          expect(typeof body.deadline).toBe('number')
          expect(body.deadline).toBeGreaterThan(1_700_000_000)
          expect(body.deadline).toBeLessThan(2_000_000_000)
        })
      })

      it('fires init-labels + PATCH chain when sync checkbox is checked', async () => {
        const { calls } = mockFetchSequence([
          ...initialLoad,
          { ok: true, status: 201, json: { project: { id: 42 } } },
          { ok: true, json: { ok: true, repo: 'octocat/hello' } },
          { ok: true, json: {} },
          { ok: true, json: { projects: [] } },
          { ok: true, json: { agents: [] } },
        ])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('Project name'), { target: { value: 'Hello' } })
        fireEvent.change(screen.getByPlaceholderText('owner/repo'), { target: { value: 'octocat/hello' } })
        fireEvent.click(screen.getByRole('button', { name: /Add Project/i }))

        await waitFor(() => {
          const init = calls.find(c => c.url === '/api/github' && c.init?.method === 'POST')
          expect(init).toBeTruthy()
          const initBody = JSON.parse(init!.init!.body as string)
          expect(initBody.action).toBe('init-labels')
          expect(initBody.repo).toBe('octocat/hello')

          const patch = calls.find(c => c.url === '/api/projects/42' && c.init?.method === 'PATCH')
          expect(patch).toBeTruthy()
          const patchBody = JSON.parse(patch!.init!.body as string)
          expect(patchBody.github_sync_enabled).toBe(1)
        })
      })

      it('skips chain when sync checkbox is unchecked', async () => {
        const { calls } = mockFetchSequence([
          ...initialLoad,
          { ok: true, status: 201, json: { project: { id: 42 } } },
          { ok: true, json: { projects: [] } },
          { ok: true, json: { agents: [] } },
        ])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('Project name'), { target: { value: 'Hello' } })
        fireEvent.change(screen.getByPlaceholderText('owner/repo'), { target: { value: 'octocat/hello' } })
        const cb = screen.getByLabelText(/create\.enableSyncLabel/i)
        fireEvent.click(cb) // uncheck
        fireEvent.click(screen.getByRole('button', { name: /Add Project/i }))

        await waitFor(() => {
          const post = calls.find(c => c.url === '/api/projects' && c.init?.method === 'POST')
          expect(post).toBeTruthy()
          expect(calls.find(c => c.url === '/api/github')).toBeUndefined()
          expect(calls.find(c => c.url === '/api/projects/42' && c.init?.method === 'PATCH')).toBeUndefined()
        })
      })

      it('shows inline warning when init-labels fails but keeps project created', async () => {
        const { calls } = mockFetchSequence([
          ...initialLoad,
          { ok: true, status: 201, json: { project: { id: 42 } } },
          { ok: false, status: 500, json: { error: 'GITHUB_TOKEN not configured' } },
          { ok: true, json: { projects: [] } },
          { ok: true, json: { agents: [] } },
        ])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('Project name'), { target: { value: 'Hello' } })
        fireEvent.change(screen.getByPlaceholderText('owner/repo'), { target: { value: 'octocat/hello' } })
        fireEvent.click(screen.getByRole('button', { name: /Add Project/i }))

        await waitFor(() => {
          expect(screen.getByText(/create\.initLabelsFailedWarning/i)).toBeTruthy()
          // PATCH was NOT attempted
          expect(calls.find(c => c.url === '/api/projects/42' && c.init?.method === 'PATCH')).toBeUndefined()
        })
      })

      it('no chain when github_repo is empty', async () => {
        const { calls } = mockFetchSequence([
          ...initialLoad,
          { ok: true, status: 201, json: { project: { id: 42 } } },
          { ok: true, json: { projects: [] } },
          { ok: true, json: { agents: [] } },
        ])
        render(<ProjectManagerModal onClose={() => {}} />)
        await waitFor(() => expect(screen.queryByText('Loading projects...')).toBeNull())
        fireEvent.change(screen.getByPlaceholderText('Project name'), { target: { value: 'Hello' } })
        fireEvent.click(screen.getByRole('button', { name: /Add Project/i }))

        await waitFor(() => {
          expect(calls.find(c => c.url === '/api/projects' && c.init?.method === 'POST')).toBeTruthy()
          expect(calls.find(c => c.url === '/api/github')).toBeUndefined()
        })
      })
    })
    ```

    Before committing: run `pnpm test -- project-manager-modal.test.tsx` and confirm all 9 tests pass. If the color-palette buttons lack aria-label, Task 1 MUST add `aria-label={c}` on each palette button (already specified in Task 1 Step 3).

    STEP 3 — Final smoke check: run full test suite `pnpm test` to ensure no regressions. The existing projects-panel.test.tsx should still pass (gap 1 wires to this same modal; the modal now opens with new fields but the mock in projects-panel.test.tsx replaces the real modal, so no coupling).
  </action>

  <verify>
    <automated>pnpm test -- project-manager-modal.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/components/modals/__tests__/project-manager-modal.test.tsx` shows the file
    - `pnpm test -- project-manager-modal.test.tsx` exits 0 with exactly 9 tests passing
    - `grep -n "action: 'init-labels'" src/components/modals/project-manager-modal.tsx` finds exactly one match inside createProject
    - `grep -n "PATCH" src/components/modals/project-manager-modal.tsx` finds the new `method: 'PATCH'` call to /api/projects/{id} with github_sync_enabled (in addition to the existing PATCH in saveEdit)
    - `grep -n "initLabelsWarning" src/components/modals/project-manager-modal.tsx` finds both the state declaration and the JSX that renders the warning
    - `grep -n "setInitLabelsWarning" src/components/modals/project-manager-modal.tsx` finds at least the catch-block set and the reset-to-null at the top of createProject
    - `pnpm test` exits 0 with 0 failures (full suite regression check)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>

  <done>
    The create flow now supports GitHub-linked projects in a single form submission. Users who provide an owner/repo string get labels initialized and sync enabled automatically, with graceful degradation if the GitHub API call fails. The new test file covers all 9 behaviors.
  </done>
</task>

</tasks>

<verification>
- Unit tests: `pnpm test -- project-manager-modal.test.tsx` — 9 tests pass
- Full suite: `pnpm test` — no regressions
- Typecheck: `pnpm typecheck`
- Lint: `pnpm lint`
- i18n atomicity: `grep -l '"create"' messages/*.json | wc -l` outputs `10`
- API contract: POST body now includes github_repo/deadline/color (verified via test 6)
- Chain contract: init-labels and PATCH fire only when sync-enabled AND repo valid (verified via tests 6 + 7 + 9)
- Graceful failure: init-labels 500 does not prevent creation (verified via test 8)
</verification>

<success_criteria>
Creating a new project with GitHub integration is now a single-form operation. The user fills name + ticket_prefix + optionally github_repo (owner/repo) + deadline + color, optionally leaves "Enable GitHub sync + initialize labels" checked, and clicks submit once. MC creates the project, initializes the 8 MC labels on the repo, enables two-way sync, and refreshes the project list. If GitHub is unreachable, the project is still created and a non-blocking warning appears.
</success_criteria>

<output>
After completion, create `.planning/phases/08-projects-entry-point/08-05-SUMMARY.md` with:
- Files modified (modal + 10 locale files) and files created (test file + __tests__ directory)
- Gap closed: "UAT Gap 2 — create-project modal upgraded with GitHub linking at creation"
- Tests added count (9) and total pass count
- API contract notes: POST /api/projects body shape, POST /api/github?action=init-labels chain, PATCH /api/projects/{id} for sync flag
- Graceful failure pattern (init-labels error → inline warning, project still created)
- Out-of-scope items reaffirmed: no live repo picker, no pre-existing label import, no auth selector
</output>
