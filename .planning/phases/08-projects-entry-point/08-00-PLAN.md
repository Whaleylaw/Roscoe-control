---
phase: 08-projects-entry-point
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/projects/route.ts
  - src/store/index.ts
  - src/app/api/projects/__tests__/get-last-activity.test.ts
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "GET /api/projects returns a `last_activity_at` field per project (unix ms number or null)"
    - "Project interface in the Zustand store includes `last_activity_at?: number`"
    - "Projects with tasks return MAX(tasks.updated_at) * 1000 (ms); projects with no tasks return null"
  artifacts:
    - path: src/app/api/projects/route.ts
      provides: "GET handler with LEFT JOIN + MAX(tasks.updated_at) returning last_activity_at"
      contains: "last_activity_at"
    - path: src/store/index.ts
      provides: "Project interface extended with last_activity_at"
      contains: "last_activity_at?: number"
    - path: src/app/api/projects/__tests__/get-last-activity.test.ts
      provides: "Unit test covering GET /api/projects last_activity_at behavior"
  key_links:
    - from: src/app/api/projects/route.ts
      to: tasks table
      via: "LEFT JOIN tasks ON tasks.project_id = p.id + MAX(tasks.updated_at) GROUP BY p.id"
      pattern: "LEFT JOIN tasks"
---

<objective>
Extend the backend + store contract so downstream UI (projects list panel) can display a "last activity" hint per project.

Purpose: The projects list panel (Plan 01) shows either a deadline or a last-activity hint per row (D-10, D-16). The data must come from the server — no client-side aggregation (D-18). This plan is a self-contained backend + type change that unblocks Plans 01, 02, 03 running in parallel.

Output: `GET /api/projects` returns `last_activity_at` per project; the `Project` TypeScript interface in the Zustand store reflects the new field; a unit test covers the query behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-projects-entry-point/08-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@src/app/api/projects/route.ts
@src/store/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GET /api/projects with last_activity_at via LEFT JOIN + unit test</name>
  <files>
    - src/app/api/projects/route.ts
    - src/app/api/projects/__tests__/get-last-activity.test.ts
  </files>
  <read_first>
    - .planning/phases/08-projects-entry-point/08-CONTEXT.md (decisions D-16, D-17, D-18)
    - src/app/api/projects/route.ts (current GET handler SQL — the `SELECT p.id, p.workspace_id, p.name, ... (SELECT COUNT(*) FROM tasks t WHERE t.project_id = p.id) as task_count ...` block)
    - src/app/api/projects/__tests__/project-sessions.test.ts (existing test file in this directory — mirror its vi.mock setup for @/lib/db, @/lib/auth, @/lib/logger, @/lib/workspaces)
    - .planning/codebase/TESTING.md (dynamic import pattern for route handler testing)
  </read_first>
  <behavior>
    - Test 1: GET /api/projects returns 200 with `projects` array; each project row contains a `last_activity_at` key
    - Test 2: Project with tasks returns `last_activity_at` as a number (unix milliseconds) equal to MAX(tasks.updated_at) * 1000
    - Test 3: Project with no tasks returns `last_activity_at: null`
    - Test 4: SQL executed by the handler matches /LEFT JOIN\s+tasks\s+t\s+ON\s+t\.project_id\s*=\s*p\.id/ and /MAX\(t\.updated_at\)/ and /GROUP BY\s+p\.id/ (three regex assertions on the captured SQL string)
  </behavior>
  <action>
    Modify the GET handler in `src/app/api/projects/route.ts` so the main SELECT additionally computes `last_activity_at`. Keep the existing correlated subqueries (`task_count`, `assigned_agents_csv`) intact — add the new column via a LEFT JOIN so we can use MAX + GROUP BY without losing the subquery results.

    Concrete change: replace the current single-statement SELECT with an equivalent statement that adds a LEFT JOIN on `tasks` + `MAX(t.updated_at)`. The `tasks.updated_at` column is stored as unix seconds (SQLite convention used elsewhere in this codebase — see `unixepoch()` in the INSERT at line ~114) so multiply by 1000 for millisecond output to match the `last_activity_at?: number` contract (D-17 specifies "unix ms timestamp").

    Target SQL (replace the prepared statement string at approximately lines 40-49):

    ```sql
    SELECT p.id, p.workspace_id, p.name, p.slug, p.description, p.ticket_prefix, p.ticket_counter, p.status,
           p.github_repo, p.deadline, p.color, p.github_sync_enabled, p.github_labels_initialized, p.github_default_branch, p.created_at, p.updated_at,
           (SELECT COUNT(*) FROM tasks t WHERE t.project_id = p.id) as task_count,
           (SELECT GROUP_CONCAT(paa.agent_name) FROM project_agent_assignments paa WHERE paa.project_id = p.id) as assigned_agents_csv,
           (SELECT MAX(t.updated_at) * 1000 FROM tasks t WHERE t.project_id = p.id) as last_activity_at
    FROM projects p
    WHERE p.workspace_id = ?
      ${includeArchived ? '' : "AND p.status = 'active'"}
    ORDER BY p.name COLLATE NOCASE ASC
    ```

    Rationale for subquery over LEFT JOIN + GROUP BY: the existing SELECT already uses two correlated subqueries for `task_count` and `assigned_agents_csv`. Adding a third correlated subquery preserves the existing 1-row-per-project shape WITHOUT needing to add GROUP BY across 15+ columns. Deviates from D-18's exact wording ("LEFT JOIN + GROUP BY") but achieves the same result (server-side MAX aggregation, not client-side) with lower risk. Document this in a one-line code comment: `// last_activity_at: correlated subquery (matches existing task_count pattern); D-18's intent — server-side MAX, not client-side — is preserved.`

    Update the `projects` mapping (line ~51-55) so `last_activity_at` is explicitly included in the returned object. Because we use `...row` spread, it will pass through automatically, but add an explicit normalization: if `row.last_activity_at` is null, return `null`; otherwise coerce to `Number(row.last_activity_at)`.

    Create unit test `src/app/api/projects/__tests__/get-last-activity.test.ts`:
    - `vi.mock('@/lib/db', ...)` with a `prepare` mock that captures the SQL string and returns a stub with `.all()` returning fixture rows
    - `vi.mock('@/lib/auth', ...)` returning `{ user: { id: 1, username: 'admin', role: 'admin', workspace_id: 1, tenant_id: 1 } }`
    - `vi.mock('@/lib/logger', ...)` and `vi.mock('@/lib/workspaces', () => ({ ensureTenantWorkspaceAccess: vi.fn(), ForbiddenError: class {} }))`
    - `vi.mock('@/lib/rate-limit', () => ({ mutationLimiter: vi.fn(() => null) }))` (POST is in the same file; mock to prevent accidental invocation)
    - Use dynamic import after mock setup: `const { GET } = await import('@/app/api/projects/route')`
    - Fixture: two rows — one with `last_activity_at: 1_700_000_000_000`, one with `last_activity_at: null`
    - Assert response body `projects[0].last_activity_at === 1_700_000_000_000` and `projects[1].last_activity_at === null`
    - Capture the SQL string passed to `prepare` and assert the three regexes listed under Test 4 in `<behavior>`

    Do NOT modify POST, do NOT change existing correlated subqueries, do NOT rename any column.
  </action>
  <verify>
    <automated>pnpm test -- src/app/api/projects/__tests__/get-last-activity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/projects/route.ts` contains `last_activity_at` (grep: exact literal string appears at least twice — once in SQL, once in the normalization map)
    - `src/app/api/projects/route.ts` contains `MAX(t.updated_at) * 1000` (grep: exact literal substring)
    - `src/app/api/projects/__tests__/get-last-activity.test.ts` exists
    - `pnpm test -- src/app/api/projects/__tests__/get-last-activity.test.ts` exits 0 with at least 3 passing assertions
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    GET /api/projects returns `last_activity_at: number | null` per project; the SQL adds a correlated subquery that calls `MAX(tasks.updated_at) * 1000`; unit test covers the happy path, the null path, and the SQL shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Project interface in Zustand store with last_activity_at</name>
  <files>
    - src/store/index.ts
  </files>
  <read_first>
    - .planning/phases/08-projects-entry-point/08-CONTEXT.md (decision D-17)
    - src/store/index.ts (lines 329-344 — current Project interface)
  </read_first>
  <action>
    Open `src/store/index.ts`. Locate the `Project` interface starting at line 329. Add a new optional field `last_activity_at?: number` as the last field before the closing brace (after `github_default_branch?: string` at line 343).

    Exact insertion — the interface must end up as:

    ```typescript
    export interface Project {
      id: number
      name: string
      slug: string
      description?: string
      ticket_prefix: string
      status: string
      github_repo?: string
      deadline?: number
      color?: string
      task_count?: number
      assigned_agents?: string[]
      github_sync_enabled?: boolean
      github_labels_initialized?: boolean
      github_default_branch?: string
      last_activity_at?: number
    }
    ```

    Do NOT modify any other part of the store. Do NOT change the `fetchProjects` action — the new field propagates automatically via the existing spread from the API response.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/store/index.ts` contains `last_activity_at?: number` (grep: exact literal)
    - The `last_activity_at?: number` line appears inside the `Project` interface block (grep with context: 5 lines before the match must contain `export interface Project`)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    `Project` type includes optional `last_activity_at: number`; downstream consumers (projects list panel) can read `project.last_activity_at` without TypeScript errors.
  </done>
</task>

</tasks>

<verification>
1. `pnpm test -- src/app/api/projects/__tests__/get-last-activity.test.ts` passes
2. `pnpm typecheck` passes
3. Manual spot-check: `grep -n last_activity_at src/app/api/projects/route.ts` shows at least 2 hits; `grep -n last_activity_at src/store/index.ts` shows 1 hit
</verification>

<success_criteria>
- Backend emits `last_activity_at` per project with correct null/number semantics
- TypeScript `Project` contract reflects the new field
- Unit test covers happy path, null path, and SQL shape
- No other handlers or store fields are touched
</success_criteria>

<output>
After completion, create `.planning/phases/08-projects-entry-point/08-00-SUMMARY.md`.
</output>
</content>
</invoke>