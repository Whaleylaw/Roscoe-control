---
phase: 11-runtime-foundation-v1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/model-registry.ts
  - src/lib/__tests__/model-registry.test.ts
  - src/lib/validation.ts
  - src/lib/__tests__/validation-model-override.test.ts
autonomous: true
requirements:
  - MODEL-01
  - MODEL-03

must_haves:
  truths:
    - "Caller can import getModel() from src/lib/model-registry.ts and get typed metadata for Opus 4.7 / Sonnet 4.6 / Haiku 4.5"
    - "getModel() returns null (not throw) when the identifier is unknown"
    - "POST /api/tasks with model_override = unknown model is rejected at the validation layer with a clear error message referencing the registry"
    - "POST /api/tasks with model_override = known model passes validation"
  artifacts:
    - path: "src/lib/model-registry.ts"
      provides: "Code-seeded model registry module with getModel() lookup"
      exports: ["Model", "getModel", "isKnownModel", "MODEL_IDS"]
      contains: "claude-opus-4-7"
    - path: "src/lib/__tests__/model-registry.test.ts"
      provides: "Vitest coverage of getModel + isKnownModel + registry shape"
    - path: "src/lib/validation.ts"
      provides: "Zod schemas including model_override check in createTaskSchema"
      contains: "model_override"
    - path: "src/lib/__tests__/validation-model-override.test.ts"
      provides: "Vitest coverage confirming unknown model_override is rejected"
  key_links:
    - from: "src/lib/validation.ts"
      to: "src/lib/model-registry.ts"
      via: "imports isKnownModel / getModel and uses it inside createTaskSchema.refine"
      pattern: "from ['\"]\\.\\/model-registry['\"]|isKnownModel|getModel"
    - from: "src/lib/__tests__/model-registry.test.ts"
      to: "src/lib/model-registry.ts"
      via: "import getModel / MODEL_IDS and assert metadata for all three seeded models"
      pattern: "from ['\"]\\.\\.\\/model-registry['\"]"
---

<objective>
Create the code-seeded model registry module and wire its `isKnownModel` check into task validation so `POST /api/tasks` (and `PATCH /api/tasks/:id`) rejects unknown `model_override` values at the schema layer. This is the first of four parallel Wave 1 substrate plans for Phase 11.

Purpose: Lock in MODEL-01 (typed registry) and MODEL-03 (task-override validation). Phase 12 (recipe indexer) and Phase 14 (claim-time model resolution) will both import `getModel` / `MODEL_IDS` from this module, so the shape chosen here is the milestone-wide contract.

Output:
- `src/lib/model-registry.ts` — typed const map seeded with the three current Claude models, exports `Model` type, `getModel(id)`, `isKnownModel(id)`, and a `MODEL_IDS` const tuple.
- `createTaskSchema` / `updateTaskSchema` extended with an optional `model_override: string` field validated against `isKnownModel`.
- Full Vitest coverage for both modules.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-runtime-foundation-v1-2/11-CONTEXT.md

@src/lib/validation.ts

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From src/lib/validation.ts (current shape — extend, don't rewrite):
```typescript
export const createTaskSchema = z.object({
  title: z.string().min(1, 'Title is required').max(500),
  description: z.string().max(5000).optional(),
  status: z.enum(['backlog','inbox','assigned','awaiting_owner','in_progress','review','quality_review','done','failed']).default('inbox'),
  priority: z.enum(['critical','high','medium','low']).default('medium'),
  project_id: z.number().int().positive().optional(),
  // ...many optional fields...
  tags: z.array(z.string().min(1).max(100)).max(50).default([]),
  metadata: taskMetadataSchema.default({}),
})

export const updateTaskSchema = createTaskSchema.partial()
```

The schema is consumed by:
- src/app/api/tasks/route.ts (POST) — via `validateBody(request, createTaskSchema)`
- src/app/api/tasks/[id]/route.ts (PATCH) — via `validateBody(request, updateTaskSchema)`

Both call sites already surface zod issues as `{ error: 'Validation failed', details: [...] }` (400). Adding a `.refine()` on `model_override` will flow through automatically.

From .planning/phases/11-runtime-foundation-v1-2/11-CONTEXT.md (locked):
- Exact identifiers only — no aliases: `claude-opus-4-7`, `claude-sonnet-4-6`, `claude-haiku-4-5-20251001`
- Metadata fields per entry EXACTLY: `{provider, context_window, output_tokens_max, supports_tools, supports_thinking}` — no pricing / display_name / doc_url
- Lookup API: `getModel(id): Model | null` — null on miss, no throw, no Result<T,E>
- Code-seeded const, immutable. No override file, no alias map.
</interfaces>

Project conventions (from CLAUDE.md + .planning/codebase/CONVENTIONS.md):
- Named exports only for lib modules (no default exports)
- Path alias `@/` for app-side imports; lib-internal imports stay relative
- camelCase for functions, PascalCase for types, SCREAMING_SNAKE_CASE for module-level constants
- Tests live in `src/lib/__tests__/` using Vitest
- Strict TS — avoid `any`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create model-registry module with typed const map and getModel lookup</name>
  <files>src/lib/model-registry.ts, src/lib/__tests__/model-registry.test.ts</files>
  <action>
Create `src/lib/model-registry.ts` with:

1. Exported `Model` type with EXACTLY these fields (no more, no less):
   ```typescript
   export interface Model {
     provider: 'anthropic'  // string-literal union; currently only 'anthropic'; widen when a new provider ships
     context_window: number
     output_tokens_max: number
     supports_tools: boolean
     supports_thinking: boolean
   }
   ```

2. Immutable `MODELS` const map keyed by exact model identifier. Use `as const satisfies Record<string, Model>` so TS locks the shape without losing literal keys. Seed EXACTLY these three entries — identifiers are locked by CONTEXT.md and must match character-for-character:
   - `'claude-opus-4-7'` → provider: 'anthropic', context_window: 200000, output_tokens_max: 32000, supports_tools: true, supports_thinking: true
   - `'claude-sonnet-4-6'` → provider: 'anthropic', context_window: 200000, output_tokens_max: 64000, supports_tools: true, supports_thinking: true
   - `'claude-haiku-4-5-20251001'` → provider: 'anthropic', context_window: 200000, output_tokens_max: 8192, supports_tools: true, supports_thinking: false

3. Exports:
   - `export type Model` (interface above)
   - `export const MODELS` (the map)
   - `export const MODEL_IDS = Object.keys(MODELS) as ModelId[]` (union of literal keys via `keyof typeof MODELS`; also export `export type ModelId = keyof typeof MODELS`)
   - `export function getModel(id: string): Model | null` — returns `MODELS[id] ?? null`. Do NOT throw. Do NOT return `undefined`. Explicit null.
   - `export function isKnownModel(id: string): id is ModelId` — type-guard wrapper returning `id in MODELS`.

4. File header JSDoc block that explains: (a) this is a code-seeded, immutable registry, (b) no aliases, no override file in v1.2, (c) adding a model = a PR, (d) references MODEL-01 / MODEL-03 REQ-IDs.

5. DO NOT add: pricing, display_name, doc_url, aliases, a Proxy wrapper, a registerModel() hook, or any config-file loader. These are deferred per CONTEXT.md and would be dead code that Phase 12/14 would then have to unlearn.

Then create `src/lib/__tests__/model-registry.test.ts` with Vitest covering (all three should be short, deterministic, no DB, no fs):
- `getModel('claude-opus-4-7')` returns an object with `provider === 'anthropic'`, `context_window === 200000`, `supports_thinking === true`
- `getModel('claude-sonnet-4-6')` and `getModel('claude-haiku-4-5-20251001')` return non-null with expected shape
- `getModel('gpt-4')` returns `null` (not undefined, not a thrown error)
- `getModel('')` returns `null`
- `isKnownModel('claude-opus-4-7') === true`, `isKnownModel('random') === false`
- `MODEL_IDS.length === 3` and contains all three identifiers (snapshot-style assertion acceptable)
- Type-level assertion (compile-time only): a variable typed `ModelId` can be assigned `'claude-opus-4-7'` but not `'gpt-4'` — use `// @ts-expect-error` comment to assert the negative case.

Test style: follow `src/lib/__tests__/auth.test.ts` + `cron-utils.test.ts` patterns — `describe` + `it`, no mocks needed here.
  </action>
  <verify>
    <automated>pnpm test -- model-registry</automated>
  </verify>
  <done>
  `pnpm test -- model-registry` passes. `pnpm typecheck` passes. `src/lib/model-registry.ts` exists with the three seeded models and Model/getModel/isKnownModel/MODEL_IDS/ModelId exports. The `@ts-expect-error` line demonstrates the literal-union guard holds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire model_override validation into createTaskSchema / updateTaskSchema</name>
  <files>src/lib/validation.ts, src/lib/__tests__/validation-model-override.test.ts</files>
  <action>
Modify `src/lib/validation.ts`:

1. Import at top (relative path, matching existing style in that file — it uses `from 'zod'` directly, so keep consistent):
   ```typescript
   import { isKnownModel, MODEL_IDS } from './model-registry'
   ```

2. Extend `createTaskSchema` by adding the `model_override` field to the `z.object({ ... })` literal. Place it alphabetically near `metadata` or right after `tags` — pick whichever keeps the diff minimal:
   ```typescript
   model_override: z
     .string()
     .min(1)
     .max(200)
     .refine(isKnownModel, (val) => ({
       message: `model_override '${val}' is not in the model registry. Known models: ${MODEL_IDS.join(', ')}`,
     }))
     .optional(),
   ```

   The refine message MUST explicitly mention "model registry" AND list the known identifiers — the success criterion in CONTEXT.md requires "a clear validation error referencing the registry".

3. `updateTaskSchema` is `createTaskSchema.partial()` today — leave that line alone; the new field inherits `.optional()` automatically.

4. Do NOT touch `.omit()` shapes, `createAgentSchema`, `bulkUpdateTaskStatusSchema`, or `taskMetadataSchema`. Minimal diff.

5. Do NOT add validation for `recipe_slug`, `workspace_source`, `read_only_mounts`, or `extra_skills` here — those are Phase 13 (TCTX-01..06). Keep this plan's scope to MODEL-03 only.

Then create `src/lib/__tests__/validation-model-override.test.ts` with Vitest covering:
- `createTaskSchema.safeParse({ title: 'x' })` succeeds (model_override is optional)
- `createTaskSchema.safeParse({ title: 'x', model_override: 'claude-opus-4-7' })` succeeds
- `createTaskSchema.safeParse({ title: 'x', model_override: 'claude-sonnet-4-6' })` succeeds
- `createTaskSchema.safeParse({ title: 'x', model_override: 'gpt-4-turbo' })` fails AND the zod error message includes the string `'model registry'` AND includes `'claude-opus-4-7'` (so callers can see which IDs are valid)
- `updateTaskSchema.safeParse({ model_override: 'claude-opus-4-7' })` succeeds
- `updateTaskSchema.safeParse({ model_override: 'unknown-model' })` fails

Use `.safeParse(...)` so you can assert on `.error.issues` without try/catch boilerplate. No DB, no fetch, no mocks — pure schema assertions.
  </action>
  <verify>
    <automated>pnpm test -- validation-model-override</automated>
  </verify>
  <done>
  New test file passes. `pnpm typecheck` passes. A POST to `/api/tasks` with `{ title: 'x', model_override: 'gpt-4' }` would now return 400 with the detail line containing `model_override 'gpt-4' is not in the model registry`. Existing task-creation flows (no `model_override`) continue to succeed — zero behavior change for callers that don't set the field.
  </done>
</task>

</tasks>

<verification>
- `pnpm test -- model-registry` — registry shape + lookup behavior
- `pnpm test -- validation-model-override` — schema-layer validation
- `pnpm typecheck` — `ModelId` literal union holds, no `any` leaked
- `pnpm lint` — conventions match existing lib files
- Manual spot-check: `grep -n 'isKnownModel\|MODEL_IDS' src/lib/validation.ts` shows the import + refine
- Grep confirms only the four files in `files_modified` changed: `git diff --stat` should show only those four files
</verification>

<success_criteria>
1. `src/lib/model-registry.ts` exists and exports `Model`, `ModelId`, `MODELS`, `MODEL_IDS`, `getModel`, `isKnownModel`
2. The three current Claude models are seeded with EXACT identifiers from CONTEXT.md
3. `getModel('unknown')` returns `null`; no throw path
4. `createTaskSchema` accepts `model_override` when known, rejects when unknown, with a message that includes "model registry"
5. Two new Vitest files pass; existing tests remain green (`pnpm test` full run clean)
6. `pnpm typecheck` clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-foundation-v1-2/11-01-SUMMARY.md` documenting:
- The exact shape of `Model` and the three seeded entries
- Import pattern that Phases 12 / 14 should use (`import { getModel, isKnownModel } from '@/lib/model-registry'`)
- The validation error format (so Phase 12's recipe-indexer error surface for MODEL-02 can match it)
</output>
