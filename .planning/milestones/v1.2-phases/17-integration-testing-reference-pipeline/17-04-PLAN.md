---
phase: 17-integration-testing-reference-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/lib/__tests__/phase-17-daemon-pipeline.test.ts
  - scripts/mc-runner-smoke.sh
autonomous: true
requirements:
  - RTEST-02
user_setup: []

must_haves:
  truths:
    - "A daemon-subprocess integration test spawns scripts/mc-runner.mjs, starts a live test server, and drives the full pipeline end-to-end"
    - "Test asserts the REAL runner daemon boot loop + register + heartbeat + SSE subscribe + claim + docker run + submit sequence"
    - "Test asserts end state: task reaches 'done' after runAegisReviews"
    - "Test cleans up: SIGTERMs daemon, SIGKILLs on timeout, label-scoped docker rm -f, rm worktrees, close server"
    - "smoke harness preserve-on-stop subcommand is implemented (reserved stub at lines 542-544 replaced)"
  artifacts:
    - path: "src/lib/__tests__/phase-17-daemon-pipeline.test.ts"
      provides: "Daemon-subprocess integration test (D-02 second lane)"
      min_lines: 250
      contains: "scripts/mc-runner.mjs"
    - path: "scripts/mc-runner-smoke.sh"
      provides: "preserve-on-stop bash subcommand implementation"
      contains: "run_preserve_on_stop"
  key_links:
    - from: "src/lib/__tests__/phase-17-daemon-pipeline.test.ts"
      to: "scripts/mc-runner.mjs"
      via: "spawn('node', ['scripts/mc-runner.mjs'])"
      pattern: "mc-runner\\.mjs"
    - from: "src/lib/__tests__/phase-17-daemon-pipeline.test.ts"
      to: "live test HTTP server on dynamic port"
      via: "http.createServer bound to Next.js route handlers"
      pattern: "createServer|listen\\(0"
    - from: "scripts/mc-runner-smoke.sh"
      to: "docker kill -s SIGTERM + worktree preservation assertion"
      via: "new run_preserve_on_stop function"
      pattern: "preserve_on_stop"
---

<objective>
Ship the D-02 daemon-subprocess integration test (the "full lane" companion to 17-03's direct-helpers test). This spawns `scripts/mc-runner.mjs` as a real child process, binds a real HTTP server to a dynamic port, and lets the daemon's boot loop (register → heartbeat → SSE subscribe → claim → docker run → submit) drive the pipeline end-to-end. Highest-fidelity RTEST-02 coverage. Slower (~60-180s) — runs alongside 17-03 but covers the boot-loop + SSE + polling paths that direct-helpers skip.

Also extends `scripts/mc-runner-smoke.sh` to implement the `preserve-on-stop` subcommand (reserved stub at lines 542-544 per CONTEXT.md canonical refs).

Purpose: Catches daemon-boot-order regressions, SSE subscriber regressions, 15s polling fallback regressions that direct-helper tests miss. Also: proves `scripts/mc-runner.mjs` itself composes correctly against the Phase 17-01 review-flip.

Decision D-02: both lanes ship. Decision D-03: daemon + real container (the spec's "high fidelity" mode).

Output: One new Vitest integration test file + one new bash subcommand.
</objective>

<execution_context>
@./get-shit-done-main/get-shit-done/workflows/execute-plan.md
@./get-shit-done-main/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-integration-testing-reference-pipeline/17-CONTEXT.md
@.planning/phases/17-integration-testing-reference-pipeline/17-RESEARCH.md
@.planning/phases/17-integration-testing-reference-pipeline/17-01-PLAN.md

<interfaces>
From scripts/mc-runner.mjs (daemon entry — DO NOT modify):
```
Boot sequence (Phase 14-08b LOCKED):
  1. Read .data/runner.secret → exit 1 if missing
  2. docker info → exit 2 if Docker down
  3. GET /api/runner/config (with runner-secret bearer) → exit 1 if config missing
  4. Reconcile pending containers
  5. Heartbeat (10s interval)
  6. SSE subscribe to task.runner_requested + 15s poll fallback
  7. GC tick

Env vars daemon reads:
  - MC_URL (defaults to http://127.0.0.1:3000)
  - MISSION_CONTROL_DATA_DIR (locates runner.secret)
  - MC_RUNNER_SECRETS_DIR (defaults to .data/runner/secrets/)

SIGTERM graceful: stops accepting new claims, lets in-flight containers run to completion.
SIGKILL abrupt: next boot reconciles orphans.
```

From scripts/mc-runner-smoke.sh (bash entry — to be extended):
```
Lines 542-544 (reserved stubs):
  preserve-on-stop|preserve-across-crash)
    die "Subcommand '$1' is reserved for Phase 15/17; not yet implemented."
    ;;
```
Phase 17 implements `preserve-on-stop`. `preserve-across-crash` stays reserved (owned by 17-05 if chosen — currently 17-05 uses the Vitest path).

From src/app/api/runner/config/route.ts (Phase 14-11):
```
GET /api/runner/config → 200 with {
  project_repo_map, mount_allowlist, max_concurrent_containers,
  runner_worktrees_dir, runner_logs_dir, runner_secrets_dir
}
Requires runner-secret bearer. Test server must serve this route or runner exits 1.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase-17-daemon-pipeline.test.ts spawning real runner daemon</name>
  <files>src/lib/__tests__/phase-17-daemon-pipeline.test.ts</files>
  <read_first>
    - src/lib/__tests__/phase-17-pipeline-integration.test.ts (Plan 17-03 output — copy fixture + mock setup verbatim; this test's DELTA is "spawn daemon instead of direct-helpers". Note whether 17-03 used a real or stubbed Aegis seam — for consistency, mirror the same choice here unless there's a specific reason to differ.)
    - scripts/mc-runner.mjs (full file — understand exact env vars, exit codes, boot sequence; identify how to point the daemon at the test server)
    - scripts/mc-runner-smoke.sh (full file — the bash smoke is the structural template; this test is that workflow inside Vitest)
    - src/app/api/runner/config/route.ts (runner daemon boot step 3 — test server must route this)
    - src/app/api/runner/heartbeat/route.ts (daemon sends every 10s — must route)
    - src/app/api/runner/ready-tasks/route.ts (daemon polls every 15s — must route)
    - src/app/api/runner/pending-containers/route.ts (daemon reconciles on boot — must route)
    - src/app/api/runner/claim/[task_id]/route.ts (claim — must route)
    - src/app/api/runner/tasks/[task_id]/runner-exit/route.ts (daemon posts on exit — must route)
    - src/app/api/runner/tasks/[task_id]/submit/route.ts (post-17-01 review-flip — must route)
    - src/app/api/events/route.ts (SSE endpoint — daemon subscribes; can be stubbed with a simple keep-alive stream if fully wiring is painful)
    - src/app/api/tasks/[id]/checkpoints/route.ts (container POSTs here via runner-token — must route)
    - src/lib/task-dispatch.ts lines 414-570 (runAegisReviews — identify credential/API seam for the stub fallback in Phase E below)
  </read_first>
  <action>
    Create `src/lib/__tests__/phase-17-daemon-pipeline.test.ts`. Structure mirrors 17-03 but replaces the direct-helper invocation with a spawned daemon.

    File header:
    ```ts
    // @vitest-environment node
    ```

    Boundary-mock setup: identical to 17-03 (same vi.mocks for db, runner-secret, rate-limit, security-events, event-bus).

    Key difference: this test needs a REAL HTTP server the daemon child-process can reach. Use `http.createServer()` bound to port 0 for dynamic allocation. The server routes the 8 endpoints listed in read_first (config, heartbeat, ready-tasks, pending-containers, claim, runner-exit, submit, checkpoints) by matching URL path + method and delegating to the imported Next.js route handlers.

    Also routes `/api/events` as a minimal SSE stream:
    ```ts
    if (url.pathname === '/api/events') {
      res.writeHead(200, {
        'Content-Type': 'text/event-stream',
        'Cache-Control': 'no-cache',
        Connection: 'keep-alive',
      })
      res.write(':ok\n\n')
      // Forward broadcastMock.mock.calls into SSE frames as they arrive — optional.
      // Minimum: keep the stream open so daemon subscriber believes it's connected.
      return
    }
    ```

    Fixture setup in beforeAll:
    1. testDb = new Database(':memory:') + runMigrations
    2. mkdtemp root + subdirs (repos, worktrees, recipes, runner-data-dir, runner-secrets, logs)
    3. Write runner.secret file at `runnerDataDir/runner.secret` (matching vi.mock value: `phase17-test-secret-abcdefghijklmnop`)
    4. setupGitRepo(repoPath)
    5. INSERT settings (project_repo_map, mount_allowlist, max_concurrent_containers, runner_worktrees_dir, runner_logs_dir, runner_secrets_dir)
    6. Copy recipes/hello-world/ to recipesDir; indexRecipe({ dbOverride: testDb })
    7. Start HTTP server on port 0; capture port
    8. Set env for daemon: `const daemonEnv = { ...process.env, MC_URL: \`http://127.0.0.1:\${port}\`, MISSION_CONTROL_DATA_DIR: runnerDataDir, MC_RUNNER_SECRETS_DIR: runnerSecretsDir }`

    In the single test body `it('full pipeline via scripts/mc-runner.mjs daemon', async () => { ... }, 180_000)`:

    **Phase A — Create task:**
    - Call POST /api/tasks handler directly (via imported route) with body `{recipe_slug: 'hello-world', workspace_source, status: 'assigned'}`
    - Assert status 201, task.status === 'assigned'

    **Phase B — Spawn daemon:**
    ```ts
    const daemon = spawn('node', ['scripts/mc-runner.mjs'], {
      env: daemonEnv,
      stdio: ['ignore', 'pipe', 'pipe'],
    })
    const logs: string[] = []
    daemon.stdout.on('data', (c) => logs.push('[stdout] ' + c.toString()))
    daemon.stderr.on('data', (c) => logs.push('[stderr] ' + c.toString()))
    ```

    **Phase C — Wait for daemon to claim and complete task:**
    - Poll testDb every 500ms for up to 120 seconds:
      - When task.status transitions through 'in_progress' and eventually to 'review' OR 'done', proceed
    - Attach diagnostic: on timeout, include `logs.join('')` in the failure message

    **Phase D — Assert Phase 17-01 review flip:**
    - task.status === 'review' immediately after container submit (may be racy — assert within 5s of the review broadcast)
    - task.container_id === null
    - task.completed_at === null at this phase
    - task_checkpoints count >= 1
    - JSONL line count matches
    - task_runner_tokens.revoked_at is set

    **Phase E — Aegis:**
    - `await runAegisReviews()` — import from @/lib/task-dispatch
    - Poll testDb for final done:
      - task.status === 'done'
      - task.completed_at is a positive unix timestamp

    **Aegis credential fallback (D-06 boundary-mock pattern):**
    If `runAegisReviews()` requires live Aegis credentials / external API connectivity that are NOT available in test environments (inspect `src/lib/task-dispatch.ts` lines 414-570 for the credential seam — e.g., `AEGIS_API_KEY`, `fetch()` to an external endpoint, SDK init that throws without a token), the test MUST stub the verdict using the D-06 boundary-mock pattern. Add this near the top of the test file alongside the other `vi.mock` calls:

    ```ts
    // D-06 boundary-mock: stub Aegis verdict if the real path requires creds we don't have in test.
    // Adjust the import path to match the ACTUAL module exporting runAegisReviews (read task-dispatch.ts
    // to confirm; it may be @/lib/task-dispatch, @/lib/aegis-orchestrator, or @/lib/aegis-reviews).
    vi.mock('@/lib/task-dispatch', async (importActual) => {
      const actual = await importActual<typeof import('@/lib/task-dispatch')>()
      return {
        ...actual,
        runAegisReviews: vi.fn().mockImplementation(async () => {
          // Perform the DB transition the real function would do: find tasks WHERE status='review',
          // flip to 'done', set completed_at. This keeps the test asserting the real DB state machine
          // while bypassing the external Aegis API call.
          const db = (await import('@/lib/db')).getDatabase()
          const reviewTasks = db.prepare("SELECT id FROM tasks WHERE status IN ('review', 'quality_review')").all() as { id: number }[]
          const now = Math.floor(Date.now() / 1000)
          for (const t of reviewTasks) {
            db.prepare("UPDATE tasks SET status = 'done', completed_at = ? WHERE id = ?").run(now, t.id)
          }
          return { ok: true, message: `Approved ${reviewTasks.length} task(s) (stubbed for test)` }
        }),
      }
    })
    ```

    Decision rule (mirrors 17-03): prefer the REAL `runAegisReviews()` if it works in the test env (e.g., Aegis is a pure in-process function with no network deps). Use the stub ONLY if the real path requires external credentials/API. For consistency, make the SAME choice as 17-03's pipeline integration test — if 17-03 stubbed, 17-04 stubs; if 17-03 used the real path, 17-04 uses the real path. Document which path was taken in 17-04-SUMMARY.md. The test file MUST contain EITHER:
    - (a) a real call to `runAegisReviews()` with no mock (grep-verifiable: `grep -c "vi.mock.*task-dispatch" → 0`), OR
    - (b) the explicit `vi.mock('@/lib/task-dispatch', ...)` stub above (grep-verifiable: `grep -c "vi.mock.*task-dispatch" → 1`)

    One or the other — not both, not neither.

    **Phase F — Graceful daemon shutdown (afterEach or test body teardown):**
    ```ts
    daemon.kill('SIGTERM')
    await new Promise((r) => setTimeout(r, 2000))
    if (!daemon.killed) daemon.kill('SIGKILL')
    ```

    Cleanup (afterEach):
    - Label-scoped docker cleanup (mc.test.phase17daemon label on docker run — may need to set via `MC_RUNNER_DOCKER_EXTRA_LABELS` env or similar; if scripts/mc-runner.mjs doesn't honor an extra-labels env, the test uses the existing `mc.task_id` label that runner-docker.ts always applies and filters by taskId)
    - Read runner-docker.ts composeDockerArgs to confirm existing labels; use them for cleanup
    - rm -rf worktreePath
    - server.close(); testDb.close(); rm -rf tmpRoot

    describe.skipIf(!dockerAvailable || !imageAvailable) wrapper — identical to 17-03.

    Test timeout: 180_000 ms (3 min) — daemon boot + real docker pull + container run + submit + Aegis roundtrip.
  </action>
  <verify>
    <automated>pnpm test --run src/lib/__tests__/phase-17-daemon-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/__tests__/phase-17-daemon-pipeline.test.ts` exists
    - File starts with `// @vitest-environment node`
    - `grep -c "scripts/mc-runner.mjs" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` >= 1
    - `grep -c "spawn\|spawnSync" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` >= 2 (daemon spawn + docker/cleanup)
    - `grep -c "SIGTERM\|SIGKILL" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` >= 2 (graceful + force)
    - `grep -c "describe.skipIf" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` >= 1
    - `grep -c "runAegisReviews\|task-dispatch" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` >= 1
    - `grep -c "createServer\|listen(0" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` >= 1
    - `grep -c "testcontainers" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` === 0 (D-07)
    - `grep -c "'review'\|status.*review" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` >= 2
    - **Aegis-seam coverage (D-06 one-or-the-other rule):** exactly ONE of the following must hold, grep-verifiable:
      - (a) Real path: `grep -c "vi.mock.*task-dispatch\|vi.mock.*aegis" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` === 0 AND the test exercises real `runAegisReviews()` end-to-end, OR
      - (b) Stubbed path: `grep -c "vi.mock.*task-dispatch\|vi.mock.*aegis" src/lib/__tests__/phase-17-daemon-pipeline.test.ts` === 1 AND the mock implementation flips 'review' → 'done' in testDb
      - NOT both simultaneously; NOT neither. For consistency with 17-03, mirror the SAME choice as 17-03's pipeline integration test; document in 17-04-SUMMARY.md.
    - On a Docker-equipped host: test passes in under 180s
    - On a non-Docker host: test skips silently
    - `pnpm typecheck` exits 0
    - File size >= 250 lines
  </acceptance_criteria>
  <done>Daemon-subprocess integration test spawns real runner daemon against a dynamic-port test server, exercises the full boot loop + claim + container + submit + Aegis sequence, cleans up deterministically. Aegis seam is either real or explicitly stubbed — grep-verifiable, documented in SUMMARY, consistent with 17-03's choice.</done>
</task>

<task type="auto">
  <name>Task 2: Implement preserve-on-stop subcommand in scripts/mc-runner-smoke.sh</name>
  <files>scripts/mc-runner-smoke.sh</files>
  <read_first>
    - scripts/mc-runner-smoke.sh (full file — especially the existing `run_hello_world` function for pattern, the EXIT trap at lines 106-120, and the reserved stubs at lines 542-544)
    - scripts/mc-runner.mjs (understand graceful SIGTERM behavior)
    - .planning/phases/14-runner-container-v1-2/14-10-SUMMARY.md (smoke harness conventions)
  </read_first>
  <action>
    Open `scripts/mc-runner-smoke.sh`. Locate the reserved stub at lines 542-544:
    ```bash
      preserve-on-stop|preserve-across-crash)
        die "Subcommand '$1' is reserved for Phase 15/17; not yet implemented."
        ;;
    ```

    Replace ONLY the `preserve-on-stop` branch (keep `preserve-across-crash` reserved). The new dispatch:
    ```bash
      preserve-on-stop)
        run_preserve_on_stop
        ;;
      preserve-across-crash)
        die "Subcommand 'preserve-across-crash' is reserved for Phase 17; not yet implemented."
        ;;
    ```

    ABOVE the `case "${1:-help}" in` dispatch, add a new function `run_preserve_on_stop()`. Pattern after `run_hello_world`, with these steps:

    1. **Banner:** `log "Phase 17 preserve-on-stop: verifying worktree + .mc/ are preserved when runner stops mid-task"`

    2. **Preconditions:** reuse run_hello_world's preflight (docker info, image inspect, AUTH_USER/AUTH_PASS, API_KEY). If any missing, die with helpful message.

    3. **Seed:** call the existing seed helpers (or inline the equivalent) to:
       - Configure runtime.project_repo_map + runtime.mount_allowlist via PUT /api/settings using $MC_API_KEY
       - Ensure `mc-runner-smoke` project exists (create via POST /api/projects if missing)
       - Create a task with `recipe_slug=hello-world` and `workspace_source={project_id, base_ref: "main"}`, capture $TASK_ID

    4. **Spawn runner daemon:** `node scripts/mc-runner.mjs &`, capture PID. Set EXIT trap to SIGTERM-first, 5s grace, SIGKILL on timeout (per 14-10 LOCKED printf rule).

    5. **Wait for claim:** poll `curl -s -H "x-api-key: $MC_API_KEY" "$MC_URL/api/tasks/$TASK_ID" | jq -r .status` until it returns "in_progress". Timeout 60s. If timeout, tail daemon stderr and die.

    6. **Wait for first checkpoint:** poll `curl -s -H "x-api-key: $MC_API_KEY" "$MC_URL/api/tasks/$TASK_ID/checkpoints" | jq -r '.checkpoints | length'` until >= 1. Timeout 30s.

    7. **SIGTERM the daemon:** `kill -TERM $RUNNER_PID`. Wait up to 10s for daemon exit.

    8. **Stop the container gracefully:** `docker stop --time=15 $(docker ps -q --filter "label=mc.task_id=$TASK_ID" | head -n1)` (if any). Wait for exit.

    9. **Assert worktree preserved:**
       - `WORKTREE=$(curl -s -H "x-api-key: $MC_API_KEY" "$MC_URL/api/tasks/$TASK_ID" | jq -r .worktree_path)`
       - `test -d "$WORKTREE" || die "Worktree $WORKTREE missing after daemon stop"`
       - `test -f "$WORKTREE/.mc/task.json" || die "task.json missing"`
       - `test -f "$WORKTREE/.mc/progress.md" || die "progress.md missing"`
       - `test -f "$WORKTREE/.mc/checkpoints.jsonl" || die "checkpoints.jsonl missing"`
       - `test -s "$WORKTREE/.mc/checkpoints.jsonl" || die "checkpoints.jsonl is empty — expected at least 1 line"`

    10. **Report:** `log "OK: daemon stopped gracefully; worktree at $WORKTREE preserved with .mc/ intact"`. Exit 0.

    11. **Cleanup on exit:** EXIT trap also removes the task (DELETE /api/tasks/$TASK_ID) and the worktree directory.

    DO NOT:
    - Modify `run_hello_world`
    - Change `preserve-across-crash` branch (still deferred stub — claimed by a future plan, not 17-05)
    - Add new top-level env vars beyond what `run_hello_world` already requires (MC_URL, MC_API_KEY, AUTH_USER, AUTH_PASS, optional POLL_BUDGET_SEC)
    - Use `printf '%()T'` — per 14-10 LOCKED rule use `printf '[%s] %s\n' "$(date '+%H:%M:%S')" "$*"` in log helpers

    Use `set -euo pipefail` if the script already has it (it does); respect existing shell conventions.

    Run `bash -n scripts/mc-runner-smoke.sh` to lint-check; resolve any syntax errors before committing.
  </action>
  <verify>
    <automated>bash -n scripts/mc-runner-smoke.sh &amp;&amp; grep -c "run_preserve_on_stop" scripts/mc-runner-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "run_preserve_on_stop" scripts/mc-runner-smoke.sh` >= 2 (function definition + case dispatch)
    - `grep -c "preserve-on-stop)" scripts/mc-runner-smoke.sh` >= 1
    - `grep -c "not yet implemented" scripts/mc-runner-smoke.sh` === 1 (only preserve-across-crash still reserved)
    - `bash -n scripts/mc-runner-smoke.sh` exits 0 (syntax clean)
    - `grep -c "SIGTERM\|kill -TERM" scripts/mc-runner-smoke.sh` includes the new `preserve-on-stop` function usage
    - `grep -c "printf '\[%s\] %s" scripts/mc-runner-smoke.sh` >= 1 (new function uses the LOCKED log format, NOT %()T)
    - `grep -c "printf '%()T'" scripts/mc-runner-smoke.sh` === 0 (LOCKED rule from 14-10)
    - `grep -c "checkpoints.jsonl\|progress.md\|task.json" scripts/mc-runner-smoke.sh` >= 3 (assertions in new function)
    - Script can be invoked end-to-end on a dev host with a running MC + Docker: `bash scripts/mc-runner-smoke.sh preserve-on-stop` exits 0 (integration test, not required to be green in this plan but verified at SUMMARY time)
  </acceptance_criteria>
  <done>`preserve-on-stop` subcommand implemented; `preserve-across-crash` still reserved; script lints clean; new function follows 14-10 LOCKED logging + trap conventions.</done>
</task>

</tasks>

<verification>
  - On a Docker-equipped dev host: `pnpm test --run src/lib/__tests__/phase-17-daemon-pipeline.test.ts` passes in < 180s
  - On a CI host without Docker: test skips silently
  - `bash -n scripts/mc-runner-smoke.sh` — syntax clean
  - `pnpm typecheck` exits 0
  - `pnpm lint` — no new errors
  - Commit(s): one for the daemon test, one for the bash script. Conventional Commits.
</verification>

<success_criteria>
  - Daemon-subprocess integration test exists, docker-gated, spawns real `scripts/mc-runner.mjs`, drives full RTEST-02 pipeline through the daemon boot loop
  - Aegis seam either exercises the real path OR includes explicit D-06 boundary-mock stub (one or the other, grep-verifiable, consistent with 17-03)
  - Test runs in < 180s on Docker hosts; skips on non-Docker hosts
  - SIGTERM graceful shutdown + SIGKILL fallback + label-scoped docker cleanup prevents leaked processes/containers
  - Review-flip + Aegis-to-done transition asserted against the Phase 17-01 behavior
  - `preserve-on-stop` bash subcommand ships with worktree-preservation + .mc/ file assertions
  - `preserve-across-crash` stays reserved (future plan)
  - No new npm dependencies (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/17-integration-testing-reference-pipeline/17-04-SUMMARY.md` documenting:
  - Whether the test found any runner daemon boot-order bugs uncovered (diff vs direct-helper lane)
  - Measured wall-clock time for a full daemon-path run
  - How SSE subscriber was handled in the test server (keep-alive stub vs full event broadcast forwarding)
  - **Which Aegis seam path was chosen: (a) real `runAegisReviews()` call, OR (b) D-06 boundary-mock stub. Confirm consistency with 17-03-SUMMARY.md's choice. Include rationale.**
  - Any flakes observed and their mitigation (timing, race, order)
  - Confirmation that `preserve-on-stop` runs green against a live dev MC + Docker
</output>
</output>
