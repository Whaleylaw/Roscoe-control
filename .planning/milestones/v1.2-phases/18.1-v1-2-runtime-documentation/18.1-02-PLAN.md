---
phase: 18.1-v1-2-runtime-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runtime/runner-daemon.md
  - docs/runtime/admin-config.md
autonomous: true
requirements:
  - DOC-RUN
  - DOC-CFG
user_setup: []

must_haves:
  truths:
    - "Operator can read docs/runtime/runner-daemon.md and boot the runner daemon successfully (foreground + LaunchAgent paths)"
    - "Operator understands the runner's 7-step boot sequence and what each exit code means"
    - "Operator can read docs/runtime/admin-config.md and enumerate all 8 runtime.* settings keys with their types, defaults, and pnpm mc commands"
    - "Operator understands runtime.project_repo_map is the EXCLUSIVE resolution path (no env-var fallback) — pitfall 4 addressed"
    - "Operator is warned never to pass secrets on docker run argv (CONTAINER-01 invariant) — pitfall 6 addressed"
    - "Operator sees explicit instruction to set MISSION_CONTROL_RECIPES_DIR for standalone builds — pitfall 7 addressed"
    - "Operator sees the recipe-declared-secrets pattern (ENV VAR NAMES only, values in .data/runner/secrets/<NAME>) — pitfall 8 addressed"
    - "runner-daemon.md LINKS to scripts/README.runner.md rather than duplicating its 154 lines (Don't-Hand-Roll #1)"
    - "Every claim cites a source-of-truth file path + line range (verifiable by harness in Plan 07)"
  artifacts:
    - path: "docs/runtime/runner-daemon.md"
      provides: "DOC-RUN — operator-facing runner daemon intro + deep-reference links"
      min_lines: 160
      contains: "scripts/README.runner.md"
    - path: "docs/runtime/admin-config.md"
      provides: "DOC-CFG — admin config reference for runtime.* settings, secrets store, auth tiers, data-dir layout"
      min_lines: 200
      contains: "runtime.project_repo_map"
  key_links:
    - from: "docs/runtime/runner-daemon.md"
      to: "scripts/README.runner.md"
      via: "relative markdown link in header + per-section deep-link pointers"
      pattern: "README\\.runner\\.md"
    - from: "docs/runtime/admin-config.md project_repo_map section"
      to: "src/app/api/runner/config/route.ts + src/app/api/settings/route.ts settingDefinitions"
      via: "prose citing exclusive-resolution rule"
      pattern: "project_repo_map.*EXCLUSIVE|No env-var fallback"
    - from: "docs/runtime/admin-config.md standalone section"
      to: "scripts/mc-runner.mjs:1047-1049 (getRecipesRoot default)"
      via: "prose + env var table entry"
      pattern: "MISSION_CONTROL_RECIPES_DIR"
---

<objective>
Co-author `docs/runtime/runner-daemon.md` and `docs/runtime/admin-config.md`. These are co-planned because ~80% of the admin-config surface an operator touches IS runner-config — splitting them across parallel plans risks contradictory statements (research LOCKED this, see 18.1-RESEARCH.md §Bundle B rationale).

Purpose: Today the runner daemon operator surface lives at `scripts/README.runner.md` (authoritative, 162 lines). There is NO operator-facing admin-config reference — operators discover `runtime.*` settings by reading `src/app/api/settings/route.ts`. This plan produces:
1. A thin `runner-daemon.md` that frames the daemon for operators, links to the authoritative `scripts/README.runner.md` for deep content, and EMPHASIZES drift-sensitive facts (exit codes, project_repo_map exclusivity, secrets-off-argv).
2. A complete `admin-config.md` that enumerates all 8 `runtime.*` settings, `.data/` layout, auth secrets (`AUTH_SECRET`, `API_KEY`), runner secrets store, env-var catalog, and standalone-mode requirements.

Output: Two new markdown files under `docs/runtime/`. Neither duplicates `scripts/README.runner.md` — `runner-daemon.md` is an intro that links to it; `admin-config.md` complements it with broader admin surface. Together they address pitfalls 4 (project_repo_map exclusive), 6 (secrets never on argv), 7 (MISSION_CONTROL_RECIPES_DIR for standalone), and 8 (recipe.secrets is names not values).

Scope guardrail: DOCUMENTATION ONLY. No src/, scripts/, docker/, or recipes/ code changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-v1-2-runtime-documentation/18.1-RESEARCH.md

<!-- Authoritative runner reference (LINK TO, DO NOT DUPLICATE) -->
@scripts/README.runner.md

<!-- Source-of-truth files for runner-daemon.md -->
@scripts/mc-runner.mjs
@scripts/com.missioncontrol.runner.plist
@src/lib/runner-secret.ts
@src/app/api/runner/config/route.ts

<!-- Source-of-truth files for admin-config.md -->
@src/app/api/settings/route.ts
@src/lib/task-runtime-settings.ts
@src/lib/auth.ts

<!-- Style precedents -->
@docs/cli-agent-control.md
@docs/deployment.md

<interfaces>
<!-- Key contracts from source-of-truth files the executor MUST reference -->

From scripts/mc-runner.mjs (confirmed via grep):
- `process.env.MISSION_CONTROL_DATA_DIR` (line 48) — default: `<cwd>/.data`
- `process.env.MC_URL` (line 49) — default: `http://127.0.0.1:3000`
- `process.env.RUNNER_ID` (line 50) — default: `runner-<hostname>-<pid>`
- `process.env.MISSION_CONTROL_RECIPES_DIR` (line 1048) — default: `<cwd>/recipes`
- `process.env.PORT` (line 1036) — drives `host.docker.internal:<PORT>` in composed MC_API_URL
- Exit 1 (lines 401, 449) — bootstrap failures (runner.secret missing, /api/runner/config unreachable)
- Exit 2 (line 415) — docker daemon unreachable
- Exit 0 (line 1157) — graceful shutdown (SIGINT/SIGTERM)

From src/app/api/settings/route.ts settingDefinitions (lines 60-96):
- `runtime.mount_allowlist` — string[] JSON — paths the runner may mount into containers
- `runtime.read_only_mounts_cap` — int — default 10
- `runtime.extra_skills_cap` — int — default 20
- `runtime.max_concurrent_containers` — int — global runner concurrency cap
- `runtime.project_repo_map` — object {project_id: abs_path} — EXCLUSIVE resolution path
- `runtime.max_memory_per_container` — string (e.g. "2g")
- `runtime.max_cpu_per_container` — number (e.g. 1.0)
- `runtime.failed_gc_window_days` — int — default 7

From STATE.md locked decisions (Phase 14-08b):
- "No env-var fallback — a misconfigured /api/runner/config must fail loud at boot rather than silently ship wrong repo paths"
- Boot sequence 7-step order: runner.secret → docker info → /api/runner/config → reconcile → heartbeat → SSE+poll → GC tick
- SIGHUP re-fetches config without daemon restart

From STATE.md locked decisions (Phase 14-07):
- CONTAINER-01 invariant: no --env flag on docker argv carries MC_API_TOKEN value; secrets flow via --env-file only
- Image is LAST argv element (flag-ordering guard)

From docker networking (Phase 14-05):
- MC_API_URL inside container = `http://host.docker.internal:${PORT || 3000}`, NOT `localhost`
- Runner passes `--add-host host.docker.internal:host-gateway`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/runtime/runner-daemon.md — operator-facing daemon intro + links to scripts/README.runner.md</name>
  <files>docs/runtime/runner-daemon.md</files>
  <action>
Create `docs/runtime/runner-daemon.md` as a THIN OPERATOR INTRO that LINKS to `scripts/README.runner.md` for deep content. Do NOT rewrite the 162 lines of `scripts/README.runner.md` — that file stays authoritative (Don't-Hand-Roll #1 from research).

Structure (follow Pattern 1 "surface-scoped reference doc with top-of-file map"):

## Header
- H1: "Runner Daemon"
- Source of truth: `scripts/mc-runner.mjs`, `scripts/README.runner.md` (authoritative deep reference), `src/lib/runner-*.ts`
- Who reads this: Operators deploying Mission Control's autonomous agent runtime
- Prerequisites: Mission Control running, Docker Desktop, Node 22+
- Map table of contents: What the runner does, Prerequisites, First run (foreground), LaunchAgent install, Boot sequence, Env vars, Exit codes, Logs layout, Configuration + secrets, Troubleshooting, Related docs

## Section: What the runner does (2-paragraph overview)
- Summarize the 7 bullets from `scripts/README.runner.md:9-21` (boots, reads secret, verifies docker, pulls config, reconciles, SSE+poll, claims tasks, seeds .mc/, runs container, reports container-started, streams logs, enforces timeout, posts runner-exit, GC tick)
- Include ONE Mermaid sequence diagram showing the claim→run→checkpoint→submit pipeline at a high level (fenced ```mermaid block). Keep it text-reviewable. Don't duplicate in INDEX.md — this is the runner-scoped view.

## Section: Prerequisites
- Single-sentence redirect: "See [`scripts/README.runner.md#prerequisites`](../../scripts/README.runner.md#prerequisites) for the authoritative list."
- Then inline the 4 bullets from that file verbatim (Docker Desktop, Node 22+, .data/runner.secret, MC server at MC_URL).

## Section: First run (foreground)
- Fenced bash block: `node scripts/mc-runner.mjs`
- "For the full boot log and troubleshooting, see [`scripts/README.runner.md#first-run-foreground`](../../scripts/README.runner.md#first-run-foreground)."

## Section: LaunchAgent install (macOS)
- 4-step bullet list quoting `scripts/README.runner.md:58-77` verbatim with a one-line lead-in.

## Section: Boot sequence (NEW CONTENT — not in README.runner.md)
- Enumerate the 7 steps from STATE.md Phase 14-08b LOCK:
  1. Read `.data/runner.secret` (exit 1 if missing)
  2. `docker info` (exit 2 if unreachable)
  3. `GET /api/runner/config` (exit 1 if unreachable — NO env-var fallback)
  4. Reconcile orphaned containers
  5. UPSERT heartbeat
  6. Subscribe SSE `task.runner_requested` + 15s poll fallback
  7. 10-minute GC tick
- Cite `scripts/mc-runner.mjs` + `src/app/api/runner/config/route.ts`.
- Note SIGHUP re-fetches config without daemon restart.

## Section: Env vars
- 4-column table "Name | Type | Default | Source" for runner-only env vars (Pattern 2 from research):
  - `MC_URL` | URL | `http://127.0.0.1:3000` | `scripts/mc-runner.mjs:49`
  - `MISSION_CONTROL_DATA_DIR` | abs path | `<cwd>/.data` | `scripts/mc-runner.mjs:48`
  - `MISSION_CONTROL_RECIPES_DIR` | abs path | `<cwd>/recipes` | `scripts/mc-runner.mjs:1048`
  - `RUNNER_ID` | string | `runner-<hostname>-<pid>` | `scripts/mc-runner.mjs:50`
  - `PORT` | int | `3000` | used by composed MC_API_URL inside container
- Note: Admin-level runtime.* settings are covered in `docs/runtime/admin-config.md`.

## Section: Exit codes
- Fenced block with the 2-column table (verbatim from `scripts/README.runner.md:51-56`):
  - 1 — Bootstrap failed (runner.secret missing or /api/runner/config unreachable)
  - 2 — Docker daemon unreachable
- Cite `scripts/mc-runner.mjs:34-36` (exit constants) and the three `process.exit(N)` call sites (lines 401, 415, 449, 1157).

## Section: Logs layout
- Single-sentence redirect + inline the log tree from `scripts/README.runner.md:110-128` verbatim.

## Section: Configuration + secrets
- Prose: "The daemon is configured via admin settings (`runtime.*`), not env vars. See [`docs/runtime/admin-config.md`](./admin-config.md) for the full settings reference."
- EXPLICIT PITFALL CALLOUT #6 — "⚠️ Secrets never flow on `docker run` argv":
  - "The CONTAINER-01 invariant forbids any `docker run -e SECRET=value` argv pattern. All secrets — including `MC_API_TOKEN` and recipe-declared `recipe.secrets` — flow via `--env-file`."
  - Show the recommended pattern: `install -m 0600 /dev/stdin .data/runner/secrets/ANTHROPIC_API_KEY <<<'sk-...'`
  - Cite `src/lib/__tests__/runner-docker.test.ts` asserting argv contains no MC_API_TOKEN=.
- Cross-link to `docs/runtime/admin-config.md#secrets-store` for the full secrets story.

## Section: Troubleshooting
- Single-sentence redirect: "See [`scripts/README.runner.md#troubleshooting`](../../scripts/README.runner.md#troubleshooting) for the canonical troubleshooting list."
- Then inline 3-4 most-common items verbatim (runner exits 1, runner exits 2, task stays in `assigned`, container exits immediately).

## Section: Related docs
- Bullet list cross-linking to:
  - `docs/runtime/admin-config.md` — all admin settings
  - `docs/runtime/agent-contract.md` — what the container does once launched
  - `docs/runtime/task-board-surfaces.md#runner-status-banner` — how runner health surfaces in the UI
  - `scripts/README.runner.md` — authoritative deep reference
  - `docs/runtime/getting-started.md` — end-to-end tutorial (will exist after Plan 05)

## Writing rules
- Every claim cites a source file with line range.
- Use ⚠️ for drift-guard callouts; no icon libraries.
- Relative links from `docs/runtime/` use `../../` to reach repo root.
- Conventional Commits, no AI attribution.
  </action>
  <verify>
    <automated>test -f docs/runtime/runner-daemon.md &amp;&amp; wc -l docs/runtime/runner-daemon.md | awk '$1 &gt;= 160 { exit 0 } { exit 1 }' &amp;&amp; grep -q "scripts/README.runner.md" docs/runtime/runner-daemon.md &amp;&amp; grep -q "CONTAINER-01" docs/runtime/runner-daemon.md &amp;&amp; grep -q "env-file" docs/runtime/runner-daemon.md &amp;&amp; grep -qE "exit 1|Exit 1|code \|" docs/runtime/runner-daemon.md</automated>
  </verify>
  <done>docs/runtime/runner-daemon.md exists, is at least 160 lines, links to scripts/README.runner.md at multiple section depths, includes the secrets-off-argv pitfall callout with CONTAINER-01 invariant language, and documents exit codes.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/runtime/admin-config.md — runtime.* settings + .data layout + auth + standalone</name>
  <files>docs/runtime/admin-config.md</files>
  <action>
Create `docs/runtime/admin-config.md` — the operator-facing admin configuration reference. This has NO pre-existing authoritative doc; it is the definitive source.

Structure (Pattern 1):

## Header
- H1: "Admin Configuration"
- Source of truth: `src/app/api/settings/route.ts` (settingDefinitions), `src/lib/task-runtime-settings.ts`, `src/lib/auth.ts`
- Who reads this: Admins configuring the runner before first use or adjusting caps/limits in production
- Prerequisites: Admin-tier auth (API_KEY with admin scope, or signed-in admin session)
- Map TOC: The 8 runtime.* settings, Mount allowlist rules, Project repo map, Concurrency + resource caps, Secrets store, Auth tiers + secrets, Data directory layout, Standalone-mode requirements, Runner-config endpoint, Related docs

## Section: The 8 runtime.* settings
- 4-column table "Key | Type | Default | Source" enumerating all 8 keys with `src/app/api/settings/route.ts:60-96` line references.
- One-paragraph operator guidance for each of the 8 keys.
- Fenced bash blocks with the `pnpm mc settings set runtime.<key> '<value>'` pattern for each.

## Section: Mount allowlist rules
- Prose: `runtime.mount_allowlist` is a JSON array of absolute paths the runner is permitted to mount as read-only refs into containers. Any `read_only_mounts[*].source` that is not a PREFIX of an allowlist entry is rejected at task creation.
- Cite `src/lib/task-runtime-settings.ts` (resolveMountAllowlist) and the validation path.
- Show example:
  ```bash
  pnpm mc settings set runtime.mount_allowlist '["/Users/me/Github", "/opt/skills"]'
  ```

## Section: Project repo map
- EXPLICIT PITFALL CALLOUT #4 — "⚠️ `runtime.project_repo_map` is the EXCLUSIVE resolution path":
  - "The runner resolves `workspace_source.project_id` to a local git repo path EXCLUSIVELY via `runtime.project_repo_map`. There is NO env-var fallback (e.g., `RUNNER_PROJECT_REPO_MAP` does not exist). A misconfigured map fails loud at boot — the daemon exits 1 on `/api/runner/config` rejection rather than silently shipping wrong repo paths."
  - Cite Phase 14-08b LOCK in STATE.md.
- Show the canonical set command:
  ```bash
  pnpm mc settings set runtime.project_repo_map '{"1":"/abs/path/to/repo-for-project-1"}'
  ```
- Note: After a settings update, send the daemon SIGHUP (`launchctl kickstart "gui/$(id -u)/com.missioncontrol.runner"` on macOS) so the new map takes effect without full restart.
- Cite `scripts/README.runner.md#project-repo-mapping` for the SIGHUP semantics.

## Section: Concurrency + resource caps
- Prose explaining how `runtime.max_concurrent_containers`, `runtime.max_memory_per_container`, `runtime.max_cpu_per_container` interact.
- Prose: Recipe-level `max_concurrent` and per-task resource limits are BOUNDED by these admin caps — the lower value wins.
- Cite `src/lib/runner-claim.ts` `resolveResourceLimits` (see STATE.md Phase 14-05 locked decisions).

## Section: Secrets store
- Prose: "The runner's secrets store is `.data/runner/secrets/<NAME>`. Each file is one secret value. The runner reads these at claim time and injects them via `--env-file`."
- EXPLICIT PITFALL CALLOUT #8 — "⚠️ `recipe.secrets` is ENV VAR NAMES, not values":
  - "Recipes declare secret names in `recipe.yaml` (`secrets: [ANTHROPIC_API_KEY]`). The server NEVER sees the values. The runner reads `/User/<path>/.data/runner/secrets/ANTHROPIC_API_KEY` at claim time."
  - Show the install command:
    ```bash
    install -m 0600 /dev/stdin .data/runner/secrets/ANTHROPIC_API_KEY <<<'sk-...'
    ```
  - "Missing secret files log a warning and are omitted from the env-file — intentional graceful degradation per Phase 14-08b."
- EXPLICIT PITFALL CALLOUT #6 — CONTAINER-01 invariant restated with link:
  - "Never pass secrets on `docker run` argv. The runner enforces `--env-file`-only; a unit test (`src/lib/__tests__/runner-docker.test.ts`) scans argv for `MC_API_TOKEN=` and asserts it is absent. See [`docs/runtime/runner-daemon.md#configuration--secrets`](./runner-daemon.md#configuration--secrets)."

## Section: Auth tiers + secrets
- 3-column table "Principal | How authenticated | What it can reach":
  - `admin` session cookie | Signed-in admin user | All routes
  - `operator` session cookie | Signed-in operator | Non-admin routes
  - `viewer` session cookie | Signed-in viewer | Read-only + own-data routes
  - `API_KEY` bearer | `Authorization: Bearer $API_KEY` | Admin-scoped API routes (non-runner)
  - `runner` (principal id -1000) | `.data/runner.secret` bearer | `/api/runner/*` only
  - `runner-token` (principal id -2000) | Per-task bearer | 7-entry allowlist (see agent-contract.md)
- Cite `src/lib/auth.ts` (runner principal + runner-token principal sections) for each.
- Prose: "Auto-generated secrets — `AUTH_SECRET`, `API_KEY`, and `.data/runner.secret` — persist to `.data/.auto-generated` on first boot. Delete that file to regenerate."
- Cite `src/lib/runner-secret.ts` (generates .data/runner.secret).

## Section: Data directory layout
- ASCII tree of `.data/` showing:
  - `.data/mission-control.db` — SQLite database
  - `.data/.auto-generated` — persisted auto-generated secrets
  - `.data/runner.secret` — runner principal bearer
  - `.data/runner/secrets/<NAME>` — recipe-declared secret values
  - `.data/runner/logs/task-<id>/attempt-<n>/` — runner logs per attempt
  - `.data/runner/worktrees/task-<id>/` — per-task git worktree (destroyed on terminal, retained for `failed_gc_window_days`)
  - `.data/runner/daemon.log` / `daemon.err` — LaunchAgent stdout/stderr
- Prose: "Set `MISSION_CONTROL_DATA_DIR` env var to move `.data/` elsewhere (e.g., volume-mount target)."

## Section: Standalone-mode requirements
- EXPLICIT PITFALL CALLOUT #7 — "⚠️ Standalone mode REQUIRES `MISSION_CONTROL_RECIPES_DIR`":
  - "`pnpm build && node .next/standalone/server.js` runs with cwd set to `.next/standalone/`, which does NOT contain your `recipes/` tree. The recipe indexer defaults `getRecipesRoot()` to `<cwd>/recipes` and will find nothing. Phase 14-10's smoke harness hard-fails with a remediation message on this misconfiguration."
  - "Always set `MISSION_CONTROL_RECIPES_DIR=/absolute/path/to/your/recipes` before launching standalone."
  - Cite `scripts/mc-runner.mjs:1047-1049` (getRecipesRoot default) and STATE.md Phase 14-10 LOCK.

## Section: Runner-config endpoint
- Prose: "The runner fetches its runtime config via `GET /api/runner/config` at boot and on SIGHUP. The response shape is `{ project_repo_map, max_concurrent_containers, failed_gc_window_days, mount_allowlist }`. Authenticated via the `runner` principal (`.data/runner.secret` bearer)."
- Cite `src/app/api/runner/config/route.ts`.
- Show a cURL spot-check:
  ```bash
  curl -s -H "Authorization: Bearer $(cat .data/runner.secret)" \
    http://127.0.0.1:3000/api/runner/config | jq .
  ```

## Section: Related docs
- Cross-link to: runner-daemon.md, recipes.md, agent-contract.md, task-board-surfaces.md, getting-started.md.

## Writing rules
- Every setting gets a source-of-truth line reference.
- All three of pitfalls #4, #6, #7, #8 rendered as ⚠️ blockquote callouts.
- Relative links; no icon libraries; conventional commits; no AI attribution.
  </action>
  <verify>
    <automated>test -f docs/runtime/admin-config.md &amp;&amp; wc -l docs/runtime/admin-config.md | awk '$1 &gt;= 200 { exit 0 } { exit 1 }' &amp;&amp; grep -c "runtime\." docs/runtime/admin-config.md | awk '$1 &gt;= 8 { exit 0 } { exit 1 }' &amp;&amp; grep -q "project_repo_map" docs/runtime/admin-config.md &amp;&amp; grep -q "EXCLUSIVE" docs/runtime/admin-config.md &amp;&amp; grep -q "MISSION_CONTROL_RECIPES_DIR" docs/runtime/admin-config.md &amp;&amp; grep -q "ENV VAR NAMES" docs/runtime/admin-config.md &amp;&amp; grep -q "CONTAINER-01" docs/runtime/admin-config.md &amp;&amp; grep -q "host.docker.internal" docs/runtime/admin-config.md || true</automated>
  </verify>
  <done>docs/runtime/admin-config.md exists, is at least 200 lines, enumerates all 8 runtime.* settings keys, addresses pitfalls #4 (project_repo_map exclusive), #6 (CONTAINER-01 secrets-off-argv), #7 (MISSION_CONTROL_RECIPES_DIR standalone), and #8 (recipe.secrets names-not-values), and documents the .data/ layout + auth tiers.</done>
</task>

</tasks>

<verification>
- Both files exist, well-formed markdown
- runner-daemon.md does NOT duplicate scripts/README.runner.md (line count ~160 vs the 500+ it would be if rewritten)
- admin-config.md covers all 8 runtime.* settings with pnpm mc commands
- All 4 targeted pitfalls (#4, #6, #7, #8) have explicit ⚠️ callouts in at least one of the two files
- Plan 07 (harness) will later grep every env var / setting key / endpoint mentioned against source
</verification>

<success_criteria>
- docs/runtime/runner-daemon.md and docs/runtime/admin-config.md created
- All verify grep assertions pass
- No changes to src/, scripts/, docker/, or recipes/ files
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-v1-2-runtime-documentation/18.1-02-SUMMARY.md`.
</output>
