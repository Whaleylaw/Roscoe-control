---
phase: 18-v1-2-tech-debt-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md
autonomous: true
requirements:
  - audit-td-4
  - RTEST-01
user_setup: []

must_haves:
  truths:
    - "No occurrence of the literal string 'indexed_error' remains in 17-02-PLAN.md frontmatter or inline documentation"
    - "Every former 'indexed_error' reference in 17-02-PLAN.md is replaced with 'error' to match the actually-shipped recipe-indexer status constant"
    - "A brief inline doc-drift correction note documents the rename and cites the audit-td-4 authority (17-02-SUMMARY / 17-VERIFICATION both already note the drift as documentation-only)"
    - "No other file is modified — only 17-02-PLAN.md"
    - "No code, test, or schema file is touched — the recipe-indexer constant was always 'error', and the test file src/lib/__tests__/recipe-indexer.test.ts already asserts 'error'"
  artifacts:
    - path: ".planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md"
      provides: "Updated plan text using the real recipe-indexer status string 'error' instead of the stale 'indexed_error' draft string"
      contains: "status.*'error'"
  key_links:
    - from: ".planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md"
      to: "src/lib/recipe-indexer.ts"
      via: "narrative references to recipe-indexer return type now use the actual shipped enum value 'error'"
      pattern: "'error'"
    - from: ".planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md"
      to: ".planning/phases/17-integration-testing-reference-pipeline/17-02-SUMMARY.md"
      via: "doc-drift correction note referencing 17-02-SUMMARY.md deviation-note already on record"
      pattern: "17-02-SUMMARY|audit-td-4"
---

<objective>
Align Plan 17-02's frontmatter and inline references from the stale draft string `indexed_error` to the actually-shipped recipe-indexer status enum value `error`, per v1.2 milestone audit item #4.

Purpose: When Plan 17-02 was authored, the recipe-indexer's error-row status was tentatively called `indexed_error` in the plan's drafted test code and frontmatter `contains:` assertion. The actually-shipped `src/lib/recipe-indexer.ts` uses the simpler constant `error`. The Phase 17-02 executor tests already assert against `error` (correctly); only the PLAN text is stale. Both `17-02-SUMMARY.md` (lines 41, 104, 115, 117) and `17-VERIFICATION.md` (line 112) explicitly document this drift as "plan-text drift, not a code bug — tests already exercise the real status string." This plan closes the documentation-only drift.

Output: A single PLAN.md file corrected — four occurrences of `indexed_error` replaced with `error`, plus a one-sentence correction note added near the top of the file documenting the Phase 18-04 rename and citing the 17-02-SUMMARY and 17-VERIFICATION deviation records.

NO CODE CHANGES. NO TEST CHANGES. NO SCHEMA CHANGES. This is a text-only drift closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md
@.planning/phases/17-integration-testing-reference-pipeline/17-02-SUMMARY.md
@.planning/phases/17-integration-testing-reference-pipeline/17-VERIFICATION.md

# The shipped recipe-indexer source — authoritative for the real status enum
@src/lib/recipe-indexer.ts

<interfaces>
<!-- The four lines in 17-02-PLAN.md that contain 'indexed_error' -->
Line 29:  "      contains: \"indexed_error\""
Line 94:  "): Promise<{ status: 'indexed' | 'indexed_error' | 'skipped_missing' | 'skipped_unchanged' }>"
Line 183: "         expect(result.status).toBe('indexed_error')"
Line 197: "         expect(result.status).toBe('indexed_error')"

<!-- The real shipped recipe-indexer status type (narrow this by reading src/lib/recipe-indexer.ts first) -->
The actually-shipped return type uses 'error' as the error-row status string. Confirm by grepping the source before editing:
```bash
grep -nE "status:.*'(indexed|error|skipped)" src/lib/recipe-indexer.ts
```

<!-- Deviation records already on disk that document this drift -->
17-02-SUMMARY.md line 41: "Plan text referenced recipe-indexer status='indexed_error'; actual shipped enum uses status='error'. Recorded as a plan-text drift, not a code bug — tests already exercise the real status string"
17-02-SUMMARY.md line 104-117: full "Plan-text drift" deviation block
17-VERIFICATION.md line 112: "Plan 17-02 plan text drift: Plan references status='indexed_error'; actual shipped enum uses status='error'. Documented in 17-02-SUMMARY. No code change required; pre-existing tests assert the real value."

<!-- Correction template -->

Line 29 before:
  contains: "indexed_error"
Line 29 after:
  contains: "error"

Line 94 before:
): Promise<{ status: 'indexed' | 'indexed_error' | 'skipped_missing' | 'skipped_unchanged' }>
Line 94 after:
): Promise<{ status: 'indexed' | 'error' | 'skipped_missing' | 'skipped_unchanged' }>

Lines 183/197 before:
         expect(result.status).toBe('indexed_error')
Lines 183/197 after:
         expect(result.status).toBe('error')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the real shipped recipe-indexer status enum + rewrite the four 'indexed_error' occurrences in 17-02-PLAN.md</name>
  <files>.planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md</files>
  <action>
    Step 1 — Verify the actually-shipped recipe-indexer enum before editing. This is a cross-check, not speculation:
    ```bash
    grep -nE "status:.*'(indexed|error|skipped)" src/lib/recipe-indexer.ts
    grep -nE "status.*'indexed'|status.*'error'|status.*'skipped'" src/lib/recipe-indexer.ts
    ```
    Also check the existing test file:
    ```bash
    grep -n "'error'" src/lib/__tests__/recipe-indexer.test.ts
    ```
    Confirm the real enum uses `error` (not `indexed_error`). If for any reason the source uses a DIFFERENT string, use whatever the source actually ships — the principle is "plan text aligns with shipped code."

    Step 2 — Grep 17-02-PLAN.md to enumerate every occurrence:
    ```bash
    grep -n "indexed_error" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md
    ```
    Expected: four matches at lines 29, 94, 183, 197 (as of the audit snapshot — confirm against the current file state before editing).

    Step 3 — Apply the four string replacements:
    - Line 29: `contains: "indexed_error"` → `contains: "error"`
    - Line 94: `'indexed' | 'indexed_error' | 'skipped_missing' | 'skipped_unchanged'` → `'indexed' | 'error' | 'skipped_missing' | 'skipped_unchanged'`
    - Line 183: `expect(result.status).toBe('indexed_error')` → `expect(result.status).toBe('error')`
    - Line 197: `expect(result.status).toBe('indexed_error')` → `expect(result.status).toBe('error')`

    Do a final grep to confirm zero remaining `indexed_error` occurrences:
    ```bash
    grep -n "indexed_error" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md
    ```
    Expected: zero matches.

    Step 4 — Insert a doc-drift correction note at the top of the file. Place it AFTER the frontmatter `---` closing delimiter but BEFORE the first `<objective>` or H1 heading:

    ```markdown
    > **Doc-drift correction (Phase 18-04 / audit-td-4):** The plan originally used `indexed_error` as the recipe-indexer error-row status string; the actually-shipped constant in `src/lib/recipe-indexer.ts` is `error`. Four occurrences (frontmatter `contains:` + inline TypeScript return-type signature + two `expect(...).toBe(...)` examples) are corrected to use `error`. The shipped test file `src/lib/__tests__/recipe-indexer.test.ts` already asserts the real value. See `17-02-SUMMARY.md` lines 41/104-117 and `17-VERIFICATION.md` line 112 for the existing deviation records.
    ```

    Step 5 — Do NOT:
    - Touch any code file (`src/lib/recipe-indexer.ts` and its tests are already correct)
    - Touch `17-02-SUMMARY.md` or `17-VERIFICATION.md` (both already document the drift on record)
    - Edit any other PLAN.md file
    - Change any other part of 17-02-PLAN.md (tasks, verification block, execution context, interfaces blocks beyond the four enumerated corrections)
    - Introduce new requirement IDs
  </action>
  <verify>
    <automated>! grep -q "indexed_error" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md &amp;&amp; grep -q "audit-td-4\|Phase 18-04" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md &amp;&amp; grep -cE "'error'" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md | awk '{ if ($1 &gt;= 2) { print "OK" } else { print "FAIL: &lt;2 'error' occurrences"; exit 1 } }'</automated>
  </verify>
  <done>
    Zero `indexed_error` occurrences remain in 17-02-PLAN.md; at least two `'error'` occurrences are present (plus the ones in the corrected TypeScript return-type union); a doc-drift correction note referencing audit-td-4 / Phase 18-04 appears at the top of the file; 17-02-SUMMARY.md and 17-VERIFICATION.md are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sanity-check + commit the single-file change</name>
  <files>.planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md</files>
  <action>
    Step 1 — Sanity check. Confirm ONLY 17-02-PLAN.md was modified:
    ```bash
    git diff --name-only
    ```
    Expected output (exactly one line):
    ```
    .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md
    ```

    Step 2 — Confirm no code, tests, config, or schema files have been modified:
    ```bash
    git diff --name-only | grep -E '\.(ts|tsx|js|jsx|mjs|cjs|sql|yaml|yml|json)$' && echo "FAIL: code/config file modified" || echo "OK: only markdown modified"
    ```
    Expected: `OK: only markdown modified`

    Step 3 — Commit the single-file change under a conventional commit. No AI attribution trailers.

    ```bash
    node ./.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(18-04): align 17-02-PLAN indexed_error references with shipped 'error' enum (audit-td-4)" --files .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md
    ```

    Commit body should state: (1) Closes audit-td-4 from v1.2-MILESTONE-AUDIT.md. (2) Four plan-text references renamed from draft `indexed_error` to shipped `error` constant. (3) Doc-drift correction note added citing 17-02-SUMMARY.md + 17-VERIFICATION.md deviation records. (4) Code, tests, schema untouched — shipped constant was already `error`.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "18-04" &amp;&amp; git log -1 --name-only --pretty=format:"" | grep -c "^\." | awk '{ if ($1 == 1) { print "OK" } else { print "FAIL: expected 1 file, got " $1; exit 1 } }'</automated>
  </verify>
  <done>
    Single `docs(18-04)` commit exists; exactly one file changed in the commit (17-02-PLAN.md); no `.ts`/`.tsx`/`.mjs`/`.cjs`/`.js`/`.sql`/`.yaml`/`.json` file appears in the commit.
  </done>
</task>

</tasks>

<verification>
Closure evidence for audit-td-4:
1. `grep -n "indexed_error" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md` returns zero matches
2. `grep -n "'error'" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md` returns at least 2 matches (line 94 return-type signature + line 183/197 expect calls)
3. `grep -q "audit-td-4\|Phase 18-04" .planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md` succeeds (doc-drift note present)
4. `git diff HEAD~1 HEAD --name-only` for this commit returns exactly one path: `.planning/phases/17-integration-testing-reference-pipeline/17-02-PLAN.md`
5. No code, test, or schema file modified
6. `pnpm test --run src/lib/__tests__/recipe-indexer.test.ts` still passes (unchanged; sanity check only, not required by this plan)

When `/gsd:audit-milestone v1.2` re-runs, audit item #4 ("Plan 17-02 frontmatter references indexed_error vs actual error") should drop from the tech_debt block.
</verification>

<success_criteria>
- ROADMAP Phase 18 Success Criterion 4 satisfied: Plan 17-02 frontmatter and any inline references to `indexed_error` are aligned with the actual `error` status string used by the recipe-indexer constant, without changing runtime behavior
- One markdown file modified, zero code files modified
- `indexed_error` eliminated from 17-02-PLAN.md
- Single `docs(18-04)` conventional commit with no AI attribution
</success_criteria>

<output>
After completion, create `.planning/phases/18-v1-2-tech-debt-cleanup/18-04-SUMMARY.md` recording:
- The single file modified (17-02-PLAN.md)
- The four line locations corrected (29, 94, 183, 197 — or the current equivalent lines after the edit)
- A before/after sample of one correction (e.g. the TypeScript return-type signature rewrite)
- Confirmation that zero code/test/config/schema files were touched
- Confirmation that audit-td-4 is closed
- requirements_completed: [RTEST-01] (plan-text alignment — already-satisfied requirement)
</output>
